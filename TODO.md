# Project To-Do & Handoff

## Current Focus
Lock in core combat + resource rules so any AI can safely work on the project.

## Recently Done (2026-08-25, battle screen pt.6) — Real pixel-art maps + character poses wired into live battle
- [x] **Random day/night battlefield backgrounds.** The user pushed real pixel-art assets to the repo under `/Artwork` (a local folder they didn't know how to add themselves — walked them through GitHub's web "Add file → Upload files" flow, then merged the resulting commit from `main` into this branch). 4 battlefield backgrounds (Day Forest Map/Map2, Night Forest Map/Map2) are now picked uniformly at random every time a battle starts or restarts (`pickRandomMapBackground()`, called from `startBattle()`/`restartBattle()`), applied to `#battlefield` as a background-image layered under a flat dark tint for legibility. Deployment screen resets to a plain solid color (`initDeployment()`) so it never shows a stale map before a battle has actually begun.
- [x] **Character art + Idle/Attack/Impacted poses.** Only 3 Ranged and 4 Melee character skins exist (far fewer than the 45-character roster), each with 3 pose images. Per direct instruction, whichever specific roster characters actually get deployed (either side) are mapped onto those skins by `assignHeroArt()` — grouped by side+type, in deployment order, cycling back to the start of the pool once a side has more of that type than the pool has skins ("use a copy of that same type"). Every hero's card shows its Idle pose by default; `triggerHeroPose(hero, 'attack'|'impacted')` briefly (550ms) swaps it to Attack (on the attacker) or Impacted (on the target) before reverting — hooked into `performAttack()` and `dealBoardDamage()` (so Arcane Bomb/Runic Wave hits also trigger Impacted, not just normal attacks).
- [x] **Real bug found and fixed while building this**: the first implementation put the pose art in an `<img class="card-art">` child positioned with `z-index:-1` to sit behind the card's own text — it rendered completely invisible. Root cause: `.card` uses `position:relative` with no explicit `z-index`, which does NOT establish a CSS stacking context on its own, so the negative-z-index image escaped past the card entirely and painted behind unrelated ancestors elsewhere on the page instead of just this card's background. Fixed by switching to a **CSS `background-image`** on the card itself (`heroArtBackground()`, shared by `buildHeroCardEl()` and `triggerHeroPose()`) instead of a positioned child element — backgrounds are guaranteed by spec to paint below all of an element's own content, no stacking-context subtleties possible. Caught via a real-browser screenshot showing a blank card, then confirmed root cause with a targeted z-index experiment (positive z-index made it visible, proving the box/content were fine and only the layering was wrong) before picking a fix.
- [x] Verified via scripted Playwright: every deployed hero (both sides) gets a real, correctly-cycling skin assignment; all art `<url>`s in the applied backgrounds resolve with zero failed network requests; the battlefield's background is a real map URL during battle and `none` during deployment; attacking correctly flips the attacker to Attack and the target to Impacted, and both cleanly revert to Idle after the delay; skin assignment and HP correctly persist/reset across `restartBattle()`; running restart several times shows the map genuinely varying, not stuck on one value; all prior features (Formation presets, deployment placement, enemy turn pause/shards) still pass with zero regressions. Followed up with real-browser screenshots confirming visual quality — the character art, map backdrop, and pose swap all render correctly together.
- [x] Updated CLAUDE.md and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues. A few pieces of the provided art have a baked-in dark rectangular background (not transparent, unlike most of the set) — visually adequate against the card's own dark theme but worth flagging if more art is added later with inconsistent backgrounds. Deployment preview cards were deliberately left on their old icon-placeholder look (art assignment isn't settled until battle start); revisit if the user wants a preview treatment too.

## Recently Done (2026-08-25, character placement page pt.6) — Formation explainer moved to a bubble; Formations button now offers 6 preset unit layouts
- [x] **Moved the old Formation popup's explainer text into a speech-bubble tooltip.** Direct request: "Take that pop-up information and create a bubble just left of the formation button." Added a small "ℹ" info button (`#btn-formation-info`) just left of the Formations button; tapping it toggles `#formation-info-bubble`, holding the same 3 melee-coverage bullets the old popup showed (the popup's 3 illustrative diagrams were dropped rather than also relocated — their job is now better served by the new preset diagrams below, which show real, complete formations instead of isolated single-rule illustrations). `positionFormationInfoBubble()` computes the bubble's position at open time and clamps it to `left: max(8px, ...)` so it can't run off the left edge of the viewport — the info button sits close enough to the page's left margin on this layout that a naive CSS-only anchor pushed the bubble partly off-screen (caught via a real-browser bounding-rect check, not just visual review). Closes on a second tap of the info button or a tap anywhere else on the page.
- [x] **The Formations button now opens a swipeable picker of 6 preset unit-placement layouts** instead of the explainer. Direct request: "give a few options for unit placement... practical placements utilizing the staggered protection effect... about six pre-set options... swipe left or right." Added `FORMATION_PRESETS` (6 entries, each a Ranged-row set + Melee-band set using real `RANGED_SLOTS` indices, every band pair pre-verified `bandsConflict()`-free) and reused the existing swipe/‹›/dots carousel mechanics (previously showing 3 hardcoded explainer slides) to browse them — each slide now shows a name, a one-line tactical blurb, and a mini diagram (`buildFormationPreviewSVG()`) instead of a static illustration. An "Apply This Formation" button applies whichever slide is currently centered.
- [x] **`applyFormationPreset()` reorganizes the player's existing picks rather than discarding them**: it clears the current deployment, then reassigns the preset's Ranged rows/Melee bands preferring the heroes the player had already placed (regardless of their previous position or type-fit), and only pulls a fresh hero from the roster to fill a slot when there aren't enough already-chosen heroes of the needed type left; if a preset needs fewer of a type than the player had out, the extras simply return to the roster.
- [x] The six presets, each demonstrating a different practical use of aligned vs. staggered (boundary) coverage: **Twin Wall** (3R/2M, two boundary guards covering 3 lanes — max efficiency), **Spearhead** (1R/4M, one protected unit behind a wall of blades), **Checkerboard** (3R/2M, spread lanes with one flank deliberately exposed), **Bottleneck** (4R/1M, a wide line funneled through one choke point), **Vanguard Brawl** (2R/3M, one guard plus two forward melee for extra frontline damage), **Solo Guard** (1R/1M, the minimal pair — deliberately smaller than `TEAM_CAP`, a lean starting option).
- [x] Widened `#placement-controls` from 220px to 300px max-width — the new info button pushed the row's natural content width past the old cap, which (with `justify-content:center`) was causing the whole row to silently overflow/shift off-center rather than visibly break; caught via a real bounding-rect check that showed the first child's left edge sitting left of the container's own left edge, not by eyeballing a screenshot.
- [x] Verified via scripted Playwright: bubble open/close (toggle + outside-click) and its exact text content; modal opens with 6 dots and the correct slide-0 title; carousel navigation (‹/›) advances/retreats through the real `FORMATION_PRESETS` order; applying each of the 6 presets in turn produces the exact expected Ranged-filled count, Melee count, deployed total, and on-screen melee card count (no dropped/duplicated cards); a downsize test (apply a 5-total preset, then a 2-total preset) confirms exactly 3 heroes return to the roster; existing manual placement (tap "+"/"−", Filter panel toggle) still works correctly after using a preset — zero console errors throughout. Followed up with real-browser screenshots confirming the bubble renders fully on-screen and legible, and that two different preset diagrams (Twin Wall, Spearhead) plus the resulting live battlefield cards all render correctly with no visual overlap.
- [x] Updated CLAUDE.md and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues. The 6 presets were designed for variety and to showcase the staggered-coverage mechanic, not derived from any in-game balance data — worth revisiting once real playtesting or a meta/tier system exists to confirm they're actually good picks, not just valid ones.

## Recently Done (2026-08-25, battle screen pt.5) — Slight pause before the enemy turn starts; enemy mana shards fall from off-screen above
- [x] **Added a deliberate pause before the enemy acts.** The player-turn-to-enemy-turn handoff already had a `setTimeout(enemyTurn, 600)` before this round, but per direct feedback ("give a slight pause before the enemy goes") it wasn't reading as a real beat. Extracted it into a named `ENEMY_TURN_START_DELAY_MS` constant and bumped it to 900ms — the "Enemy Turn..." indicator and board-lock still appear instantly (so the handoff itself feels responsive), but the enemy's mana boost/attacks now wait a beat before actually happening.
- [x] **Enemy mana gain now shows shard particles falling from above the screen**, dramatizing the existing "simulates their match" mana boost the same way player matches already spawn shards flying to a hero's mana bar. Since the enemy has no visible match-3 board of their own to clear tiles from, per instruction the shards instead originate from just off-screen above the viewport (`top: -20px`, i.e. "as if their mana board is hidden above the screen") and fall straight down into that hero's mana bar. New `spawnEnemyManaShards(hero)`, called for each of the 1-2 randomly-boosted enemies right alongside their mana grant in `enemyTurn()`.
- [x] Factored the previously-inline particle-animation loop out of `spawnManaShards()` into a shared `flyManaShards(startX,startY,endX,endY,hex,count)` helper, since `spawnEnemyManaShards()` needed the exact same spawn/stagger/fade/cleanup behavior with only the start point and hex color differing — avoided a second copy of that ~20-line loop.
- [x] Pushed the enemy's first attack delay from 400ms to 700ms so it starts after the mana shards' flight (~590ms including their stagger) has visibly landed, rather than attacking before the player can see where the mana went. Purely a pacing tweak — the underlying mana value was always granted synchronously, before any shard visual.
- [x] Verified via scripted Playwright: triggering `endPlayerTurn()` directly and sampling at 700ms shows zero shards (proving the pause holds), sampling past 900ms shows shards created (count = 3 per boosted enemy) with live `getBoundingClientRect()` positions confirming they're genuinely mid-flight through the visible viewport, and they fully self-remove once their flight completes. Followed up with a real-browser screenshot mid-flight showing two small glowing diamonds descending toward an enemy hero's mana bar. Zero console errors.
- [x] Updated CLAUDE.md and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-25, battle screen pt.4) — Animated tile swap, HP bars, and mana shard particles
- [x] **Tile swap now slides visually instead of teleporting.** New `animateTileSwap(x1,y1,x2,y2,onDone)` swaps the board data first, re-renders, then FLIP-animates (`getBoundingClientRect()` deltas + `transform:translate()` + forced reflow + `requestAnimationFrame`) the two tile elements sliding from their old positions into their new ones over `SWAP_SLIDE_MS = 160`ms. `attemptTileSwap()` was rewritten around it: the slide plays first, then match detection runs; if there's no match (and no bomb activation), the same slide plays again in reverse to visually revert the swap. `isAnimating`/board-lock now get set at the very top of `attemptTileSwap()` (previously only inside the has-match branch) since there's now a real async window before the outcome is known.
- [x] **Live battle HP is now a depleting bar, not plain text.** New `.hp-bar-bg`/`.hp-bar`/`.hp-num` CSS: a dark track, a red-gradient fill sized to `hp/maxHp` (CSS `transition: width` for a smooth drain), and the numeric `hp/maxHp` ratio still overlaid on top as text, per instruction ("leave the number visible for now"). Wired into `buildHeroCardEl()` (initial render) and `updateHeroUI()` (updates the bar's `width` + the overlay text on every HP change). Scoped to live battle only — the deployment screen's preview card (`buildDeployPreviewCard`) still shows static `.hp-text` ("N HP") since it only ever shows full/undamaged HP.
- [x] **Matched tiles now spawn glittering shard particles that fly to the matching hero's mana bar.** New `spawnManaShards(x,y,color)`: for every living player hero whose `color` matches the cleared tile, spawns 3 small diamond-shaped particles (`.mana-shard`, `#shard-layer` fixed-position overlay, `z-index:500`) at the tile's screen position with slight random jitter, then (staggered ~45ms apart) animates each one's `left`/`top`/`opacity`/`transform` toward the center of that hero's `.mana-bar-bg`, fading and shrinking as it arrives, then removes itself. Hooked into the single commit point in `processMatches()` where all cleared cells (normal matches, bomb blasts, crystal full-clears) get nulled out, so the effect fires uniformly across every clear path, not just plain 3-matches.
- [x] Verified via scripted Playwright: HP bar width/number correctly track HP (100%/"4/4" → 50%/"2/4" after damage + `updateHeroUI`); mid-swap the tile element has a non-identity `transform` matrix applied and `isAnimating` is true (proof the slide is actually running, not skipped); after a forced match, shard elements were created (multiple per matching hero) and fully auto-removed themselves once their flight completed; zero console errors throughout. Followed up with a real-browser visual pass (deploy 3 heroes → Start Battle → force damage → screenshot; force a same-color match → screenshot mid-flight): the HP bar reads clearly at a glance, and mid-flight shards render as small glowing diamonds departing the matched tile's cell, matching the "tiny glittering shards" request.
- [x] Updated CLAUDE.md and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, battle screen pt.3) — Fixed can-act glow going stale for unrelated heroes; added a full-mana glow for Heroes
- [x] **Fixed a real bug in the just-added "glowing hero" cue**: user report "User characters should always glow when they can attack" surfaced that the `.card.can-act` glow wasn't reliably refreshing. Root cause: `grantMana()` only calls `updateHeroUI()` on heroes of the matched color, so a hero whose color wasn't part of a given swap's match (but who could still act, e.g. from mana banked on a previous turn) never got its glow class re-toggled after `swapUsedThisTurn` flipped true. Fixed by moving `heroes.forEach(updateHeroUI)` into `checkAutoEndPlayerTurn()` itself — the one function already called after every swap, attack, and Do Nothing — so every living hero's card (glow included) refreshes on every such checkpoint, not just the ones an action happened to touch directly.
- [x] **Added a second, distinct glow**: a Hero's (`isSpecial`) mana bar now gets its own gold pulse (`.mana-bar.mana-full`) once `mana >= maxMana`, toggled in `updateHeroUI()`. Normal characters never get it, regardless of how full their bar is — no Special to signal readiness for, per direct instruction ("only for heroes").
- [x] Verified via scripted Playwright: a controlled setup with two player heroes of different colors, both already holding enough mana to act, confirms BOTH now show `can-act` after a swap that only matched one of their colors (previously only the matched-color hero would); the Hero's mana bar shows `mana-full` at max mana while the normal character's does not, even though its own bar is also fully filled; confirmed the gold box-shadow is actually rendering (not just the class present) via computed style.
- [x] Updated CLAUDE.md's Core Rules and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, battle screen pt.2) — Logical Targeting + new tap-to-attack input model, replacing the old select-then-select flow
- [x] **Replaced the old two-tap "select your hero, then tap an enemy to confirm" attack flow** with a one-tap default + optional pre-aim, per direct spec. Tapping a player hero that can currently act (now visually "glowing" — see below) attacks immediately: a normal character straight to Basic, a Hero (`isSpecial`) after its existing Basic/Special/Do Nothing popup choice (popup itself unchanged). Tapping an enemy no longer attacks by itself — it only sets an optional pre-aim; tapping the already-aimed enemy again clears it. Order matters: **aim is enemy-first, then hero** ("tap enemy, then hero for aimed attack") — the reverse of the old hero-first flow.
- [x] **Added Logical Targeting** (`pickLogicalTarget()`) as the default target whenever the player doesn't pre-aim: ranks the existing legal-target list (`getValidTargets()` — unchanged melee/guarded-ranged rule, never pixel/visual distance) by (1) highest current mana — "closest to Special" since `SPECIAL_COST` is one flat number for everyone — (2) then lowest HP, (3) then nearest lane to the attacker's own (new `heroLane()` helper, unifying Ranged's `laneIndex` and Melee's `primaryLane(coverage)`). A pre-aimed target that turns out illegal for the specific hero tapped (e.g. aimed at a guarded Ranged unit, then a Melee hero is tapped) silently falls back to the Logical Target rather than blocking the attack.
- [x] **Added a "glowing" visual cue** (`.card.can-act`, gold pulsing box-shadow via a new `canActPulse` keyframe animation) for every player hero that could currently be tapped to act — driven by a new `canHeroAct(h)` helper mirroring `onHeroClick`'s own gating, recomputed on every `updateHeroUI()` call so it stays live. The pre-aimed enemy shows the pre-existing green `.card.selected` glow, repurposed (see below).
- [x] **Retired `selectedHero`/`selectedAttackType`** entirely (removed the variables and every reset site — `processMatches`, `endPlayerTurn`, `startBattle`, `restartBattle`, `onTilePointerDown`, the modal handlers) — that state existed only to remember "which hero is waiting for a target tap," which no longer happens now that attacks resolve in one hop. Replaced with a new `aimedTarget` (enemy hero or null) wherever the old variable was being defensively reset, and repurposed the existing `.card.selected` CSS class to mean "this is the pre-aimed enemy" instead of "this player hero is mid-selection."
- [x] `resolveAttack(attacker, attackType)` is the new shared entry point both the direct one-tap path (normal characters) and the modal's Basic/Special buttons call into — it picks aimed-vs-logical, consumes the aim, then calls the existing `performAttack()` unchanged.
- [x] **Deliberately did not touch Auto Attack** — it still picks a uniformly random valid target, not Logical Targeting. This wasn't part of the request (which was scoped to manual play specifically, "when not aiming") and Auto Attack was explicitly scoped as "random valid target" in an earlier exchange; flagging this as a possible fast-follow if the user wants Auto Attack upgraded to Logical Targeting too, but not assuming it.
- [x] Verified via scripted Playwright: a normal character's one-tap attack correctly targets the highest-mana legal enemy (tie-broken by lowest HP, confirmed with a controlled mana/HP setup); pre-aiming a *non*-logical target and then tapping a hero correctly attacks the aimed target instead; tapping the same aimed enemy twice clears the aim; a Hero still opens its popup on tap and resolves via Logical/aimed targeting only after Basic/Special is chosen; the can-act glow is absent before the swap, appears once affordable, and disappears once the hero has acted; aiming at a target illegal for the eventual attacker (guarded Ranged vs. a Melee hero) correctly falls back to the Logical Target instead of blocking the attack.
- [x] Updated CLAUDE.md's Core Rules and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues. Worth asking the user whether Auto Attack should also switch from random targeting to Logical Targeting, now that the logic exists and is shared/reusable (`pickLogicalTarget()`) — not assumed here since it wasn't requested.

## Recently Done (2026-08-19, battle screen) — Added Auto Attack toggle (random valid target, Heroes sometimes bank mana)
- [x] **Added an "Auto Attack" toggle** (`#btn-auto-attack`) to the battle screen, per direct instruction after a sizing discussion — confirmed scope first ("random valid target" for targeting, like the enemy AI already uses) before building. Positioned far-left of the End Turn button in the same row, with End Turn staying genuinely centered.
- [x] When on: after the player's own swap resolves (Match-3 board itself stays fully manual, untouched by this feature), each living player hero that can afford its Basic auto-attacks a random valid target — reusing `getValidTargets()`, the exact targeting rule the existing enemy AI (`enemyTurn()`) already uses. A Hero (`isSpecial`) that can already afford Special always uses it; one that can only afford Basic gets a random 40% chance (`AUTO_ATTACK_BANK_CHANCE`) to bank mana instead of attacking — "saving for a greater attack," per the request — functionally the same as manually picking Do Nothing. Normal characters always attack if able, same rule as manual play. Paced one hero at a time (500ms) via `scheduleAutoAttack()`/`runNextAutoAttack()` so it's followable, not instant.
- [x] The toggle is a standing preference (`autoAttackEnabled`), not per-battle state — survives Restart Battle and Change Deployment without needing to be re-enabled, verified via test. `cancelAutoAttack()` (clears any pending scheduled hop) is called alongside the existing `endTileDrag()` at the same reset points (`endPlayerTurn`, `startBattle`, `restartBattle`) so a stray timer from a previous battle can never fire into a new one.
- [x] **Layout bug found and fixed while building this**: absolutely-positioning both buttons inside `#battle-controls-row` (needed so End Turn's centering is independent of Auto Attack's own width, which changes between its "OFF"/"ON" labels) caused the row itself to collapse to zero width — `#end-turn-wrap` is a flex child of `body` under `align-items: center`, and with both its descendants taken out of normal flow via `position: absolute`, it had no remaining content to size itself against and shrank to 0, silently placing both buttons on top of each other at the page's center. Fixed by giving `#end-turn-wrap` an explicit `width:100%; max-width:420px;` so it no longer depends on its now-absolutely-positioned children for sizing. Also shortened both labels ("▶ Auto: OFF"/"ON", "End Turn" — full descriptions moved to `title` tooltips) since the original longer labels were wide enough to collide even after the width fix.
- [x] Verified via scripted Playwright: Auto Attack sits flush at the row's left edge, End Turn's center matches the row's center exactly, the two buttons' rects don't overlap; a full auto-attack cascade correctly resolves all player heroes (including a Special use) and hands the turn to the enemy; disabling mid-cascade immediately stops further auto-attacks; the setting and a clean (non-leaked) timer state both survive `restartBattle()`.
- [x] Updated CLAUDE.md's Core Rules and context/GAME_MECHANICS.md (renamed section from "Auto-Play Toggle" to "Auto Attack Toggle" to match the actual button name).
- [ ] Next AI: no known issues. The 40% bank chance and 500ms pacing are first-guess numbers, not something the user specified precisely — worth tuning if it feels too aggressive/passive or too fast/slow once played for real.

## Recently Done (2026-08-19, match-3 board pt.2) — Arcane Bomb now also activates on a direct tap
- [x] **Fixed: tapping an Arcane Bomb did nothing — only a touching swap detonated it.** User report: "Tapping the arcane bomb does not make it explode, I have to swap a tile with it for it to activate." This was a real gap, not by design — the Crystal had tap-to-activate from the start, but the Bomb never did.
- [x] Generalized `activateCrystalTap(x, y)` → `activateSpecialTap(x, y, type)`, called for both `'crystal'` and `'bomb'` from `onTilePointerUp()`'s no-drag (plain tap) branch. The Bomb keeps every one of its existing triggers too (matched into a group, any adjacent swap touching it) — tap is purely additive, and (unlike the Crystal) the Bomb can still be freely dragged as part of a normal swap.
- [x] Verified via scripted Playwright: a plain tap (pointerdown+pointerup, zero movement) on a Bomb now detonates it and deals damage; Crystal tap-to-open-modal still works (regression check); Bomb still detonates via a touching swap with no color-match involved (regression check).
- [x] Updated CLAUDE.md's Core Rules and context/GAME_MECHANICS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, match-3 board) — Removed tile letters, replaced tap-tap swap with tap-hold-drag/swipe
- [x] **Removed the color-letter (R/G/B/Y/P) from normal board tiles** — `renderBoard()` now sets `textContent` to `''` for a plain colored tile; only special tiles still show anything (💣 bomb, 🔮 crystal). Bumped `.tile`'s `font-size` from 0.7rem to 0.9rem now that it only ever needs to fit a single emoji, not a letter.
- [x] **Replaced the old tap-tile-1-then-tap-tile-2 swap flow with tap-and-hold-then-drag/swipe-onto-a-neighbor**, per direct instruction ("like most well known match 3 games do it"). `onTileClick()` is gone, replaced by `onTilePointerDown()`/`onTilePointerMove()`/`onTilePointerUp()`/`onTilePointerCancel()` (pointer events — works for touch, mouse, and pen alike, mirroring the drag-to-place pattern already used on the deployment screen). A swap fires as soon as the drag crosses a 16px threshold in a clear direction (`TILE_SWIPE_THRESHOLD_PX`) — no need to release first, one swap per gesture. Added `touch-action: none` to `.tile` so touch drags on the board are never intercepted as a native page scroll.
- [x] The old two-tap "select tile 1, then tile 2" state (`selectedTile`) is gone entirely — removed the variable and every reset site (`processMatches`, `endPlayerTurn`, `startBattle`, `restartBattle`), replaced where relevant with `endTileDrag()` (which also cleans up the window-level pointermove/pointerup/pointercancel listeners, not just a variable).
- [x] Extracted the actual swap-and-check logic into `attemptTileSwap(x1, y1, x2, y2)` and the crystal-tap-activation logic into `activateCrystalTap(x, y)` — both are now called from the new pointer handlers instead of being inlined in one big click handler. Preserved every existing rule exactly: a crystal can never be one half of a swap (dragging onto/off one is a no-op; only a direct tap with no drag activates it), a bomb still detonates from any adjacent swap touching it (matched or not), reverting a no-match swap still works, and `swapUsedThisTurn`/`isAnimating`/turn-phase gating are all unchanged.
- [x] Verified via scripted Playwright, each scenario in its own fresh page load to avoid cross-test async contamination: no letters on normal tiles; a vertical drag-swap that creates a match resolves correctly; a swap with no match correctly reverts; a drag off the edge of the board is a safe no-op (no crash); dragging a neighbor onto a crystal is a no-op while a plain tap on it still opens the choice modal; a bomb still detonates from a touching swap with no color-match involved.
- [x] Updated context/GAME_MECHANICS.md (new input-mechanism bullet, fixed stale `onTileClick()` references in the Bomb/Crystal activation section).
- [ ] Next AI: no known issues. Real on-device touch feel not verifiable in this environment — the synthetic pointer-event tests confirm the logic is correct, but genuine touchscreen swipe responsiveness needs the user's on-device confirmation (consistent with how prior touch-related changes in this project have been flagged).

## Recently Done (2026-08-19, character placement page pt.5) — Removed info/log text on both screens, relocated Character Codex, split Filter onto its own row
- [x] **Removed the instructional blurb (`#info-text`) and the running message log (`#log`) from both the placement screen and the live battle screen**, per direct instruction with an annotated screenshot (both crossed out, on both pages). Kept both elements in the DOM (just `display: none` via a new shared CSS rule) rather than deleting them outright — `log()` writes to `#log` unconditionally on nearly every game action (attacks, matches, turn transitions, victory/defeat) with no null-guard, so removing the element would throw. Confirmed via a live test that `log()` still runs without error while invisible.
- [x] **Character Codex** was a permanent top-of-page link on both screens (`.topbar`/`.nav-btn`, present regardless of game phase) — it's deployment-only now, per direct instruction ("remove codex button" on the battle page). Removed the standalone topbar entirely and folded the link (`#btn-character-codex`, styled to match Formation/Filter) into `#placement-controls`, so it's shown/hidden by the exact same toggle as Formation and the deployed-count text — no separate wiring needed.
- [x] **Repositioned the placement screen's controls into two rows**, per direct instruction: row 1 (`#placement-controls`) is Formation → "Deployed: N/5" → Character Codex, left to right; row 2 (new `#filter-toggle-wrap`) is the Filter toggle alone, centered, directly underneath. `#deploy-filter-bar` (the expanding panel) now sits below THIS row instead of the row above, so it still opens directly under the button that reveals it. Also fixed a latent (pre-existing, not user-reported) staleness bug while touching this code: the Filter button's own label/open-styling wasn't being reset on a fresh `initDeployment()` call, so re-entering deployment after a battle could show "▴ Filter" (open-styled) even though the panel itself was freshly collapsed — added an explicit reset alongside the existing `deployFilterBarOpen = false`.
- [x] Verified via scripted Playwright + screenshots: `.info`/`#log` confirmed `display: none` on both screens; Codex link present, correctly targets `Roster.html`, visible on deploy and invisible (parent hidden) on battle; Filter row appears below the Formation/Deployed/Codex row and the expanding panel appears below that.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, character placement page pt.4) — Collapsible Filter panel + Colors sub-filter, status text reworded, controls row reordered
- [x] **Filter chips moved behind a collapsible "▾ Filter" toggle button** (`#btn-filter-toggle`), per direct instruction — the existing Melee/Ranged/Heroes/Non-Heroes/Clear chip bar (`#deploy-filter-bar`) is now hidden by default and only expands when tapped (chevron flips ▾→▴, button fills gold while open). Reset closed every time deployment is (re)entered.
- [x] **Added a "🎨 Colors" second-level disclosure** within the filter panel: tapping it reveals a sub-row of 5 circular color chips (Red/Blue/Green/Yellow/Purple, using the existing `COLOR_HEX` palette). These are the real filters (`deployActiveFilters` gained `'color-<name>'` entries) — "Colors" itself only shows/hides the sub-row, it doesn't filter anything on its own. `renderDeployRoster()`'s filter logic extended with a third OR-within/AND-across group (color), matching the existing type/hero-status pattern exactly.
- [x] **Clear button now grays out (and is unclickable, via `pointer-events:none`) until at least one filter is active** — `.filter-chip.clear-chip.disabled`, toggled by `deployActiveFilters.size > 0`.
- [x] **Status text changed** from "0 of 5 characters deployed" to "Deployed: 0/5" (`updateDeployStatus()`).
- [x] **Reordered/regrouped the placement screen's controls row**: `#formation-btn-wrap` (right-aligned, previous change) replaced with `#placement-controls` — a single centered horizontal row holding, left to right: Formation button, "Deployed: X/5", Filter toggle button — per direct instruction on ordering. `#deploy-status` moved out of `#deploy-panel` into this row (same element/id, just relocated + restyled to fit inline). `#deploy-filter-bar` also moved out of `#deploy-panel` to sit directly below this row (so the expanded filter panel appears right under the Filter button that opens it), rather than above the roster list where it used to live.
- [x] Verified via scripted Playwright: status text format, filter bar starts collapsed, Clear starts disabled and becomes enabled the instant a filter is applied, Colors sub-row appears/disappears on toggle, a Melee+Red combined filter correctly narrows the roster to only red Melee characters (6 of 19 in the test run), Clear resets both the filter state and the disabled look; live battle screen confirmed to hide both `#placement-controls` and `#deploy-filter-bar`.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, character placement page pt.3) — Formation button moved outside the placement box
- [x] **Moved the Formation button from inside/overlapping the placement box to a new row directly underneath it**, right-aligned — per user feedback on a screenshot ("the formation button is inside the placement section... It should sit just outside to the bottom right. Or maybe it should be just underneath it"). Previously it was `position: absolute` inside `#battlefield`, which could overlap a card in the bottom row (visible in the screenshot they sent).
- [x] Converted it from a dynamically-rebuilt element (previously recreated inside `renderDeployBattlefield()` on every render) to a static element (`#formation-btn-wrap` > `#btn-formation`) toggled the same way as `#end-turn-wrap`/`#deploy-panel` — shown in `initDeployment()`, hidden in `startBattle()` — since unlike the placed-hero cards, it never needs to change per-render. `#formation-btn-wrap` shares `#battlefield.deploy-battlefield-narrow`'s 220px max-width and right-aligns the button within it (`justify-content: flex-end`), so "right-aligned" tracks the actual box edge rather than the wider page width. Removed the now-unneeded `position: relative` from `#battlefield` (nothing else used it).
- [x] Verified via scripted Playwright with the bottom row of both columns filled (the overlap-prone case): button sits 16px below the box's bottom edge with zero vertical overlap, right edge flush with the box's right edge; modal still opens/closes correctly; confirmed hidden (`display: none`) on the live battle screen.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: no known issues. Still more changes expected on this same placement screen per the user.

## Recently Done (2026-08-19, character placement page pt.2) — Outer melee dashed box removed, Formation button + explainer modal added
- [x] **Removed the outer dashed "placement lining" that wrapped the entire Melee column** (`.bf-column.dropzone-hint`'s `outline`) per direct instruction ("remove outer dashed line... but leave the inner dashed line") — kept the individual "tap + to place" empty-slot cards' own dashed borders (`.card.empty-slot`'s `border`) untouched, and kept the empty-melee-placeholder's stretch-to-fill-column-height sizing rule (it's scoped to the same `.dropzone-hint` class, which is still added to the Melee column in `renderDeployBattlefield()` — now purely a scoping marker, no visible styling of its own).
- [x] **Added a "🛡 Formation" button** to the bottom-right corner of the placement box (`#btn-formation`, absolutely positioned within `#battlefield`, which is now `position: relative`). Deployment-only: rebuilt fresh every `renderDeployBattlefield()` call (so it never needs separate show/hide toggling) and never appears on the live battle screen, verified via a live-browser check.
- [x] **Added a Formation explainer modal** (`#formation-modal`) opened by that button: one static page with the three requested lines as a bullet list, plus a 3-slide swipeable image carousel below it (inline SVG mini-diagrams, no external image assets exist in this project) illustrating each: (1) a Melee card blocking an incoming enemy arrow, (2) a Ranged card safely behind a Melee card in the same lane, (3) one Melee card straddling two Ranged cards, both incoming arrows stopped at the one Melee. Carousel supports touch/mouse swipe (pointerdown→pointerup dx threshold), tappable dot indicators, and ‹/› buttons; closes via a ✕ button.
- [x] **Bug found + fixed while building the carousel**: the ‹/› nav buttons were unclickable — `#formation-track`'s own `transform` (used for the slide-offset animation) implicitly creates a CSS stacking context per spec even though the element has `position: static`, and being later in the DOM than the buttons, painted above them despite the buttons being `position: absolute`. Fixed by giving `.formation-nav` an explicit `z-index: 2`.
- [x] Verified via scripted Playwright: outer outline confirmed `outline-style: none` on the Melee column; Formation button present and anchored to the bottom-right corner; modal opens/closes; ‹/› buttons and dot clicks both advance/jump slides correctly; simulated swipe gestures in both directions correctly advance and clamp at the ends; live battle screen confirmed to have no Formation button and no leftover `deploy-battlefield-narrow`/dropzone-hint styling.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: the user said there's still more coming for this same placement screen — check with them before assuming it's done. No known issues with what's implemented so far. The carousel SVG diagrams use plain emoji glyphs (🏹/⚔️/🛡) for icons — if/when this project adds real character art or a custom icon set, these could be swapped to match.

## Recently Done (2026-08-19, character placement page) — Removed enemy section, centered + de-labeled the YOU columns
- [x] **Removed the enemy section from the deployment/character-placement screen entirely** (`renderDeployBattlefield()`), per direct instruction — previously a single "ENEMY ❓" placeholder column stood in for the hidden enemy team; now nothing is rendered for the enemy at all on this screen.
- [x] **Centered the player's Ranged/Melee columns** in the freed-up space — added a `deploy-battlefield-narrow` CSS class (halves `#battlefield`'s max-width 420px → 220px) applied only during deployment, removed again by `renderBattlefield()` so it never affects the live battle screen. No new centering CSS was needed beyond that — the parent `body` already centers its children via flex `align-items: center`.
- [x] **Removed "YOU" from the column labels** on this screen — `makeColumnEl('player', ...)` calls in `renderDeployBattlefield()` now pass just `'🏹'`/`'⚔️'`, no "YOU" prefix. The live battle screen's labels (`renderBattlefield()`) are untouched — still "YOU 🏹"/"YOU ⚔️"/"ENEMY ⚔️"/"ENEMY 🏹".
- [x] Verified via headless-browser screenshot + `getBoundingClientRect()`: deployment screen shows only the 2 player columns, no "YOU" text, box exactly centered in the viewport (`left:100, right:320` of 420px); live battle screen still renders all 4 original columns/labels unchanged, `deploy-battlefield-narrow` class confirmed absent there.
- [x] Updated CLAUDE.md's Core Rules, context/GAME_MECHANICS.md, and context/DECISIONS.md.
- [ ] Next AI: **the user said they have another change coming for this same screen** — check with them before assuming this page is "done." No known issues with what's implemented so far.

## Recently Done (2026-08-19, newest) — Runic Crystal now also auto-triggers via a same-color match, not just tap
- [x] **Extended the Bomb's existing "matched into a new group" auto-trigger to the Runic Crystal too**, per direct instruction after a clarifying exchange: "Runic Crystal and Arcane Bomb are supposed to auto trigger if the player swaps a tile of the same color that it's connected to" / "Runic crystals should not trigger immediately unless two more tiles of the same color connected with it." Investigated first (see below) — match-4/5 spawn detection itself was already correct and verified via direct testing; the actual gap was that a pre-existing Crystal caught in a fresh same-color match did nothing special (design was deliberately tap-only), while a Bomb already auto-detonated in that same situation.
- [x] Removed the `if (type !== 'bomb') return;` guard in `queueExplosionIfSpecial()` (inside `processMatches()`, ~line 2100) — the rest of the pipeline (`processExplosionQueue()`, `applyCrystalEffect()`) was already written generically enough to handle a match-caught Crystal exactly like a directly-tapped one, once the gate was lifted; no other code changes were needed. A cell can only join a match group in the first place if it's part of a ≥3 connected run, so "the crystal + at least two more connected tiles of its color" falls out of the existing match-detection logic for free — no extra threshold check was added.
- [x] The Bomb's other trigger (any adjacent swap touching its cell, regardless of color/match) is unchanged — kept since removing it wasn't requested and it's documented, tested, pre-existing behavior.
- [x] Verified via scripted Playwright: (1) match-4 straight line correctly spawns a Bomb, (2) `findMatchGroups()` correctly identifies an L-shaped 5-cell group with the right intersection cell (crystal-spawn case), (3) a pre-existing red Crystal, with a swap connecting two more red tiles into it, now correctly auto-opens the choice modal and resolves the chosen effect, (4) a pre-existing red Bomb in the same setup still auto-detonates exactly as before (regression check, unaffected by the change).
- [x] Updated CLAUDE.md's Core Rules (Match-3 Match Type Rules & Special Tiles), context/GAME_MECHANICS.md, and context/DECISIONS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, latest of all) — End Turn button now visually grayed out, not just click-blocked
- [x] **Grayed out the End Turn button** while it's unusable, per direct instruction ("Gray out end turn button until all normal characters have attacked that round") — previously it was only gated at click-time (clicking did nothing but log a message), giving no visual signal it was unavailable.
- [x] Added `updateEndTurnButtonState()` (~near `normalCharacterMustActFirst`): sets `btn.disabled` and dims to `opacity: 0.4` (same enabled/disabled visual convention already used by the Basic/Special modal buttons) unless it's the player's turn, the swap has been used, AND no normal character can still afford its attack — otherwise full opacity and `disabled = false`. Hooked into `updateHeroUI()`, `updateTurnUI()`, and the top of `checkAutoEndPlayerTurn()` so it recomputes on every state change that could affect eligibility (swap completion, any attack, mana grants, turn start/end) without needing a new call site at every mutation.
- [x] Verified via scripted Playwright across the full sequence: disabled pre-swap, still disabled post-swap while the normal character (Vesper Quill) hasn't attacked, auto-enables the instant she attacks, and a click at that point correctly ends the turn.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, even later) — End Turn now blocked until normal characters attack
- [x] **Fixed a bug in the just-added End Turn button**: it let a normal (non-`isSpecial`) character skip its mandatory attack, which contradicts the pre-existing rule (see `checkAutoEndPlayerTurn`'s comment) that normal characters have no "Do Nothing" and must attack if they can afford it. User report: "End turn button should not be accessible until after normal characters attack. They must attack if possible."
- [x] Added `normalCharacterMustActFirst()` (~before the button handler): finds the first living, unacted player character with `isSpecial === false` that can still afford its Basic. The button's click handler now checks this after the swap gate and, if found, logs `"<name> can still attack and has no Do Nothing option — it must attack before you can end your turn."` and does nothing — same click-time-gate pattern as the existing swap check, not a grayed-out/disabled style. Heroes (`isSpecial: true`) are unaffected — the button still banks their mana freely once every normal character has either attacked or can no longer afford to.
- [x] Verified via scripted Playwright: click blocked (turn stays `'player'`, no state change) while a normal character with enough mana hasn't acted; after that character attacks, the same click now succeeds and banks the Hero's mana without forcing it to act.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: no known issues.

## Recently Done (2026-08-19, latest) — "End Turn" button to bank mana instead of per-hero Do Nothing
- [x] **Added a manual "End Turn" button**, per direct request: with a mixed team of Heroes and regular characters, previously the only way to skip a hero's attack (to bank its mana for a future Special/heavier basic) was opening that hero's action popup and tapping "Do Nothing" — and regular (non-`isSpecial`) characters didn't even have that option, since they select straight into Basic with no popup. The new button (`#btn-end-turn`, in `#end-turn-wrap`) sits between `#battlefield` and `#board-wrap` in the DOM — i.e. between the Battlefield and the Match-3 board — and is shown/hidden alongside `#turn-indicator`/`#battle-controls` (`startBattle()` shows it, `initDeployment()` hides it).
- [x] Clicking it still requires the turn's mandatory swap first (same gate as attacking — logs the same "swap first" message via `swapUsedThisTurn` if clicked early). Once available, it marks every living, not-yet-acted player hero's `actedThisTurn = true` (banking their mana without spending it — identical semantics to the existing per-hero "Do Nothing"), then calls the existing `endPlayerTurn()`. No changes to `checkAutoEndPlayerTurn()`'s automatic path — this is an additional manual override, not a replacement.
- [x] Verified via a scripted Playwright pass (headless, `page.evaluate` driving `CHARACTERS`/`heroes` directly): guard message before swap, all-heroes-banked + turn correctly handed to enemy after swap.
- [ ] Next AI: no known issues. Not yet confirmed on a real phone/touchscreen.

## Recently Done (2026-08-19) — GitHub Pages hosting + mobile roster scroll fix
- [x] Enabled GitHub Pages (Settings → Pages, `main` branch, `/root`) at `https://christianhouser.github.io/War-of-Shards/`, so the game is live on a real URL for phone testing and for the user's collaborators. Added a root `index.html` that redirects to `Match_3_game.html` so the bare Pages URL works without knowing the filename.
- [x] **Fixed roster list being unscrollable on touch** — user report: "too touchy for mobile," any touch on the roster list immediately grabbed a card instead of letting the list scroll. Root cause: roster cards had `touch-action: none` + an unconditional `preventDefault()` on `pointerdown`, so the browser never got a chance to scroll. Fixed via `attachRosterDragStart()` (~line 1040): on touch, drag-start is now delayed behind a 250ms press-and-hold (cancelled by >10px movement or the browser taking over as a native pan via `pointercancel`); a quick swipe scrolls instead. Mouse/pen unaffected (still instant drag). Only applied to the roster list — already-placed battlefield cards don't scroll, so they're untouched. Verified via synthetic touch PointerEvents; real on-device touchscreen feel not verifiable in this environment, flagged to the user to confirm on their phone.
- [x] ~~Next AI: if the 250ms hold delay feels off on-device...~~ Superseded same day — see below, touch no longer drags at all.

## Recently Done (2026-08-19, later) — Deployment: tap +/- badges, drag restricted to mouse
- [x] **Replaced touch drag-to-place with tap "+"/"−" badges**, per direct instruction. Roster cards now show a green "+" badge (top-right) that auto-places the hero into the next open row from the top (`autoPlaceCharacter()`, ~line 1076: Ranged → first empty of `RANGED_SLOTS`; Melee → first row not already guarded by an aligned Melee). Placed cards show a red "−" badge that removes them. Both work on any input (tap or click).
- [x] ~~Drag (initial placement AND repositioning) is now mouse-only~~ — **refined the same day, see below**: this turned out too broad and broke touch repositioning + half-step placement entirely.
- [x] `.card-color-dot` moved from top-right to top-left to make room for the new top-right `.card-action-btn` ("+"/"−") badge.
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.

## Recently Done (2026-08-19, even later) — Mouse-only restriction narrowed to new placements only; badge restyle
- [x] **Narrowed the mouse-only drag restriction to just NEW placements from the roster** — repositioning an already-placed card now works on any input again. User feedback after testing: dragging a placed card, and reaching Melee's half-step boundary position, both need to work on touch too (only initial roster→battlefield placement needed the mouse-only guard, to avoid the roster-scroll conflict). `startRangedDrag()` now checks `opts.fromSlot == null`, `startMeleeDrag()` checks `opts.meleeIndex == null`, before applying the `pointerType !== 'mouse'` bail — a non-null value (repositioning) skips the check entirely. The battlefield doesn't scroll, so there was never a real conflict to protect against there.
- [x] **Badge restyle**: `.card-action-btn` ("+"/"−") dropped its circular background/border — now a plain, larger (0.75rem → 1.15rem) colored glyph with a dark text-shadow for legibility against the card's own color gradient, per direct request ("remove the circle, enlarge the icons a little").
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md again.
- [x] ~~Next AI: no known issues~~ — two regressions surfaced on real-device testing, see below.

## Recently Done (2026-08-19, latest) — Fixed two on-device regressions: Melee overlap again, touch drag hijacked as text selection
- [x] **Melee overlap fix, take 2**: the no-stacking check only compared exact bands (`aligned(0)` vs `boundary(0,1)` have different keys, "a0"/"b0"), so it never caught that an aligned card and an adjacent boundary card overlap heavily on screen. Renamed `bandsEqual()` → `bandsConflict()` (~line 1470) with real adjacency logic: aligned/aligned conflicts on same index, boundary/boundary conflicts on same pair, aligned/boundary conflicts if the aligned index is one of the boundary's two — but two boundary bands sharing only one index (e.g. `{0,1}`/`{1,2}`) do NOT conflict (verified this doesn't break the enemy's "2 Melee side by side" formation, which relies on exactly that).
- [x] **Fixed touch-drag on a placed card being hijacked as native text selection** ("whole screen thinks I'm trying to select text and makes the card freeze out of place"). Root cause: `touch-action: none` had been stripped from placed Ranged/Melee cards in an earlier same-day pass (when touch dragging of placed cards was mistakenly assumed gone for good) and never restored when repositioning-drag came back for touch. Re-added `touch-action: none` to both, plus `-webkit-touch-callout: none` on the base `.card` class (a separate mechanism from `touch-action`/`user-select`, specifically for iOS's long-press callout/magnifier menu).
- [x] Updated context/GAME_MECHANICS.md and context/DECISIONS.md.
- [ ] Next AI: no known issues. Caveat: the text-selection-hijack fix targets the well-established standard cause and is verified via `getComputedStyle` (`touch-action: none` present), but can't be reproduced with synthetic PointerEvents in this environment (native gesture recognition only responds to trusted touch input) — needs the user's on-device confirmation.


## Recently Done (2026-08-18) — Repo connected to GitHub + full deployment/battle regression pass
- [x] Connected this project to `github.com/ChristianHouser/War-of-Shards` (`main`, tracking `origin/main`) and pushed all existing project files. Future work just needs a normal `git push`.
- [x] Ran a full manual regression pass on `Match_3_game.html` via a real local server (`.claude/launch.json`, `python -m http.server`) driven through the Browser tool — file:// alone only renders a static snapshot, scripts don't execute, so this is the setup needed for any future live-browser testing of this project. Board/hero-click drag interactions use custom `pointerdown/pointermove/pointerup` listeners (not native HTML5 DnD), so real click/drag testing tools need to dispatch actual `PointerEvent`s with intermediate move steps (a single down+up with no moves in between does not register as a drag) — see the game's own `startRangedDrag`/`startMeleeDrag`/`onRangedDragMove`/`onMeleeDragMove` (~line 1263 on).
- [x] Verified working correctly: Ranged drag-to-any-slot + Melee aligned/boundary drag snapping and live coverage tags; deployment screen and live battle screen render the player's columns pixel-identically (the historical bug noted elsewhere in this doc); match-3 mana grants (per-tile, per-color, "no hero of this color" case); cascading matches after gravity refill; mandatory one-swap-per-turn + auto turn-advance (including correctly exempting low-mana heroes); per-lane melee/ranged targeting restriction (blocked while guarded, opens up once the guard dies); Ranged's unrestricted targeting; enemy AI auto-attacking (including using Specials); Runic Crystal spawn-on-5+-match and tap-only activation (blocked if the turn's board action was already spent); Runic Wave per-lane damage/protection; Arcane Bomb detonation via a swap that touches its cell without forming a new match (including a lethal hit + defeat message).
- [x] **Found and logged an open design question** (not a regression, not fixed): `resolveRunicWaveHits()` only ever picks one guard per lane (`heroes.find`, first match in array/placement order) when multiple Melee are stacked over the same lane — the non-selected guard(s) take no damage and have no effect on exposure. See "Open Design Questions" in context/GAME_MECHANICS.md for the reproduction case and the open question (hit all stacked guards? split damage? pick by some deliberate rule?).

## Recently Done (2026-08-18, later) — Randomized enemy formation shape + enemy hidden until battle
- [x] **Enemy Ranged/Melee split is now randomized too** (previously always 2 Ranged/3 Melee even after enemy-team randomization was added). Added `ENEMY_FORMATIONS` (~line 580): two shapes picked with equal probability per draw — the original 3 Melee/2 Ranged lane pattern, or a new 2 Melee/3 Ranged pattern (two boundary guards side by side on lanes 0/1 and 1/2, together covering all 3 lanes). `drawEnemyRoster()`/`buildRandomEnemyRoster()` generalized to take `rangedCount`/`meleeCount` instead of hardcoded constants. Hero-cap (≤2 Heroes) and color-diversity (≥4 colors) rules apply identically regardless of formation — verified via 40 simulated draws in-browser with zero violations and a ~50/50 formation split.
- [x] **Enemy setup is now hidden on the deployment screen** — `renderDeployBattlefield()`'s two real enemy columns were replaced with a single "ENEMY ❓" placeholder column (no names, types, or even a Melee/Ranged count breakdown, since that would leak which formation was drawn). `ENEMY_TEAM` is still decided once at deployment-entry time; only the on-screen reveal is deferred to `renderBattlefield()` (unchanged), which shows the real roster once "Start Battle" is pressed.
- [x] Updated CLAUDE.md's Core Rules, context/GAME_MECHANICS.md's "Enemy Team Randomization" section, and context/DECISIONS.md with this change.
- [ ] Next AI: no other known issues from this pass. If picking up the Runic Wave stacking question noted above, start at `resolveRunicWaveHits()` (~line 1773).

## Recently Done (2026-08-18, later still) — Melee can no longer stack on the same row
- [x] **Melee placement now rejects two guards on the same row/half-step** (previously explicitly allowed, with a fan-out offset). Reported by the user after dragging two Melee onto the same row and seeing them land fully on top of each other. Added `bandsEqual()`/`meleeBandOccupied()` (~line 1375) and a check in `onMeleeDragEnd()` (~line 1454) that rejects the drop (logs a message, no state change) if another Melee already occupies that exact `band` — applies to both new placements and repositioning an existing card. Verified live in-browser: same-row drop rejected with log message; different-row drop still works, including onto a row with no Ranged hero yet.
- [x] Updated CLAUDE.md, context/GAME_MECHANICS.md, and context/DECISIONS.md (this reverses an earlier documented decision that allowed stacking).
- [ ] Next AI: `layoutMeleeCards()`'s fan-out-for-stacked-cards branch (`FAN_STEP`, ~line 1404) is now dead code (no band can ever have >1 Melee) — left in place since it's harmless, but fair to simplify away if you're already in that function for something else.
- [x] **User reported the overlap fix "still" happening after a page refresh** — re-verified live via a fresh `?v=4` load that the fix is correctly in the file and working (second same-row Melee still rejected). Root cause is almost certainly the local test server: `python -m http.server` sends no `Cache-Control` header at all (confirmed via `curl -I`), so a plain browser refresh can serve a stale cached copy instead of re-fetching. Told the user to hard-refresh (Ctrl/Cmd+Shift+R). If this recurs, worth adding a no-cache header shim in front of the static server rather than relying on hard refreshes.
- [x] **Drag ghost now shows the full placed-card look** (color gradient, icon, name, HP, special glow) instead of a plain-text pill — direct request. `buildGhost()` (~line 1210) now reuses `buildDeployPreviewCard()`; `.drag-ghost` CSS (~line 322) stripped down to just positioning/floating so it doesn't fight the card styling. Verified live in-browser mid-drag.

## Recently Done (2026-08-13)
- [x] Match_3_game.html: Added a "Do Nothing" action to every player hero's action popup (Basic + Special-if-unlocked + Do Nothing + Cancel). Originally scoped to special heroes only, then extended to all player heroes — see DECISIONS.md. Choosing an attack or Do Nothing marks that hero `actedThisTurn = true` (card dims, can't be reselected) until the player's next turn, when the flag resets on all heroes. Cancel just backs out of the popup without locking anything.
- [x] Match_3_game.html: Fixed a layout bug where each hero card's mana bar was present in the DOM but effectively invisible — the card's flex content (icon + type + HP + mana bar) was taller than the card itself, so the mana bar overflowed past the bottom of the card and got painted behind the next row's card. Fixed by increasing the battlefield hero row height (58px → 72px) and trimming icon/text sizes and mana-bar padding so everything fits inside the card. Not yet visually verified in a live browser (no browser access either session) — please confirm the bar reads clearly on your device.
- [x] Match_3_game.html: Mana cap is now per-hero instead of a flat `MAX_MANA = 10`. Each hero's `maxMana` equals the highest-cost ability they can use: normal heroes cap at `BASIC_COST` (3), special heroes cap at `SPECIAL_COST` (5). Removed the unused global `MAX_MANA` constant.
- [x] Match_3_game.html: Turn flow now auto-advances — once every living player hero has acted (attacked or chosen Do Nothing), the enemy turn starts automatically (no need to click End Turn). The End Turn button is kept as a manual override if the player wants to end the turn early. Player is always the attacker/left side; enemy never acts until the player's turn is fully resolved.
- [x] Match_3_game.html: Gem/color palette expanded from 4 colors (red, green, blue, gold) to 5 (Red, Green, Blue, Yellow, Purple). The existing per-side color distribution rule was already exactly what was asked for and needed no logic change: each side's 5 heroes get random colors with at least 3 distinct colors represented and no more than 2 heroes sharing the same color (see `isValidColorSet` / `generateSideColors`).
- [x] Match_3_game.html: "Do Nothing" reverted to special-heroes-only (final answer, supersedes the same-day decision to extend it to every hero — see DECISIONS.md). Normal (single-attack-option) heroes go back to the direct-select flow with no popup, and are required to attack if they have enough mana; they only skip the turn automatically if they truly can't act (mana < BASIC_COST). `checkAutoEndPlayerTurn()` was updated to match: a normal hero without enough mana no longer blocks the enemy turn from auto-starting, but a normal hero who CAN act does block it until they attack.
- [x] Investigated a report of a purple hero's mana not filling after matching purple tiles. Ran the actual matching/mana-award code from this file in a full headless simulation (jsdom): forced a hero purple, matched 3 purple tiles, mana filled correctly (0→3 or 0→5 depending on cap); also ran 5000 trials of the color-generation function and purple appears at the same rate as every other color. Found no purple-specific bug in the code. Confirmed with the user that only PLAYER-side heroes gain mana from board matches (enemy heroes only get mana from their own automatic turn boost) — this is by design, not a bug, but the user says they saw a LEFT-side (their own) purple hero not fill up. Still unresolved — see Next Actions.
- [x] Match_3_game.html: A turn is now defined as exactly one tile swap (must create a match), then attacks, then it's over. Added `swapUsedThisTurn` state — after one successful swap the board dims (`#board-wrap.locked`) and further tile clicks are blocked with a log message, until the next player turn resets it. `checkAutoEndPlayerTurn()` is now also called right when the match cascade finishes (not just after an attack/Do Nothing), so if nobody can attack after that one swap, the turn ends automatically without needing an extra click. Verified via headless simulation: second swap attempt is blocked; turn auto-advances immediately when no one can act; turn correctly stays with the player when someone still can act.
- [x] Match_3_game.html: Removed the "End Turn" button — with the turn now fully automatic (swap → attack/Do Nothing → auto-advance once no one can act), a manual override button was no longer needed. `endPlayerTurn()` itself is unchanged and still used internally by the auto-advance logic.
- [x] Match_3_game.html: **Implemented the Melee/Ranged targeting rule that was documented in CLAUDE.md/GAME_MECHANICS.md as a core rule but was never actually coded** — melee units could previously attack any enemy, ranged or melee, with no restriction. Added `getValidTargets(attacker)`: melee attackers can only target the enemy's living Melee heroes, and gain access to Ranged only once that side's Melee is fully cleared; Ranged attackers can always target anyone. Wired into both the player's click-to-attack flow (illegal target logs a message and does nothing) and the enemy AI's target selection. Verified via headless simulation on both sides (player melee blocked from ranged while enemy melee alive → allowed once cleared; enemy AI targets confirmed restricted the same way).
- [x] Match_3_game.html: **Fixed a real turn-ending bug** — a special hero with less than `BASIC_COST` mana couldn't attack (correct) AND couldn't open their action popup either, since the mana check in `onHeroClick` returns before the special-hero branch. That meant they could never reach the "Do Nothing" button, so `actedThisTurn` could never become true for them, and `checkAutoEndPlayerTurn()` — which only exempted normal heroes from needing to act — would wait on them forever. This matched the user's exact report: they attacked with everyone who could, left the ones who couldn't, used Do Nothing for the one special hero that had a real choice to make, and the turn still didn't end (because a second, low-mana special hero was silently stuck). Fix: `checkAutoEndPlayerTurn()` now exempts ANY hero (normal or special) with mana < `BASIC_COST` — the "must act" requirement only applies once a hero actually has enough mana for a real decision. Verified via simulation: low-mana special heroes no longer block the turn; special heroes with real mana choices still must explicitly resolve (attack or Do Nothing) before the turn ends.
- [x] Match_3_game.html: Swap is now truly mandatory, not just capped at one. Two gaps closed: (1) `onHeroClick` now blocks selecting any player hero to attack until `swapUsedThisTurn` is true — previously a hero with mana left over from a *previous* turn (mana isn't reset between turns, only `actedThisTurn` is) could attack before swapping at all this turn; (2) `checkAutoEndPlayerTurn()` now also requires `swapUsedThisTurn` before it will end the turn — previously, if every hero happened to already be "resolved" (via leftover mana or lack thereof) at the start of a turn, the turn could auto-end without a swap ever happening. Verified via simulation: attacking is blocked pre-swap even with leftover mana on a hero, and the turn cannot auto-end without a swap; normal swap → attack → auto-end flow still works.
- [x] Match_3_game.html: **Removed character-card randomization** (supersedes the "random colors + random specials" behavior noted in earlier entries). `buildArmies()` no longer calls any RNG — each side now gets a fixed color per hero slot (`PLAYER_COLORS`/`ENEMY_COLORS`, one of each of the 5 colors) and fixed special-hero slots (`SPECIAL_INDICES = [0, 3]`, one ranged + one melee special per side). Deleted the now-dead `generateSideColors()`/`isValidColorSet()` random-generation functions. The match-3 board tiles themselves are untouched and still randomize normally — this only affects hero/character setup. Verified via simulation: hero colors/types/specials are now byte-for-byte identical across repeated `resetGame()` calls.

## Recently Done (2026-08-14) — Two-page split, 35-character roster, 6-slot melee stagger
- [x] **Split into two pages.** `Roster.html` (new) is a character codex listing all 35 heroes with full stats/abilities, filterable by type/color and searchable by name/ability text; links to `Match_3_game.html` via a "Go to Battlefield →" button. `Match_3_game.html` now links back via a "☰ Character Codex" button. Both pages load a new shared `characters.js` data module via `<script src="characters.js">` — the combat constants (`BASIC_COST`, `BASIC_DMG`, `SPECIAL_COST`, `SPECIAL_DMG`) and the `maxManaFor()` helper now live only in `characters.js`; the duplicate `const` declarations that used to live in `Match_3_game.html`'s inline script were removed to avoid a duplicate-declaration collision.
- [x] **Added `characters.js`**: all 35 named characters across 16 classes (Barbarian, Bard, Cleric, Druid, Fighter, Mechanist, Monk, Paladin, Ranger, Rogue, Sorcerer, Warlock, Wizard, Theurge, Witch, Vanguard), replacing the old randomly-colored placeholder heroes entirely. Each entry has id/name/title/class/type/hp/color/damageType/weapon/isSpecial/abilityName/abilityText, plus a `simpleAbility` tag for the handful of abilities that are mechanically wired up (see below). Verified via `node --check` + a full data audit (35 unique ids, correct class/type/hp/color/★ counts) against the source spec.
- [x] **Battlefield redesigned to 6 Melee slots + 3 Ranged lanes per side** (previously 3 melee + 2 ranged... actually previously alternating 5 mixed slots). Ranged units occupy one of 3 fixed lanes; each ranged card visually spans 2 melee-row-heights. Melee occupies one of 6 slots, each mapped to which ranged lane(s) it guards: slot 0 = aligned to lane 0 only, slot 1 = half-step boundary covering lanes 0+1, slot 2 = aligned to lane 1, slot 3 = boundary covering lanes 1+2, slot 4 = aligned to lane 2, slot 5 = a second aligned guard for lane 2 (back-edge reinforcement). This gives lane 0 two possible guards, lane 1 three, lane 2 three — a deliberate asymmetry forced by fitting exactly 6 slots against 3 lanes with a working half-step boundary system; flagged to the user as a design call, not something they explicitly signed off on slot-by-slot.
- [x] **Per-lane melee/ranged protection implemented**, replacing the old all-or-nothing "melee zone must be fully cleared" rule. `getCoveredLanes(side)` checks which ranged lanes currently have a living melee hero in a covering slot (aligned or boundary); `getValidTargets()` now lets melee attackers always hit any living enemy melee, and hit a specific enemy ranged unit only if no living enemy melee is currently guarding that unit's lane — independent of whether melee is still alive elsewhere. Protection is binary (covered/uncovered), matching the user's "partial or full protection both fully block" rule. Verified via simulation: a ranged unit behind a guarding melee is untargetable by opposing melee; becomes targetable the instant that guard dies; a boundary-slot guard protects both of its adjacent lanes at once; ranged attackers are unaffected and can always target anyone.
- [x] **Deployment / placement UI added.** Before a battle starts, the page shows a "Build Your Team" screen: a scrollable list of your available roster (35 characters minus the 9 fixed enemy characters = 26 selectable), and the battlefield grid with the enemy team already shown (fixed, not random) and your own slots empty. Tap a roster card to select it, then tap an empty slot of the matching type (Melee card → Melee slot only, Ranged card → Ranged lane only) to place them; tap a placed hero to send them back to the roster. "Start Battle" is enabled once at least one hero is placed. Implemented as tap-to-select/tap-to-place (not drag-and-drop) since the user left the choice open and tap is more reliable on touch devices. Verified via simulation: wrong-type placement is rejected with a log message and does not place; placing/un-placing correctly moves ids between the roster list and the slot; Start Battle is blocked at 0 placed and succeeds at ≥1.
- [x] **Enemy team is fixed, not random**: `ENEMY_TEAM_IDS` hardcodes 3 ranged (Lumi, Saryn, Mara) + 6 melee (Korr, Rorik, Brick, Sylra, Huan, Vara) — chosen so all 4 currently-implemented simple abilities (frenzy, cheapBasic2, divineStrike, brutal) are exercised automatically during any playtest, even before the player learns to use Kael (the only `cheapBasic2` character left in the player's own selectable pool).
- [x] **Wired up 4 "simple" abilities** (flat modifiers only, no new game state) per the user's "roster now, simple abilities work, complex ones deferred" answer: **Frenzy** (Korr) — after dealing any damage, gain 1 free mana, capped at max. **Cheap Basic** (Kael, Lumi) — their Basic Attack costs 2 mana instead of 3; `basicCostFor(hero)` now threads this per-hero cost through every mana-affordability check (`onHeroClick`, the action modal, `checkAutoEndPlayerTurn`, the enemy AI) so a cheap-basic hero with 2 mana is correctly treated as able to act, not exempted as "can't afford it." **Divine Strike** (Rorik) — Special deals +1 damage if the target was already damaged before the hit. **Brutal** (Brick) — Special deals +1 damage if the target has ≤3 HP before the hit. All 4 verified via direct simulation (exact mana/damage deltas checked). The remaining 30 characters' unique abilities are description-only for now — using their Special just deals the standard 5 mana → 2 damage; this is clearly labeled on the Roster page ("⚙ Implemented" vs "📜 Described" tags) so it's not mistaken for a bug.
- [x] Added "Restart Battle" (resets HP/mana/dead/acted for the current placed team and reshuffles the board, keeping your deployment) and "Change Deployment" (clears the current battle and returns to the roster-picking screen) buttons, replacing the old single "Reset Board + Armies" button — deployment is now a separate step from resetting a battle in progress, so restarting shouldn't force you to re-place your whole team.
- [x] Win/lose detection unified inside `performAttack()` (previously only checked for a win after the player's own attacks) — now checks both "all enemies dead" and "all your heroes dead" after every attack regardless of which side made it, so the enemy AI can now correctly trigger a defeat message too (previously only checked at the very end of the enemy's turn).

## Recently Done (2026-08-14, later) — 5 ranged lanes, standard card size, open roster, roster filters
- [x] **Ranged lanes increased from 3 to 5** (`NUM_LANES = 5`); Melee stays at 6 slots. Rebuilt `MELEE_SLOT_DEFS` for a 6-slot/5-lane layout: slots 0,1,2,4,5 each align fully to one lane (guaranteeing every one of the 5 lanes has at least one dedicated guard), and slot 3 is a boundary/half-step slot covering lanes 2 and 3 (the one remaining slot after 5 lanes claim their aligned guard). This is a smaller "stagger budget" than the previous 3-lane version (which had a boundary at every seam) — only the lane 2/3 seam has a half-step option now; flagged in TODO.md as worth revisiting if more staggering flexibility is wanted.
- [x] **Battlefield rendering simplified**: Ranged and Melee are no longer forced into a shared CSS Grid with row-spanning — they're just two independent vertical columns of "standard size" cards (66px tall, restored to roughly the original font sizes and a proper mana-bar with an overlaid number, replacing last round's very compact 44px/tiny-font cards). Since ranged (5) and melee (6) now have different counts, forcing them into pixel-aligned rows no longer made sense anyway — coverage is entirely data-driven (`MELEE_SLOT_DEFS`), not inferred from visual alignment. Noted for later: cards are intentionally simple placeholders and are expected to eventually be replaced with 2D character art.
- [x] **Enemy roster expanded to 5 Ranged + 6 Melee** (11 total, up from 9) to match the new lane count: `ENEMY_TEAM_IDS.ranged` is now `['lumi', 'mireya', 'caelum', 'nyx', 'saryn']` — one character per color (blue/green/yellow/purple/red) — melee unchanged (`korr, rorik, brick, sylra, huan, vara`).
- [x] **Player can now pick from the full 35-character roster**, including characters also on the enemy's fixed team (previously the 9 enemy characters were excluded from the player's pool; that exclusion is removed). The enemy's roster no longer restricts what the player can choose — a mirror matchup (same named character on both sides) is allowed.
- [x] **Added roster filters to the deployment screen**: two toggle chips ("⚔ Melee" / "🏹 Ranged") filter the available-character list to just that type, plus a "✕ Clear" button that resets the filter back to showing everyone. Useful now that the full 35-character pool is always available to browse (previously only 26). Verified via simulation: filtering by Ranged shows exactly the ranged-type subset of what's still available; Clear restores the full list.

## Recently Done (2026-08-14, latest) — 5-character team cap + freeform drag-to-place Melee coverage
This is the biggest structural change yet: team composition is no longer a fixed grid of slots at all. Both the fixed slot-count model (6 Melee/6 Ranged) from earlier today and the "3 Ranged lanes then 5 Ranged lanes" versions are fully replaced.
- [x] **Team cap set to 5 characters per side** (`TEAM_CAP = 5`), any mix of Ranged/Melee — replaces the earlier 6 Melee + 5 Ranged = 11 fixed-slot battlefield entirely.
- [x] **Ranged placement**: tap-to-place, stacks in placement order from the top with no gaps (lane index = position in that order). Tapping a placed Ranged card removes it and closes the gap (later ones shift up, re-indexing their lane).
- [x] **Melee placement is now drag-and-drop with a live coverage preview**, per direct instruction: "when they tap and drag the card, it should auto show what ranged characters it protects, while dragging it in the Melee column." Dragging a Melee character (from the roster, or repositioning an already-placed one) into the Melee column highlights, in real time, which Ranged card(s) it would guard if dropped at the current position — drop directly on one Ranged card's row to align fully with just that lane, or drop in the gap between two adjacent Ranged cards to guard both (half-step boundary). Dropping outside the Melee column cancels a new placement, or un-places an existing one being repositioned.
- [x] **Melee coverage is stored per-hero, not as a fixed slot index**: each placed Melee character carries its own `coverage` object (`{type:'aligned', lane}` or `{type:'boundary', lanes:[i,j]}`, or `null` if dropped before any Ranged exists yet — it still fights enemy Melee, just guards nothing). This replaces the fixed `MELEE_SLOT_DEFS` array from earlier today, since the number of Ranged lanes is no longer fixed — it's whatever the player actually placed (0 to 5).
- [x] **Multiple Melee can guard the same lane or boundary** (stacking allowed), per direct instruction — no cap on redundant guards.
- [x] **Enemy team also capped at 5** (was 11): `ENEMY_TEAM` is now 2 Ranged (Lumi, Saryn) + 3 Melee (Korr aligned to lane 0, Rorik on the boundary between lanes 0/1, Brick aligned to lane 1) — still exercises all 4 implemented simple abilities (frenzy, divineStrike, brutal, cheapBasic2) in a much smaller team.
- [x] Melee hero cards now show a small "🛡" coverage tag (e.g. "🛡 R1+R2") both during deployment and in battle, since coverage is no longer visually implied by a fixed grid position and needs to be legible at a glance.
- [x] **Verification caveat, more important than usual this round**: the placement math itself (`computeBandFromRects` — deciding aligned vs. boundary from a drop's Y position) was factored out as a pure, side-effect-free function and is fully unit-tested with synthetic coordinates. The actual drag *gesture* — pointerdown/pointermove/pointerup, the ghost card following the finger/cursor, live highlighting — was verified by monkey-patching `getBoundingClientRect` to simulate realistic card positions in headless jsdom (since jsdom does no real layout and normally returns all-zero rects), and a full drag→preview→drop sequence, a cancelled drop, a reposition, and a drag-out-to-remove all passed. That is still not the same as a real finger on a real screen — please check that dragging actually feels smooth and that the ghost card doesn't fight with the page's scroll on your device.

## Recently Done (2026-08-14, latest+1) — Ranged is now drag-to-any-slot too; Melee coverage reworked around fixed Ranged slots
Follow-up instruction after seeing the drag-and-drop Melee system: Ranged should be drag-placed as well (not tap-to-stack), and the Ranged column should always show its 5 possible spots so the player can see and choose exactly where each Ranged hero goes.
- [x] **Ranged placement changed from tap-to-place/stack to drag-to-any-slot.** The Ranged column now always renders exactly `RANGED_SLOTS` (= `TEAM_CAP` = 5) slots, filled or empty (dashed "drag here" placeholder), at fixed positions. Dragging a Ranged hero from the roster and dropping it on any empty slot places it there — the player's choice of position, not append-order. Dragging an already-placed Ranged hero repositions it to a different empty slot, or removes it if dropped outside the battlefield. Dropping on an already-occupied slot is rejected with a log message (no swap).
- [x] **`playerPlacements.ranged` changed from a growable array (index = placement order) to a fixed-length array of `RANGED_SLOTS`** (`charId | null` per index, index = slot/lane number). This makes gaps possible (e.g. slots 1 and 3 filled, 0/2/4 empty) — a deliberate consequence of letting the player choose position freely.
- [x] **Melee coverage math reworked to be computed against the fixed 5-slot Ranged grid instead of "however many Ranged are currently placed, stacked with no gaps."** `computeBandFromRects` (position-only, no filled/empty awareness) was split into two pure functions: `computeSlotBand(rects, y)` — purely geometric, decides which of the 5 slots (or which boundary between two adjacent ones) a Y coordinate falls in — and `resolveMeleeCoverage(band, filledMask)` — turns that into actual hero coverage, per the rule "melee can protect up to two ranged, as long as both ranged are together [i.e. both slots filled] and melee is partially in front of both": boundary coverage only applies when BOTH neighboring slots are filled; if only one side is filled, the melee just guards that one; if neither is filled, it guards nothing at that position. Live preview during drag now highlights correctly even with gaps in the Ranged column.
- [x] Roster card labels simplified to "🏹/⚔️ drag to place" for both types, since both are now drag-based with no tap-to-place path left.
- [x] **Verification**: full jsdom pointer-event simulation covering — dragging a new Ranged hero onto a specific non-top slot (not append order); repositioning a placed Ranged hero to another slot; rejecting a drop onto an occupied slot; dragging a Ranged hero off the battlefield to remove it; placing two Ranged in adjacent slots and dragging Melee into the gap between them (boundary coverage); dragging Melee to align with a single Ranged slot; the empty/empty and empty/filled boundary edge cases (no coverage, and aligned-to-the-one-filled-side, respectively); cancelling a new Melee placement by dropping outside the column; team cap (5) still rejecting a 6th hero; and a full `startBattle()` afterward confirming `getCoveredLanes()` correctly reports the boundary+aligned lanes from a real mixed deployment. All passed. Same caveat as before: this is a thorough code-level simulation of the drag gesture, not a real finger/mouse test on an actual device.

## Recently Done (2026-08-14, latest+2) — Fixed: Melee column always rendered new/repositioned cards pinned near the top
Bug report: "Melee column should allow the user to place a card anywhere in that column, not just at the top." Root cause: `renderDeployBattlefield()`'s Melee section rendered `playerPlacements.melee` in raw array order (insertion order) — a newly-placed card always got pushed to the end of the array, and repositioning an existing card only updated its `coverage`, never its array position. Since DOM/flex order = array order, a card's on-screen row was determined by when it was placed, not by where the player actually dropped it (with few cards placed, this often meant every card visually landed near the top no matter where you dragged it).
- [x] **Fix**: the Melee column now renders sorted by `primaryLane(coverage)` (same sort the live-battle view already used), computed fresh on every render — so a card's row genuinely reflects the Ranged lane(s) it's covering, and dragging it to a new vertical position visibly moves it. The real array index is still tracked alongside the sorted display order so drag/reposition/remove handlers keep referencing the correct entry (not just SEO on display order).
- [x] Verified via jsdom: placed two Melee heroes out of order (bottom lane first, then top lane) and confirmed the DOM renders the top-lane card above the bottom-lane one; placed three Melee at different lanes (including one with no coverage) and confirmed dragging a specific *rendered* card out (by its displayed position, not array position) removes exactly that hero and no other, then confirmed battle start still produces the correct hero count.

## Recently Done (2026-08-14, latest+3) — Melee free-positioning fix, take 2 (the real one), plus enemy columns centered
The previous round's fix (sorting the Melee column by `primaryLane(coverage)`) turned out to be the wrong fix, per user follow-up: "Still didn't work." Root cause of why it looked unfixed: that sort ties a card's on-screen row to what it's *covering*, not to where it was actually dropped — and with **no Ranged placed yet** (exactly the state shown in the user's screenshot), every Melee card's coverage resolves to `null`, so `primaryLane()` returns the same tiebreaker value (99) for all of them and they render back in plain insertion order — i.e. still pinned whichever order they were dropped in, completely ignoring vertical drop position. The bug was never really about sort order; it's that Melee position and Melee coverage are two different things and were incorrectly tied together.
- [x] **Real fix: `playerPlacements.melee`'s array order now IS the on-screen order, full stop — no sorting at render time.** A drop's Y coordinate is translated directly into an array insertion index via the new `computeMeleeInsertIndex(y, excludeIndex)`: it counts how many of the *other* currently-placed Melee cards sit above the drop point (comparing against each card's live on-screen vertical midpoint), and that count becomes the index the dropped card gets spliced into. Coverage is still computed completely separately (unchanged, via the Ranged-slot rects) — a card's row and what it protects are now fully independent, which is what actually lets you place a card anywhere in the column regardless of what (if anything) is currently placed in Ranged.
- [x] Repositioning an already-placed Melee card now removes it from its old array slot and reinserts it at the new drop-derived index (previously it only updated `coverage` and left the array position — and therefore the visual row — completely untouched, which was the direct cause of "cards stay at the top").
- [x] **Enemy columns (both Melee and Ranged) are now vertically centered within their column** instead of top-aligned, on both the deployment screen and the live battle view — per direct instruction ("move their layout to the middle"). Added a `.center-content` CSS class (`justify-content: center`) applied only to the two enemy columns; the player's columns are untouched (Ranged needs to stay top-aligned since it's a fixed 5-slot grid, and Melee's "top" has no special meaning now that position is fully free-form).
- [x] **Verification**: jsdom test reproducing the exact reported scenario — zero Ranged placed, three Melee cards dropped in a deliberately scrambled vertical order (middle, then top, then bottom) — confirmed the array AND rendered DOM order end up matching drop order every time; a follow-up reposition (dragging the bottom card to the very top) confirmed it actually moves there. A second regression pass re-confirmed boundary/aligned coverage math is unaffected (still resolves correctly when Ranged *is* placed), and confirmed the new `.center-content` class is present on exactly the two enemy columns in both the deploy and live-battle views, and absent from the player's columns.

## Recently Done (2026-08-14, latest+4) — Found and fixed the ACTUAL root cause: the Melee drop zone was tiny, not the placement math
User follow-up after the "take 2" fix: "Still not working, only one place to put melee." The placement math (`computeMeleeInsertIndex`, coverage resolution, etc.) was never the problem — it's a genuine CSS layout bug that headless jsdom testing structurally cannot see, because jsdom doesn't run a real layout engine (every jsdom test in this project has monkey-patched `getBoundingClientRect` to fake positions, which is great for testing drop *logic* but blind to real layout bugs like this one).
- [x] **Root cause found**: `#battlefield` used `align-items: flex-start`, so each `.bf-column` (including the Melee drop zone — that IS the element the drop-detection code checks the pointer against) was only ever as tall as its own current content. With 0 or 1 Melee card placed, that's a ~70-90px box. Dropping anywhere below that tiny box registered as "outside the Melee zone" and got treated as a cancel/remove — which is exactly "only one place to put melee": the one spot inside that small box, functionally regardless of where you actually dragged to.
- [x] **Fix**: changed `#battlefield` to `align-items: stretch`, so all 4 columns match the tallest column's height — which is reliably the Ranged column's fixed `RANGED_SLOTS` (5) rows. The Melee drop zone's actual clickable/droppable area (and the "drag here" placeholder, which now also stretches to fill it via a new `.bf-column.dropzone-hint .card.empty-slot` rule) now spans the full intended area even with zero Melee placed, matching what it visually looked like the whole time.
- [x] Bonus: this is also what made the "move enemy layout to the middle" request from last round actually visible — `justify-content:center` only has a visible effect when a column's box is taller than its content, which wasn't true before this fix (a 3-card enemy column's box was already exactly 3 cards tall, so "centering" was a no-op). Now that columns stretch to the Ranged column's height, the enemy columns visibly center within the extra space.
- [x] **Verification, upgraded**: this round used Playwright + real headless Chromium (already available in this environment) instead of jsdom, specifically because this class of bug is invisible to mocked-rect testing. Confirmed the Melee dropzone's real `getBoundingClientRect()` now matches the Ranged column's height/position exactly (365px, same top/bottom) even with zero Melee placed; performed two REAL `mouse.down()/move()/up()` drags (not simulated PointerEvents) — one dropping near the very bottom of the zone, one near the very top — and confirmed both landed correctly and in the right relative order. Also took a full-page screenshot and visually confirmed the Melee "drag here" placeholder now spans the same height as the 5 Ranged slots, and both enemy columns are visibly vertically centered next to it.

## Recently Done (2026-08-14, latest+5) — Melee placement rebuilt around real row/half-step snapping (the actual intended design)
User feedback after the CSS layout fix: "1. Melee column still won't allow me to put a melee character where I want it. 2. I should be able to place the card in the same 'row' as the ranged character, OR put it a half step down or up to be half in front of two ranged characters." This reframes the whole feature: a Melee card's on-screen position should be a literal, visible snap to a Ranged row or the half-step gap between two adjacent rows — not a free-floating position tracked by internal list order (the "take 2" fix from earlier today), which never actually looked aligned to anything.
- [x] **Split "where a Melee card sits" from "what it protects" into two independent concepts**, per-hero: `band` — pure geometry, always resolvable since the `RANGED_SLOTS` grid always exists (filled or not) — `{type:'aligned', index:i}` (level with Ranged slot i) or `{type:'boundary', indices:[i,j]}` (straddling the half-step gap between adjacent slots i/j). `coverage` — what that position actually protects RIGHT NOW, derived fresh from `band` + which Ranged slots are currently filled (via the existing `resolveMeleeCoverage`). This means placing a Melee hero next to an empty Ranged slot is fine — it'll automatically start protecting the instant a Ranged unit is placed there, with no need to re-drag it.
- [x] **Melee cards are now absolutely positioned** within the (already-stretched, from the previous fix) Melee column, so a card's row is a real pixel-for-pixel visual match to the Ranged slot(s) it's aligned/straddling — not just an internal ordering. Cards sharing the same band (stacking, still allowed) fan out with a small vertical offset so they stay visible instead of fully overlapping.
- [x] **Drag feedback upgraded to two tiers**: while dragging, the Ranged slot(s) matching the current geometric band light up gold (you'll snap here) regardless of whether they're filled; any of those that are ALSO actually being protected additionally light up green (takes visual priority). This directly answers "how do I know I'm placing it correctly" during the drag itself.
- [x] Removed `computeMeleeInsertIndex` and the array-order-based positioning system from the previous fix entirely — it's obsolete now that each card is positioned independently by its own `band`.
- [x] **Verification, real browser again**: Playwright + real Chromium, with genuine `mouse.down/move/up` drags (not simulated events). Confirmed: a Melee dropped exactly on a Ranged row renders pixel-level with it; dropped in the gap between two adjacent filled Ranged renders at the midpoint and its resolved coverage protects both; dropped level with a still-EMPTY Ranged slot renders there anyway (coverage none yet); filling that Ranged slot afterward makes the already-placed Melee start protecting it automatically, with no re-drag; repositioning an existing Melee to a new row moves it there. Also took a full-page screenshot and visually confirmed cards render level with / straddling the correct Ranged rows (see delivered screenshot in this conversation).

## Recently Done (2026-08-14, latest+6) — Widened the boundary (half-step) hit zone; too small to hit reliably
User feedback after trying the row/half-step snapping fix on their own device: "Dragging melee into melee column shows what ranged are being protected, but it's not easy to put it in front of two ranged characters." Root cause: `computeSlotBand`'s boundary target was literally the ~4px CSS gap between two adjacent Ranged cards (`rects[i].bottom` to `rects[i+1].top`) — a few-pixel-tall touch target, unrealistic to land reliably with a finger (or even a mouse without real precision).
- [x] **Rewrote `computeSlotBand` around slot CENTERS instead of slot edges.** The boundary hit-zone is now a generous band centered on the midpoint between two adjacent slots' centers, sized as `BOUNDARY_FRACTION` (currently 0.6, i.e. 60%) of the pitch between those centers — for the current 66px card / 4px gap layout (70px pitch), that's about a ±21px target instead of ±2px. The remaining 40% of the pitch (20% on each side) still resolves to "aligned" with whichever neighbor is closer.
- [x] **Verified in a real browser** (Playwright + real Chromium, mouse drags): dropping 15px away from the exact geometric midpoint — well outside where the old 4px gap target would have registered — now correctly resolves to boundary coverage of both adjacent Ranged units; dropping near a card's own center still correctly resolves to aligned coverage of just that one, confirming the widened zone didn't swallow the aligned targets entirely.
- [x] This is a pure hit-testing change — the resulting `band`/`coverage` shapes, the live gold/green drag highlighting, and the row-snapped rendered position are all unchanged; only which pointer positions map to which band shifted.
- [x] Regenerated `Match_3_game_standalone.html` (the single-file bundle for testing without a sibling `characters.js`) with this fix included.

## Recently Done (2026-08-14, latest+7) — Added a "Heroes" filter to the deployment roster
- [x] Added a third filter chip ("★ Heroes") to the deployment screen's filter bar, alongside the existing Melee/Ranged chips and Clear button. Filters `deployAvailable` down to characters with `isSpecial === true` (the same field that drives the ★ marker and named-ability display elsewhere), regardless of Melee/Ranged type — a Hero can be either. Mutually exclusive with Melee/Ranged, same toggle behavior (click again to turn off; Clear resets it too).
- [x] Note for context: 32 of the 35 characters are currently `isSpecial: true`, so the Heroes filter only excludes 3 characters (the non-special ones) — this isn't a bug, just how lopsided the current roster's special/normal split happens to be. Worth keeping in mind if the filter feels like it "doesn't do much."
- [x] Verified via jsdom: 4 chips render; Heroes filter shows exactly the 32 isSpecial characters (all carrying the ★ marker); clicking Melee while Heroes is active correctly switches to Melee-only (mutually exclusive); toggling Heroes off and clicking Clear both correctly restore the full 35-character list.
- [x] Regenerated `Match_3_game_standalone.html` with this change included.

## Recently Done (2026-08-14, latest+8) — Added a "Non-Heroes" filter to the deployment roster
- [x] Added a fifth filter chip ("☆ Non-Heroes") to the deployment screen's filter bar, alongside Melee/Ranged/Heroes and Clear. Filters `deployAvailable` down to characters with `isSpecial === false` — the mirror image of the Heroes filter added in the previous round. Mutually exclusive with the other three type/hero filters, same toggle behavior (click again to turn off; Clear resets it too).
- [x] Since 32 of the 35 characters are `isSpecial: true`, Non-Heroes shows just the 3 remaining characters: `vesper_quill`, `kael`, `lumi`.
- [x] Verified via jsdom: 5 chips render; Non-Heroes filter shows exactly those 3 non-special characters; clicking Heroes while Non-Heroes is active correctly switches to Heroes-only (mutually exclusive) and vice versa; toggling Non-Heroes off and clicking Clear both correctly restore the full 35-character list.
- [x] Regenerated `Match_3_game_standalone.html` with this change included.

## Recently Done (2026-08-14, latest+9) — Filter chips now combine (multi-select), plus 10 new "Mercenary" non-hero characters
- [x] **Multi-select filters.** Replaced the single mutually-exclusive `deployTypeFilter` with `deployActiveFilters` (a `Set`). All four chips (Melee, Ranged, Heroes, Non-Heroes) now toggle independently instead of turning each other off. Filtering rule: within a group (Melee/Ranged is the "type" group, Heroes/Non-Heroes is the "specialty" group) active chips are OR'd together; across the two groups it's AND'd. Example: Heroes + Ranged narrows the roster to Ranged Heroes only; Melee + Ranged together shows everyone (same as neither active), since every character is one or the other. Clear still resets to no filters active.
- [x] **10 new non-hero characters added** (`isSpecial: false`, all under a new "Mercenary" class), exactly as specified: melee — Kael Ironwood (Longsword, Yellow), Brother Rurik (Warhammer, Red), Grommash the Red (Greataxe, Red, 6 mana → 2 dmg), Sera Vey (Spear, Green), Tomas Dualedge (Shortsword, Blue); ranged — Lira Swiftarrow (Longbow, Green), Harkon Bolt (Heavy Crossbow, Purple, 6 mana → 2 dmg), Mira Quietshot (Light Crossbow, Blue), Fen Willow (Shortbow, Yellow), Vex Needle (Hand Crossbow, Purple). Roster is now 45 characters total (13 non-heroes, 32 heroes). Ids were chosen to avoid collisions with existing similarly-named heroes (e.g. `kael_ironwood` vs. the existing `kael`/"Master Kael Dualedge"; `fen_willow` vs. the existing `fen`/"Fen the Packbrother"; `lira_swiftarrow` vs. the existing `lira`/"Lira Voss").
- [x] **New data model: per-character Basic Attack overrides.** Two of the ten (Grommash, Harkon) use a heavier 6 mana → 2 damage basic swing instead of the standard 3 → 1, which the old shared-constant-only model (`BASIC_COST`/`BASIC_DMG`) couldn't express. Added optional `basicCost`/`basicDmg` fields per character plus `basicCostFor()`/`basicDmgFor()` helpers in `characters.js` (falls back to the shared constants, or the existing `cheapBasic2` discount, when unset) — and fixed `maxManaFor()` to raise a character's mana cap to match an overridden `basicCost` (otherwise Grommash/Harkon could never reach the 6 mana their attack needs and would be permanently unable to act). `basicCostFor`/`basicDmgFor` are now the single shared source of truth, used by both `Match_3_game.html` (removed its old local duplicate `basicCostFor`) and `Roster.html` (previously hardcoded "3 mana / 1 damage" text on every card regardless of actual cost — now reads the real per-character values).
- [x] Verified via jsdom: `CHARACTERS.length` is 45; `basicCostFor`/`basicDmgFor`/`maxManaFor` return `[6,2,6]` for Grommash and Harkon and `[3,1,3]` for a default non-hero; existing `cheapBasic2` characters (Kael, Lumi) and existing Heroes (e.g. Korr) are unaffected (still `[2,3]` and `5` respectively — no regression); a full `performAttack` simulation on Grommash correctly spends all 6 mana for 2 damage. Multi-filter combinations (Heroes+Ranged, Heroes+Ranged+Melee, Non-Heroes+Melee, Clear) all produced exactly the expected roster counts.
- [x] Updated `CLAUDE.md` and `context/GAME_MECHANICS.md` to reflect the 45-character roster, the multi-select filter behavior, and the `basicCost`/`basicDmg` override mechanism. Regenerated `Match_3_game_standalone.html`.

## Recently Done (2026-08-14, latest+10) — Deployment/battle layout parity, a real melee-card name-squish bug fix, and randomized enemy teams
- [x] **Battle screen now uses the exact same layout as deployment.** Screenshots showed the player's Ranged column collapsing from the fixed 5-slot grid (deployment) to a compact N-card list (battle) — the same heroes visually jumped to different rows the instant "Start Battle" was pressed — and the Melee column switched from band-pixel-aligned cards to a plain sorted list. Fixed by rewriting `renderBattlefield()` to mirror `renderDeployBattlefield()`: the Ranged column always renders `RANGED_SLOTS` fixed rows (empty ones just render as a blank dashed placeholder, no "drag here" text since battle isn't interactive that way), and Melee cards are absolutely positioned via a new `positionMeleeCardPairs()` helper using each hero's `band` (now preserved on the hero object through `buildArmies()`/`createHeroFromChar`, not discarded after computing `coverage` at battle start). Enemy columns are unchanged (a simple centered list in both screens, since the enemy roster was never player-arranged).
- [x] **Found and fixed a real, independent CSS bug while investigating the layout screenshots**: a live Melee card's `.card-name` was rendering at ~2px tall — visually invisible — in BOTH the old and new battle rendering (so this was already broken before this round, just misread as part of the layout issue). Root cause: `.card` has a fixed `height: 66px`, but a Melee card's full content stack (icon + name + HP + mana bar + coverage tag) measures ~74px naturally; `.mana-bar-bg` is the only child with `flex-shrink: 0`, and `.card-name` has `overflow: hidden` (for its ellipsis), which resets its flexbox auto-min-height to 0 — so when the flexbox algorithm had to shrink something to fit 74px of content into 66px, `.card-name` absorbed almost the entire deficit alone. Ranged cards (no coverage tag, one fewer element) happened to fit under 66px and were never visibly broken. Fixed by bumping `.card` to `height: 78px` and adding defensive `flex-shrink: 0` to `.card-icon`/`.card-name`/`.hp-text`/`.coverage-tag` so no single element can be singled out for collapse again. Verified via real headless-Chromium screenshots (jsdom can't catch this — it has no layout engine, `getBoundingClientRect()` always returns all-zero) and a direct DOM measurement showing `.card-name`'s rendered height going from ~1.9px to its natural ~10.1px.
- [x] **Enemy team is now randomized every time deployment is (re)entered**, replacing the previous fixed 5-character squad. `randomizeEnemyTeam()` (called from `initDeployment()`) draws 2 Ranged + 3 Melee (same shape as before) under two hard constraints: at most 2 of the 5 may be Heroes (enforced by construction — decide the hero/non-hero split before picking characters, since a fully-random draw would rarely satisfy "≤2 of 5" given ~71% of the roster are Heroes), and at least 4 of the 5 colors must be distinct (enforced by bounded retry, a much friendlier probability). Melee coverage still uses the same fixed lane pattern the old static team used (lane0 / boundary / lane1) — that's a property of Ranged always having exactly 2 lanes, not of which characters get drawn.
- [x] Verified via jsdom (500 randomization runs: 0 violations of team size, no-duplicates, hero cap, or color-diversity; 0 type-mismatch — Ranged-role picks are always `type:'ranged'` characters and vice versa) and via Playwright with a real browser (full flow: drag-place a Ranged and a Melee hero in deployment, Start Battle, confirm both heroes render in the exact same row/band position as during deployment, confirm melee card names are now visible, click-to-select and click-to-target both work with no console errors).
- [x] Updated `CLAUDE.md` and `context/GAME_MECHANICS.md` (new "Enemy Team Randomization" section) to describe both the layout-parity guarantee and the randomization rules. Regenerated `Match_3_game_standalone.html`.

## Recently Done (2026-08-14, latest+11) — Dashed "placement lining" now only shows during deployment; added a permanent YOU/ENEMY divider
- [x] The dashed outline around the player's Melee column (`.dropzone-hint`) — a "this is a drop target" affordance — no longer appears once the battle starts, since nothing is draggable there anymore. `renderBattlefield()` now adds a new, leaner `.melee-positioned` class (just `position: relative`, the containing block the absolutely-positioned Melee cards still need) instead of `.dropzone-hint`; `renderDeployBattlefield()` keeps using `.dropzone-hint` (plus `.melee-positioned`) as before, so deployment is unaffected.
- [x] Added a permanent dotted vertical divider (`.side-divider`, `border-left: 2px dotted`) between the player's two columns and the enemy's two columns, present in BOTH deployment and battle — explicitly called out by the user as something to keep even as the dashed placement hint goes away, since it was the only visual boundary marking "your side" vs. "their side."
- [x] Verified via Playwright: deployment's Melee column still shows the dashed outline (`dropzone-hint melee-positioned`); the same column in battle shows only `melee-positioned` (`outline-style: none`, confirmed via computed style); the dotted divider (`border-left: 2px dotted rgb(119, 119, 136)`) renders correctly on the first enemy column in both screens; no console errors.

## Recently Done (2026-08-15) — Unfilled Ranged slots are now invisible during battle
- [x] Follow-up to the "placement lining" fix above: individual unfilled Ranged slots (the dashed empty-slot boxes, one per never-deployed slot) were still visibly showing during battle, same problem as the Melee dropzone outline but at the per-slot level instead of per-column. Added a `.card.battle-empty-slot { visibility: hidden; }` CSS class and switched `renderBattlefield()`'s empty-slot branches (both Ranged, and Melee's "no heroes deployed at all" placeholder) to use it instead of the dashed `.empty-slot` styling. `visibility: hidden` (not `display: none`) was the key choice — it hides the box but keeps its layout footprint, so filled slots below an invisible one don't shift up, and the Ranged slot rects Melee band-positioning depends on (`rangedSlotEls()`) stay exactly where they were in deployment.
- [x] `renderDeployBattlefield()` is untouched — deployment still shows the dashed "drag here" boxes for empty slots, since those are genuinely still droppable there.
- [x] Verified via Playwright: with Ranged slots 0 and 2 filled (out of 5), battle correctly shows slots 1/3/4 as `visibility: hidden` while all 5 still report the correct `78px` row height and expected `top` offset (no shift); filled slots stayed visible with their heroes rendered; a Melee card banded to the boundary between slots 0/1 still aligned correctly against the now-invisible slot 1's position. No console errors.

## Recently Done (2026-08-15, later) — Match-3 overhaul: Match Type Rules, Arcane Bomb, Runic Crystal, Runic Wave
This directly supersedes the long-standing "Match-3 never creates soldiers or deals direct damage" rule (updated in `CLAUDE.md` and `context/GAME_MECHANICS.md`) — matches of 4+ tiles can now damage the enemy team.
- [x] **Match resolution rewritten around connected-component grouping** (`findMatchGroups()`), replacing the old flat per-color aggregation, so an intersecting horizontal-3 + vertical-3 (T/L/plus shape) registers as one 5-cell group instead of two separate 3-groups, with the crossing cell recorded as `intersection` for special-tile spawn placement.
- [x] **Three match tiers implemented**: Match-3 (plain removal, 1 mana/tile, no other effect — unchanged from before except now routed through the new grouping code); Match-4 → spawns an **Arcane Bomb** (`boardSpecial = 'bomb'`); Match-5+ (straight run, or any T/L/plus of ≥5 cells) → spawns a **Runic Crystal** (`boardSpecial = 'crystal'`). The old flat "+1 bonus mana for 5+" rule is removed entirely — every tier now grants exactly 1 mana per tile, uniformly; differentiation is purely about which special tile spawns.
- [x] **Arcane Bomb**: when later matched (caught in a new same-color group) or activated (the player swaps any adjacent tile touching it, even without forming a new match), clears itself + its 4 orthogonal neighbors, all 5 pay mana by their own color, and deals 2 damage to one random living enemy Melee unit (fizzles with a log message if none are alive). Fixed a real bug found during testing: the bomb's own tile was never cleared/paid when swap-activated without a fresh match forming (`applyBombExplosion` only handled its 4 neighbors) — added explicit self-clear+self-mana handling mirroring the crystal's existing pattern, guarded against double-paying when the group loop already handled it.
- [x] **Runic Crystal**: its own tile always clears + pays base 1 mana first regardless of trigger path, then the player picks via a new modal — Option A clears every remaining tile of that color on the board (big mana gain), or Option B ("Runic Wave") deals damage across all 5 Ranged lanes simultaneously instead (no mana). `resolveRunicWaveHits()` implements the per-lane targeting: no guard → open ranged takes 3; fully-aligned melee guard → melee takes 3, ranged behind takes 0 (fully protected); boundary/straddling melee guard → melee takes 3 once (not per-lane), one of its two guarded lanes stays protected and the other takes 2 (half-exposed, chosen at random per activation — see DECISIONS.md).
- [x] **Chain reactions supported**: a bomb's blast or a crystal's color-clear can catch another pre-existing special tile, which gets queued and resolved before the wave is considered complete (`processExplosionQueue()`, a continuation-passing walker needed since the crystal's choice modal has to pause resolution mid-wave — a plain synchronous loop can't do that).
- [x] Added `.tile.special-bomb`/`.tile.special-crystal` CSS (distinguishing box-shadow) and the `#crystal-modal` HTML (title + "Clear All Tiles of This Color" / "🔮 Runic Wave" buttons), modeled on the existing `#attack-modal`. Added the missing `let boardSpecial = [];` module-level declaration that the rewritten logic depends on.
- [x] **Verified via jsdom** (216 assertions): match grouping/shape classification for 3/4/5/T-shape, uniform 1-mana-per-tile math with no bonus, `applyBombExplosion` (self+4-neighbor clear, per-neighbor-color mana, 2 dmg to random living enemy melee, self-mana-skip when already paid via the group loop), `applyCrystalEffect` Option A and Option B, `resolveRunicWaveHits` full-cover and boundary/partial-cover (40 trials confirming the half-exposed lane is genuinely randomized, not fixed), a full `onTileClick` swap-activation of a pre-existing bomb with no new match formed, and `applyGravityAndFill` carrying `boardSpecial` together with `board` through a gravity drop. **Verified via Playwright + real headless Chromium**: special-tile CSS classes/emoji render correctly in the DOM, the crystal choice modal opens/closes and its buttons work, a full real-UI deployment→battle→tile-swap flow produces zero console/page errors, and a 60-round stress test of random adjacent-tile swaps (auto-resolving any crystal modal that pops up) ran with zero hangs, zero console errors, across 3 separate runs.
- [x] Updated `CLAUDE.md` (revised the old "never deals direct damage" rule, added a full "Match-3 Match Type Rules & Special Tiles" bullet) and `context/GAME_MECHANICS.md` (fully rewrote the `## Match-3 Board` section with the tier rules, activation/chain-reaction behavior, and Runic Wave targeting math). See DECISIONS.md for the underspecified-design-decision interpretations made (shape-tier boundaries, spawn-cell placement priority, "matched or activated" definition, random half-exposed-lane choice, crystal's always-pay-base-mana rule).

## Recently Done (2026-08-15, latest) — Runic Crystal now only activates via a direct tap
- [x] Direct feedback: "Runic Crystal options should not appear until user taps on it after it's been created." Previously the crystal's choice modal could pop up from being incidentally caught in a fresh match, or from an unrelated swap that happened to touch its cell — both removed. A crystal now activates ONLY via a single direct click on its own tile (`onTileClick()` intercepts a click on a `boardSpecial === 'crystal'` cell before any swap logic runs); a crystal's cell can never be used as one half of a normal two-tile swap anymore — clicking it always taps it. Tapping consumes the turn's one board action (same as a swap), and is blocked with a log message if that action was already spent this turn.
- [x] A crystal caught in a fresh match (or touched by a swap between two *other* tiles) without being tapped first now just clears like an ordinary matched tile — it still pays its group-share mana, but its special effect and choice modal are lost. This is an intentional consequence, not a bug: the crystal is "wasted" if the player lets it get swept away before tapping it.
- [x] Arcane Bomb is unaffected — it still activates via being matched into a fresh group OR via a swap that touches it (no tap-only requirement), since it has no choice modal for this feedback to apply to. Flagged as a deliberate asymmetry in CLAUDE.md/GAME_MECHANICS.md, worth confirming is the intended long-term design.
- [x] Verified via jsdom (12 new assertions): a single tap with no swap opens the modal and consumes the turn's action; choosing an option afterward resolves normally; a fresh match that incidentally catches a crystal (via a swap between two unrelated tiles) never opens the modal and just clears the tile; bomb swap-activation is unaffected (regression check); tapping a crystal after the turn's action is already spent is correctly rejected. Verified via Playwright + real Chromium: a real single click opens the modal, choosing an option resolves it and grants mana, no console errors. Re-ran the 60-round random-swap stress test (now also tapping any crystal that appears ~30% of the time) with zero hangs/errors.
- [x] Updated `CLAUDE.md` and `context/GAME_MECHANICS.md` to describe the bomb/crystal activation asymmetry.

## Next Actions (priority)
1. [ ] **Real device/touch-screen check still recommended.** This round upgraded verification from jsdom to real headless Chromium (Playwright) and caught a genuine CSS layout bug that jsdom-only testing structurally could not see — a meaningfully stronger signal than before, but headless Chromium on a fixed desktop viewport is still not the same as a touch screen. Please confirm the drag/drop feels right on an actual phone: near the very top/bottom edges of the Melee zone, while the page is scrolled, and with a real finger instead of a mouse. Also worth checking whether rejecting a drop onto an occupied Ranged slot (rather than swapping the two heroes) feels right, or if a swap would be more intuitive.
2. [ ] Real playtest to confirm the turn-ending fix (low-mana specials no longer block auto-advance) feels right in practice, and to catch any other "stuck turn" edge cases the simulations didn't cover.
3. [ ] **Purple mana report still open.** Code simulation shows no bug, but the user reports their own (left-side) purple hero didn't gain mana after a purple match. Possible causes to check next: (a) stale browser cache — ask user to hard-refresh / reopen the file fresh; (b) get real browser access to visually verify; (c) ask the user to paste the exact log line shown when they match purple.
4. [ ] The remaining 30 characters' unique abilities (heals, summons, RNG effects, mana conversion, HP drain, blockers, etc.) are description-only — Special just deals standard damage for them. A follow-up pass is needed to wire these up once the simple ones are confirmed working well.
5. [ ] Cards are explicitly a placeholder today (colored box + icon + name + HP + mana bar/coverage tag) — the user has flagged that these may eventually become 2D character art/sprites. Worth keeping the card-building code (`buildHeroCardEl`, `buildDeployPreviewCard`) isolated so swapping in real art later doesn't require touching game logic.
6. [ ] Write basic auto-play AI rules (currently: enemy boosts 1-2 random units' mana each turn, then anyone who can afford Basic/Special attacks a random valid target — no strategy).
7. [ ] Choose game engine (priority: mobile performance on most phones/tablets)
8. [ ] Outline first few campaign missions (including uneven team sizes)
9. [ ] Sketch hero progression path (levels → new abilities)
10. [ ] Confirm the per-hero max-mana rule (max = cost of highest-tier ability) is the long-term design, or if a separate/larger mana pool should exist once more ability tiers are added later.
11. [ ] User floated an idea (uncommitted, "I guess"): should enemies gain mana from some kind of board/formula (referencing how Empires & Puzzles does AI mana), instead of the current random per-turn boost? Not decided — worth a follow-up conversation before building it.

## Confirmed Decisions
- Fantasy theme
- Battlefield team cap is now 5 characters per side, any mix of Ranged/Melee (supersedes every earlier fixed-slot-count framing today — 6v6, 6 Melee + 3 Ranged, 6 Melee + 5 Ranged — and the original "Max 6v6" campaign/meta note; 5 total is now the number that matters for this screen)
- One mana/stamina bar per hero
- Hero collection + Campaign + PvP + Hero side missions
- Match-3 = primarily a resource generator (1 mana/tile, every tier, uniformly), but matches of 4+ tiles now also spawn special tiles (Arcane Bomb, Runic Crystal) that deal direct damage to the enemy team when later matched/activated — see the Match-3 overhaul entry above and context/GAME_MECHANICS.md. Still never creates new soldier/hero units.
- Positional melee/ranged combat, now with per-lane (not zone-wide) protection, and Melee coverage assigned per-hero via drag-and-drop rather than a fixed slot grid — see below
- Player is always the attacker on the left side; enemy is always on the right and only acts after the player's turn is fully resolved
- Match-3 gem colors: Red, Green, Blue, Yellow, Purple (5 total)
- The old 5-hero-per-side, randomly-colored placeholder roster is retired. Heroes now come from a fixed 35-character named roster (`characters.js`), grouped into 16 classes, browsable and filterable on `Roster.html`.
- The player deploys their own team by hand from the FULL 35-character roster (max 5 total) — the enemy's fixed team does not restrict the player's choices, and the deployment screen has its own Melee/Ranged filter + Clear button to help browse. Both zones are drag-and-drop: Ranged always shows 5 fixed slots and the player drags a hero onto whichever one they want (their choice of position, gaps allowed); Melee is a free-form drop zone with a live preview, while dragging, of which Ranged slot(s) it would guard at the current position. The enemy's 5-character team (2 Ranged + 3 Melee, fixed coverage) is not random.
- Melee/Ranged targeting is per-lane: melee can always hit enemy melee, and can hit a specific enemy ranged unit only if no living enemy melee currently guards that unit's lane (aligned to it, or on the half-step boundary shared with an adjacent lane) — protection is binary, not a damage-reduction gradient, and multiple melee may stack on the same lane/boundary for redundant coverage. A boundary only protects both neighboring Ranged lanes if both are actually filled ("as long as both ranged are together"); if only one side has a Ranged hero, the boundary position just guards that one.
- A player turn = exactly one tile swap, then attack with anyone who can, then the turn is over
- 4 abilities are mechanically implemented (frenzy, cheap-basic ×2 characters, divine strike, brutal); all other unique abilities are description-only for now.
- Hero cards are a simple placeholder (icon + name + HP + mana bar, Melee also shows a coverage tag) by design — expected to eventually be replaced with 2D character art.

## Notes for next AI
Always read these two files first:
- context/GAME_OVERVIEW.md
- context/GAME_MECHANICS.md
