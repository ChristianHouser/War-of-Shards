# Design Decisions Log

Record every important decision here so future AIs (and you) never re-debate the same thing.

## Format
**Date** – Decision  
Reason / context

---

**2026-08-13** – Hero cards were resized (battlefield row height 58px → 72px; trimmed icon/type/HP font sizes; slimmer mana-bar padding).
Reason: The per-hero mana bar was already implemented and wired up correctly in JS, but the card's flex content was taller than the card's own box, so the mana bar silently overflowed past the bottom of the card and rendered behind the next row's card — it existed but wasn't visible. This was a layout bug, not a missing feature.

**2026-08-13** – Each hero's max mana equals the highest-cost ability they have access to, not a flat pool.
Reason: A flat 10-mana cap meant heroes could sit well above what they'd ever need to spend. Normal heroes (Basic Attack only) cap at `BASIC_COST` (3); special heroes (Basic + Special) cap at `SPECIAL_COST` (5). Revisit this if/when more ability tiers are added — the rule as implemented is "max = cost of your most expensive unlocked ability."

**2026-08-13** – "Do Nothing" extended to ALL player heroes, and every hero now acts once per turn (supersedes the earlier same-day decision that scoped it to special heroes only).
Reason: The turn flow was changed so the enemy turn starts automatically once every living player hero has acted (attacked or chosen Do Nothing) — see next entry. For that to work, every hero (not just specials) needs a way to explicitly declare "done," so the action popup (Basic / Special-if-unlocked / Do Nothing / Cancel) now opens for any hero the player taps, not just specials. A side effect: heroes can no longer attack more than once per turn — previously a hero could attack repeatedly as long as they had mana; now choosing an attack (or Do Nothing) locks them until the next player turn.

**2026-08-13** – Turn flow auto-advances: once every living player hero has acted, the enemy turn begins automatically.
Reason: Player is always the attacker, always on the left side; the enemy should never act until the player is done. Requiring a manual "End Turn" click after already using every hero was redundant friction. The End Turn button remains as a manual override for ending the turn before every hero has acted (e.g. deliberately banking mana on some heroes for next turn).

**2026-08-13** – Gem/hero color palette set to 5 colors: Red, Green, Blue, Yellow, Purple (previously 4: red, green, blue, gold).
Reason: Explicit request. "Gold" was replaced with Yellow, and Purple was added as the 5th. The per-side hero color distribution rule (at least 3 distinct colors among the 5 heroes, no more than 2 sharing a color) already existed in `isValidColorSet`/`generateSideColors` and satisfied the request as-is — only the `COLORS` array and matching CSS (tile colors, card gradients, color-dot swatch) needed updating.

**2026-08-13** – FINAL: "Do Nothing" is special-heroes-only. Normal heroes must attack if able, and are only exempt from blocking the turn when they truly can't act (not enough mana). This supersedes the earlier same-day decision that extended it to all heroes.
Reason: Direct instruction — "All characters on my side must use their attack if they can, except those with the option to Do Nothing. Only those with more than one attack option have that Do Nothing option." Normal heroes lost the action popup entirely and went back to direct-select (click hero → click enemy), matching the very first ask in this project. `checkAutoEndPlayerTurn()` now treats a normal hero as "resolved" if it attacked OR it has less than `BASIC_COST` mana (can't act at all); a normal hero that CAN act still blocks the auto-advance until it does.

**2026-08-13** – A player turn is now: (1) swap two tiles — exactly one swap, (2) attack with any character who can, (3) if no one can attack, the turn is over.
Reason: Direct instruction. Previously the player could swap tiles as many times as they wanted before ending their turn; now the board locks (dims via `#board-wrap.locked`) after one successful (match-creating) swap, and further clicks are blocked with a log message until the next player turn. A failed/reverted swap (no match) does not use up the turn's swap. `checkAutoEndPlayerTurn()` is now called immediately when the match cascade finishes, not just after an attack or Do Nothing, so the turn ends automatically the instant nobody is able to attack — matching "if no one can attack, turn over" without requiring a manual End Turn click. The existing per-hero resolution rules (special heroes need an explicit attack-or-Do-Nothing; normal heroes are exempt only when they truly can't afford to act) are unchanged and still govern what "no one can attack" means.

**2026-08-13** – Removed the "End Turn" button from the UI.
Reason: Direct instruction — with the turn structure now fully automatic (one swap → attack with anyone who can, or not → auto-advance the instant no one can act), there's no scenario left where the player needs to manually end their turn early. `endPlayerTurn()` the function is unchanged and still called internally by `checkAutoEndPlayerTurn()`.

**2026-08-13** – Melee/Ranged targeting restriction implemented in code for the first time.
Reason: CLAUDE.md and GAME_MECHANICS.md had documented "Melee units cannot attack enemy Ranged until the enemy Melee zone is cleared. Ranged units can attack any enemy." as a core rule since the project's design docs were written, but the actual combat code never enforced it — any hero could attack any enemy. The user's turn-order spec explicitly called this out as step 2.2, which surfaced the gap. Added `getValidTargets(attacker)`, applied to both the player's tap-to-attack flow (blocked target logs a message, no state change) and the enemy AI's target selection, so the rule now holds for both sides symmetrically.

**2026-08-13** – Fixed a turn-ending deadlock: a hero with less than `BASIC_COST` mana is now exempt from `checkAutoEndPlayerTurn()`'s "must resolve" requirement regardless of whether it's a normal or special hero (previously the exemption only applied to normal heroes).
Reason: The user reported the turn wasn't auto-ending even after attacking with everyone who could, leaving the ones who couldn't, and using Do Nothing on the special hero that had a real choice. Root cause: `onHeroClick`'s mana check (`if (h.mana < BASIC_COST) return;`) runs before the special-hero branch that opens the action popup — so a special hero with too little mana to even afford Basic Attack could never reach the "Do Nothing" button, meaning `actedThisTurn` could never be set for them, and the old logic (which only exempted normal heroes) waited on them forever. The fix makes the mana-based exemption apply to all heroes: "must attack or explicitly Do Nothing" only kicks in once a hero actually has enough mana for a real decision.

**2026-08-13** – The turn's swap is now enforced as mandatory, not just capped at one per turn.
Reason: Direct instruction — "I still need to do both: attack (if possible) and always swap two tiles." Mana carries over between turns (only `actedThisTurn` resets), so it was possible for a hero to have enough leftover mana to attack — or for every hero to already be "resolved" — before any swap happened this turn, letting the player attack (or the turn auto-end) without ever swapping. Closed both gaps: `onHeroClick` now refuses to select a player hero to attack until `swapUsedThisTurn` is true ("Swap two tiles first — attacks come after your swap."), and `checkAutoEndPlayerTurn()` now also requires `swapUsedThisTurn` before it will end the turn. This makes the sequence airtight: swap (required) → attack anyone who can → turn ends only once both the swap happened and no one can act.

**2026-08-13** – Removed randomization from character-card setup; hero colors, types, and special/normal assignment are now fixed per side.
Reason: Direct instruction — "Let's remove the randomization of the character cards." Replaced `generateSideColors()` (random, constraint-checked via `isValidColorSet`) and the random `pSpecials`/`eSpecials` index picks with fixed constants: `PLAYER_COLORS`/`ENEMY_COLORS` (one hero of each of the 5 colors per side) and `SPECIAL_INDICES = [0, 3]` (a fixed ranged + melee special per side). Deleted the now-unused `generateSideColors()`/`isValidColorSet()` functions. This only affects hero/character setup — the match-3 board tiles are untouched and still generate randomly, since that's core to the puzzle mechanic and wasn't in scope. `buildArmies()` now produces byte-identical heroes on every `resetGame()`.

**2026-08-14** – Split into two pages: `Roster.html` (character codex) and `Match_3_game.html` (the battlefield), sharing a new `characters.js` data module.
Reason: Direct instruction. `characters.js` holds the 35-character roster plus the shared combat constants (`BASIC_COST`, `BASIC_DMG`, `SPECIAL_COST`, `SPECIAL_DMG`) and `maxManaFor()`, loaded by both pages via `<script src="characters.js">`. The duplicate `const` declarations that used to live inline in `Match_3_game.html` were removed to avoid a duplicate-declaration collision now that both files load the same constants from one source. Roster.html is filterable (type/color) and searchable (name/ability text), and links to the battlefield; the battlefield links back to the roster.

**2026-08-14** – The old 5-hero, randomly-colored placeholder roster is retired in favor of 35 named characters across 16 classes.
Reason: Direct instruction — full roster (name, title, class, melee/ranged type, HP, color, damage type, weapon, and for ★ characters a named ability) transcribed into `characters.js`. Per the user's answer to a clarifying question ("roster now, simple abilities work, complex ones deferred"), all 35 characters are fully defined and browsable now, but only 4 abilities are mechanically wired into combat: Frenzy (Korr), Cheap Basic (Kael, Lumi), Divine Strike (Rorik), Brutal (Brick) — tagged via a `simpleAbility` field. The other 30 characters' Specials currently just deal the standard 5 mana → 2 damage; their ability text is shown on the Roster page with a "📜 Described" tag (vs "⚙ Implemented" for the working ones) so this isn't mistaken for a bug.

**2026-08-14** – Battlefield redesigned from 5 alternating melee/ranged slots per side to 6 dedicated Melee slots + 3 dedicated Ranged lanes per side (9 total per side, up from 5).
Reason: Direct instruction ("Melee columns should have 6 rows instead of 5"). Ranged units occupy one of 3 fixed lanes, each rendered as a card spanning 2 melee-row-heights. Melee occupies one of 6 slots; each slot is mapped to which ranged lane(s) it guards via `MELEE_SLOT_DEFS`: 2 slots aligned fully to lane 0 duplicated... (see next entry for the exact mapping and the half-step design). This also means the battlefield's per-side capacity is now 9, not 5 — noted as superseding the old flat "Max 6v6" framing for this specific screen (see TODO.md).

**2026-08-14** – Melee stagger/half-step coverage mapping finalized: slot 0 = aligned to lane 0, slot 1 = boundary covering lanes 0+1, slot 2 = aligned to lane 1, slot 3 = boundary covering lanes 1+2, slot 4 = aligned to lane 2, slot 5 = a second aligned guard for lane 2.
Reason: Answering the clarifying question "how should melee stagger in front of ranged," the user picked "half-step positions" — melee can align fully with one ranged lane (full cover of just that one) or sit on a boundary row between two lanes (partial/shared cover of both), with coverage being binary (no damage-reduction gradient). This exact 6-slot mapping was my own implementation choice to fit 6 physical slots against 3 lanes with at least one boundary row per internal seam (0/1 and 1/2) — it wasn't approved slot-by-slot by the user and produces a deliberate asymmetry (lane 0 gets 2 possible guards, lanes 1 and 2 get 3 each), since a perfectly symmetric split isn't achievable with exactly 6 slots, 3 lanes, and a working stagger system. Flagged in TODO.md for the user to weigh in on once they can see/play it.

**2026-08-14** – Melee/Ranged targeting protection changed from zone-wide ("melee can't reach ANY ranged until ALL enemy melee are dead") to per-lane ("melee can't reach a SPECIFIC ranged unit while a melee slot guarding that unit's lane is occupied").
Reason: Direct instruction — "While melee is partially or fully in front of a ranged character the ranged character cannot be attacked by opposing melee character." This is strictly more granular than the old all-or-nothing rule and is what makes the half-step stagger system meaningful: a single melee guard can now protect just its own lane(s) while other lanes remain open, rather than the entire enemy ranged line being locked down by any one surviving melee unit anywhere. Implemented via `getCoveredLanes(side)` (which ranged lanes currently have a living guarding melee) feeding into `getValidTargets()`. Ranged attackers are unaffected — always could and still can hit anyone.

**2026-08-14** – Added a deployment/placement screen: player builds their own team by tapping a character from their available roster, then tapping an empty slot of the matching type to place them (tap-to-place, not drag-and-drop).
Reason: Direct instruction, with drag-and-drop vs. tap-and-drop left to my judgment — chose tap-to-place since it's more reliable on touch devices, which matches the project's mobile-first priority (see TODO.md's "choose game engine" note). The player's selectable pool is the 35-character roster minus the 9 characters on the fixed enemy team (26 available); the enemy team is fixed (not random), consistent with the earlier "remove randomization" decision. Deployment is a separate step from resetting a battle in progress — "Restart Battle" keeps the current placement and just resets HP/mana/board, while a new "Change Deployment" button clears the battle and returns to the roster-picking screen.

**2026-08-14** – Win/lose detection unified inside `performAttack()` to check both outcomes after every attack, regardless of which side attacked.
Reason: Bug found while reworking combat for named characters — the old code only checked "did the player just win" inside the player's own attack path, and only checked "did the player just lose" once, at the very end of the enemy's whole turn (after all its attacks had already fired). Centralizing the check inside `performAttack()` means a defeat is now caught immediately after the exact enemy attack that causes it, not delayed until the turn's other attacks (which would be pointless/confusing to still process) finish.

**2026-08-14** – Ranged lanes increased from 3 to 5; Melee stays at 6 slots. Battlefield capacity per side is now 6 Melee + 5 Ranged = 11 (up from 9).
Reason: Direct instruction ("Ranged column needs 5 rows. Melee with 6 rows."). `MELEE_SLOT_DEFS` was rebuilt for the new 6-slot/5-lane ratio: slots 0, 1, 2, 4, 5 each align fully to one lane (0, 1, 2, 3, 4 respectively — every lane gets a dedicated guard), and slot 3 is the one remaining slot, placed as a boundary covering lanes 2 and 3. This is a smaller stagger budget than the previous 3-lane version (which had a boundary at every seam) — only the lane 2/3 seam currently has a half-step option, since there's exactly one "spare" slot once every lane has its aligned guard. `ENEMY_TEAM_IDS.ranged` was expanded from 3 to 5 ids (added Mireya and Caelum) so the fixed enemy team still fully occupies every ranged lane.

**2026-08-14** – Battlefield cards reverted from the very compact 44px/tiny-font design back to a "standard" uniform size (66px, larger fonts, mana bar with an overlaid number) for both Ranged and Melee.
Reason: Direct instruction ("Cards must have standard size"), with a forward-looking note that cards may eventually become 2D character art — so the current box-with-icon-and-text card is explicitly a placeholder, not a final look. This change coincided with dropping the CSS Grid row-spanning layout (which had forced Ranged cards to be double-height to visually align with 2 Melee rows) in favor of two independent flexbox columns — now that Ranged (5) and Melee (6) have different row counts, pixel-aligning them no longer made sense, and coverage was already fully data-driven (`MELEE_SLOT_DEFS`) rather than inferred from visual position.

**2026-08-14** – The player's deployment pool is now the full 35-character roster; picking a character does not depend on whether the enemy's fixed team also uses them.
Reason: Direct instruction ("Enemies can use characters but not restrict the user from selecting the same character") — supersedes the previous round's decision to exclude the enemy's 9 (now 11) fixed characters from the player's pool. A player can now field the same named character the enemy fields (a mirror matchup), if they choose to. The only remaining restriction is that a player can't place the same character into two of their own slots at once (already enforced — placing removes an id from `deployAvailable` until it's unplaced).

**2026-08-14** – Added Melee/Ranged filter chips + a Clear button to the deployment roster list.
Reason: Direct instruction. With the player's pool now the full 35 characters (up from 26) and always fully open, filtering became more useful for narrowing down what to browse. Two toggle chips ("⚔ Melee" / "🏹 Ranged") filter `deployAvailable` by type; clicking an already-active chip toggles it off. A separate "✕ Clear" button resets the filter to show everyone, requested explicitly rather than relying on re-toggling the active chip.

**2026-08-14** – Battlefield redesigned again: dropped all fixed slot/lane counts in favor of a single **team cap of 5 characters per side** (any mix of Ranged + Melee), with Ranged placed by tap-to-place (stacking top-to-bottom in placement order) and Melee placed by genuine drag-and-drop with a live coverage preview.
Reason: Direct instruction, based on 4 hand-drawn diagrams showing melee should be freely positionable either directly in front of one ranged character or straddling the gap between two adjacent ranged characters — "1. No more than 5 characters on each team. 2. ...the melee column is kind of flexible... Not sure how it should be handled visually or code wise." Asked a clarifying question on the visual/interaction model; the user's answer diverged from all three offered options: "I was thinking when they tap and drag the card, it should auto show what ranged characters it protects. While the user is dragging it in the Melee column." This requires real drag gestures, not tap-to-place, for Melee specifically. Ranged placement and stacking-guard questions were answered by accepting my recommended defaults (stack in placement order; multiple Melee may stack on the same lane/boundary). This entirely replaces the previous 6-slot/5-lane fixed-count model (and its `MELEE_SLOT_DEFS` static coverage table) from the two prior entries above — those are now historical, not current.

**2026-08-14** – Melee coverage is now a per-hero, dynamically-computed `coverage` object (`{type:'aligned', lane}` or `{type:'boundary', lanes:[a,b]}`) instead of a lookup into a fixed slot-definition table.
Reason: Once melee is drag-placed anywhere along the Melee column rather than into one of a fixed number of slots, there's no static slot index to key off of. `computeBandFromRects(rects, y)` is a pure function that takes the live `getBoundingClientRect()` results of the currently-placed Ranged cards plus the pointer's Y position and returns which lane(s) the drop point falls against (aligned to one card, or in the gap/boundary between two adjacent cards). This is invoked at drop time (and at reposition time, if an already-placed Melee card is dragged again) to set that hero's `coverage`. `getCoveredLanes()` was updated to read each living Melee hero's own `coverage` field instead of indexing into a slot table; `getValidTargets()` and all other targeting logic were unaffected since they only consume `getCoveredLanes()`'s output.
Verification note: drag-and-drop can't be exercised through real touch/mouse input in this environment. It was verified via (a) unit tests of `computeBandFromRects` against synthetic rects, and (b) full simulated pointer-event drag gestures in jsdom with `Element.prototype.getBoundingClientRect` monkey-patched to return realistic card positions. This exercises the whole preview → drop → coverage-assignment pipeline in code, but is not a substitute for testing the actual gesture on a touch device — flagged as the top item in TODO.md's Next Actions.

**2026-08-14** – `ENEMY_TEAM` rebuilt to exactly 5 characters (2 Ranged: Lumi, Saryn; 3 Melee: Korr, Rorik, Brick) to match the new `TEAM_CAP = 5`, replacing the previous 11-character (6 melee + 5 ranged) fixed enemy roster.
Reason: Direct consequence of the team-size cap dropping from 11 to 5 per side. The 5 enemy characters were deliberately chosen (not arbitrary) to keep all 4 mechanically-implemented simple abilities exercised in any playtest: Korr (frenzy), Lumi (cheapBasic2), Rorik (divineStrike), Brick (brutal). Their melee `coverage` was hand-assigned to demonstrate both coverage shapes: Korr aligned to lane 0, Rorik boundary covering lanes 0+1, Brick aligned to lane 1.

**2026-08-14** – Ranged placement changed from tap-to-place/stack-in-order to drag-to-any-of-5-fixed-slots; Melee coverage math reworked to resolve against that fixed grid (with gaps) instead of "however many Ranged happen to be placed."
Reason: Direct instruction — "1. I want user to either drag card to place of their choice. 2. Ranged column should show all five spots of choice. 3. Melee should allow user to freely drag up and down, with system showing what ranged is protected (up to two ranged at the same time) while the user drags it up or down. 4. Melee can protect up to two ranged, as long as both ranged are together and melee is partially in front of both." Point 4 is effectively confirming the existing boundary-coverage behavior from the previous round, but points 1–2 change Ranged from a stacking list to a fixed 5-slot grid the player drags into and picks a spot on — this can leave gaps (e.g. a Ranged hero in slot 3 with slots 0–2 empty). `playerPlacements.ranged` changed from a growable array (index = placement order) to a fixed-length `RANGED_SLOTS`-sized array (`charId | null`, index = slot/lane number).
Consequence for Melee coverage: since Ranged slots can now be sparse, the boundary-coverage rule needed to explicitly account for empty slots. Split the old single `computeBandFromRects` into `computeSlotBand` (pure geometry — which of the 5 fixed slots, or which boundary between two adjacent ones, a Y position falls in, independent of what's filled) and `resolveMeleeCoverage(band, filledMask)` (turns that geometric band into actual coverage): a boundary between two filled neighbors covers both; a boundary where only one neighbor is filled covers just that one; a boundary between two empty slots covers nothing. This is a direct implementation of "as long as both ranged are together" — "together" means both physically adjacent slots are occupied, not just adjacent in geometry.
Ranged and Melee are now symmetric in interaction model (both drag-and-drop) but asymmetric in placement freedom: Ranged snaps to one of 5 discrete, always-visible slots; Melee remains a continuous free-form column with no slots of its own, per the user's explicit "freely drag up and down" wording for Melee specifically.

**2026-08-14** – Melee column on the deployment screen now renders sorted by `primaryLane(coverage)` instead of raw insertion order.
Reason: Bug report — "Melee column should allow the user to place a card anywhere in that column, not just at the top." The deployment screen's Melee rendering never adopted the lane-based sort that `renderBattlefield()` (the live-battle view) already used; it rendered `playerPlacements.melee` in push/array order instead, so a card's visual row was fixed by when it was placed, not by where it was dropped — repositioning only changed the stored `coverage`, never the array position, so a card could be dragged anywhere and it would still render in its original slot in the list. Fixed by sorting a `{entry, originalIndex}` view of the array by `primaryLane` at render time (keeping the real array index for the drag/reposition/remove handlers), matching the live-battle view's existing behavior. Ranged is unaffected — it already renders by fixed slot index, which is inherently drop-position-accurate.

**2026-08-14** – Corrected the Melee free-positioning fix: card position in the column is now driven directly by drop Y coordinate (array insertion index), completely decoupled from `coverage`, instead of being derived from it via a sort.
Reason: The prior same-day fix (sort the Melee column by `primaryLane(coverage)`) was reported as not working. Diagnosis: that sort makes visual order a *function of* coverage, but coverage and position are conceptually different things — when no Ranged units are placed yet (the exact state in the user's screenshot), every Melee card's coverage is `null`, so the sort has nothing to differentiate on and falls back to insertion order, reproducing the original "always near the top" symptom. Replaced with `computeMeleeInsertIndex(y, excludeIndex)`, which counts how many other currently-rendered Melee cards sit above the drop point and uses that count as the literal array insertion index — `playerPlacements.melee`'s array order is now rendered directly with no sort, so it IS the on-screen order. Coverage is computed independently, exactly as before. This makes the two concerns fully orthogonal: a card's row is about where you dropped it; its coverage tag is about what it's protecting — a card can occupy any row with any coverage (including none).
Also, per the same message: enemy Melee/Ranged columns are now vertically centered (`justify-content:center`, via a new `.center-content` class) instead of top-aligned, so a short fixed enemy roster doesn't look pinned to the top of a taller column. Applied to both the deployment screen and the live battle view; the player's own columns are unaffected.

**2026-08-14** – Found and fixed the actual root cause of "only one place to put Melee": `#battlefield` was using `align-items: flex-start`, so the Melee drop zone's real bounding box (the same `.bf-column` element the drop-detection code tests the pointer against) was only ever as tall as its own current content — a tiny box with 0-1 Melee placed. Changed to `align-items: stretch` so all 4 columns match the tallest one (the Ranged column's fixed 5-slot height).
Reason: Two prior fixes this same day (sort-by-coverage, then position-by-drop-index) both addressed the placement *math*, which was never actually broken — verified correct by jsdom tests both times. The real bug was a CSS layout issue: dropping below the Melee column's tiny actual box registered as "outside the drop zone" and got treated as a cancel/removal, regardless of how correct the underlying insertion-index math was. This class of bug is invisible to the jsdom tests used throughout this session, since every one of them monkey-patches `getBoundingClientRect` to fake positions rather than running a real layout engine — so the tests were faithfully verifying logic against layouts that don't match the real page. Verified this round with Playwright + real headless Chromium instead: confirmed the Melee dropzone's actual computed rect now matches the Ranged column's (365px, same top/bottom) with zero Melee placed, and performed real `mouse.down/move/up` drags to the top and bottom of that zone, both landing correctly. Added a `.bf-column.dropzone-hint .card.empty-slot` CSS rule so the "drag here" placeholder itself also visually fills the stretched height, instead of sitting above an unexplained empty gap.
Side effect (a fix, not a regression): this is also what makes the earlier "move enemy layout to the middle" change actually visible — `justify-content:center` (added to the enemy columns earlier the same day) had no visible effect while a column's box was exactly as tall as its content; now that all columns stretch to the Ranged column's height, the enemy cards visibly center within the extra space.

**2026-08-14** – Melee placement rebuilt a third time: position (`band`) and mechanical protection (`coverage`) are now fully separate per-hero fields, and placed Melee cards are absolutely-positioned to be pixel-level with the Ranged row (or half-step gap) they're on.
Reason: Direct clarification after the CSS drop-zone-size fix still didn't satisfy the request: "I should be able to place the card in the same 'row' as the ranged character, OR put it a half step down or up to be half in front of two ranged characters." This reveals the actual intended design was never "a free-floating ordered list" (the previous fix) — it's a literal visual snap to the Ranged grid, exactly two placement modes (full row, or half-step between two adjacent rows), matching the original hand-drawn diagrams from the start of this feature. `band` (geometry: which Ranged slot index, or which adjacent pair) is now stored per Melee hero and is always resolvable since the Ranged slot grid is a fixed, always-rendered `RANGED_SLOTS`-length structure regardless of what's filled. `coverage` (what's actually protected) is derived on demand from `band` + current fill state, rather than frozen at drop time — so placing a Melee hero next to a not-yet-filled Ranged slot now correctly starts protecting automatically once that slot fills, without needing to re-drag.
Rendering changed from normal-flow list order to `position:absolute` cards inside the (already-stretched) Melee column, with each card's `top` computed directly from the real on-screen rect of the Ranged slot(s) its band refers to — this is what makes "same row" a literal, verifiable visual fact rather than an approximation. Cards sharing a band fan out with a small offset (stacking is still allowed). Drag feedback now shows two tiers: gold for "you'll snap here" (the raw band, filled or not) and green for "and this is actually protected" (only for filled slots).
This obsoletes the array-order-based positioning system from the same day's earlier fix (`computeMeleeInsertIndex`) entirely — array order in `playerPlacements.melee` no longer has any visual meaning.

**2026-08-14** – Widened the boundary (half-step) hit zone in `computeSlotBand` from the ~4px CSS gap between Ranged cards to a generous, pitch-relative band centered on the midpoint between two adjacent slots' centers.
Reason: Direct feedback after testing the row/half-step snapping feature — "it's not easy to put it in front of two ranged characters." The boundary target had been the literal visual gap between two Ranged cards (`rects[i].bottom` to `rects[i+1].top]`), which is only the CSS `gap: 4px` — a few-pixel-tall touch target, unrealistic to hit reliably. Rewrote around slot centers: `BOUNDARY_FRACTION = 0.6` means 60% of the pitch between two adjacent centers now counts as boundary territory (roughly ±21px for the current 66px-card/4px-gap layout), with the remaining 40% split evenly as "aligned" zones nearest each center. This is a pure hit-testing change — band/coverage shapes, drag highlighting, and row-snapped rendering are unaffected, only which pointer Y positions map to which result. Verified in a real browser: a drop 15px off the exact midpoint (well outside the old target) now correctly resolves to boundary coverage of both neighbors, while a drop near a card's own center still resolves to aligned coverage of just that one.

**2026-08-14** – Added a third deployment filter chip, "★ Heroes", filtering by `isSpecial` rather than by Melee/Ranged type.
Reason: Direct instruction — "Create a third filter: Heroes. all of the characters with special abilities are heroes." `isSpecial` was already the field driving the ★ marker and named-ability display throughout the project, so this reuses it directly rather than introducing a new concept. Mutually exclusive with the existing Melee/Ranged chips (same toggle/Clear behavior), since a Hero can be of either type. Noted in TODO.md that 32 of the 35 characters are currently `isSpecial`, so this filter only excludes 3 — not a bug, just the current roster's composition.

**2026-08-14** – Added a fourth deployment filter chip, "☆ Non-Heroes", filtering to `isSpecial === false` — the mirror image of the Heroes filter.
Reason: Direct instruction — "Add filter: Non-Heroes. For all those without special abilities." Same pattern as the Heroes filter (reuses `isSpecial`, mutually exclusive with the other three chips, same toggle/Clear behavior). Since only 3 of the 35 characters are non-special, this filter narrows the roster to exactly those 3 (`vesper_quill`, `kael`, `lumi`) — useful precisely because that group is otherwise buried in the full 35-character list.

**2026-08-14** – Deployment filter chips are no longer mutually exclusive: `deployTypeFilter` (single value) replaced with `deployActiveFilters` (a `Set`), and every chip toggles independently. Combining rule: chips within the same group OR together, groups AND together. The two groups are type (Melee/Ranged) and specialty (Heroes/Non-Heroes).
Reason: Direct instruction — "Allow multiple filters to be applied, like 'Heroes' and 'Ranged' etc." A pure OR across all active chips would be useless for narrowing (Heroes OR Ranged would just add more results, the opposite of what "combine these filters" implies), and a pure AND across all four would be broken since Melee and Ranged are mutually exclusive per-character properties (AND-ing them always yields zero results). The chosen group-based OR-within/AND-across rule is the standard faceted-filter pattern (e.g. same idea as e-commerce filter sidebars) and matches the example given: Heroes + Ranged narrows to Ranged Heroes specifically, which is what "applying both" should mean.

**2026-08-14** – Added 10 new non-hero characters (`isSpecial: false`) under a new "Mercenary" class, exactly as specified by name/weapon/color/mana/damage: 5 melee (Kael Ironwood, Brother Rurik, Grommash the Red, Sera Vey, Tomas Dualedge) and 5 ranged (Lira Swiftarrow, Harkon Bolt, Mira Quietshot, Fen Willow, Vex Needle). Roster grows from 35 to 45 characters (13 non-heroes total).
Reason: Direct instruction with a fully specified character list. Ids were deliberately chosen to avoid collisions with existing, similarly-named heroes already in the roster (e.g. the new `kael_ironwood` vs. the pre-existing `kael`/"Master Kael Dualedge"; `fen_willow` vs. the pre-existing `fen`/"Fen the Packbrother"; `lira_swiftarrow` vs. the pre-existing `lira`/"Lira Voss" — all coincidental first-name/surname overlaps with the new mundane-weapon characters). HP, `class`, `title`, and `damageType` weren't specified by the user, so reasonable values were chosen by analogy to existing characters with similar weapons (heavier weapons → more HP; `damageType: 'Physical'` throughout, matching their mundane, non-magical weapons; `title: null`, matching the precedent set by `cogwright`). None of the 10 have `abilityName`/`abilityText`/`simpleAbility` — plain troops with no unique ability, consistent with `isSpecial: false`.

**2026-08-14** – Two of the new characters (Grommash the Red, Harkon Bolt) have a heavier Basic Attack (6 mana → 2 damage) than the shared default (3 mana → 1 damage). Added optional per-character `basicCost`/`basicDmg` fields to `characters.js`, with `basicCostFor()`/`basicDmgFor()` helpers resolving the actual value (explicit override, else the existing `cheapBasic2` discount, else the shared constant) — and updated `maxManaFor()` to raise a character's mana cap when their `basicCost` exceeds the old baseline, otherwise a character with a costlier basic attack could never accumulate enough mana to use it at all.
Reason: The previous data model had no way to express a per-character Basic Attack cost/damage — it was a single shared value for every non-Special character, plus one special-cased discount (`cheapBasic2`, used by Kael and Lumi). The user's numbers for Grommash and Harkon don't fit either case, so a general override mechanism was needed rather than a second one-off special case. `basicCostFor`/`basicDmgFor` were moved into `characters.js` (previously `Match_3_game.html` had its own local `basicCostFor`, and `Roster.html` didn't compute it at all — it hardcoded "3 mana" / "1 damage" text on every card) so both screens share one source of truth and stay in sync automatically as new characters are added. Verified via jsdom that existing characters are completely unaffected (Kael/Lumi's `cheapBasic2` discount and every Hero's `maxMana` computed identically to before), and that a full attack simulation on Grommash correctly spends 6 mana for 2 damage.

**2026-08-14** – The live battle screen's `renderBattlefield()` now renders the player's Ranged/Melee columns with the exact same technique as the deployment screen's `renderDeployBattlefield()`: a fixed `RANGED_SLOTS`-slot grid for Ranged, and `band`-pixel-aligned absolute positioning for Melee (via a new `positionMeleeCardPairs()` helper, mirroring `layoutMeleeCards()`). A hero's `band` is now preserved on the live hero object (`createHeroFromChar`/`buildArmies`) instead of being discarded right after computing `coverage` at battle start.
Reason: Direct instruction — "Layout must stay same in Deployment as well as in the battle" — backed by screenshots showing the player's Ranged column compacting from 5 fixed rows down to however many heroes were actually placed, and Melee cards jumping from their deployment-time row/half-step position into a plain sorted list, the moment "Start Battle" was pressed. `playerPlacements.ranged` never changes after deployment (a Ranged hero's lane is fixed once placed, whether alive or dead), so a fixed-grid render works identically for both screens; a Melee hero's `band` similarly never changes, so preserving it onto the hero object (rather than only the derived, once-frozen `coverage`) lets the battle screen position it exactly as deployment did, including for a Melee guarding a Ranged slot that was left empty. Deliberately implemented as a separate function from the deployment screen's `layoutMeleeCards()`/`renderDeployBattlefield()` rather than a shared one, so a future change aimed at one screen can't silently regress the other. Verified with real headless-Chromium screenshots: a hero placed in Ranged slot 3 of 5, or Melee aligned to a specific row/boundary, renders in the identical pixel position in both the deployment preview and the post-"Start Battle" live view.
Investigating this surfaced a second, independent bug (see below) that had nothing to do with layout consistency — it was already present in the old battle rendering too, just easy to misread as "the layout is different" from a screenshot alone.

**2026-08-14** – Fixed `.card-name` rendering at ~2px tall (visually blank) on every live Melee card, in both the old and new battle layout. Bumped `.card`'s fixed height from 66px to 78px and added `flex-shrink: 0` to `.card-icon`, `.card-name`, `.hp-text`, and `.coverage-tag` (matching the flex-shrink:0 that `.mana-bar-bg` already had).
Reason: While investigating the layout-parity screenshots, a Playwright DOM measurement showed `.card-name`'s real rendered height at ~1.9px inside battle Melee cards, even though the correct name text was present in the DOM the whole time — a pure CSS bug, not a data/logic bug. Cause: a Melee card's full content (icon + name + HP text + mana bar + coverage tag) naturally measures ~74px, but `.card` was fixed at `height: 66px`; only `.mana-bar-bg` had `flex-shrink: 0`, and `.card-name` has `overflow: hidden` (needed for its text-ellipsis), which per the CSS flexbox spec resets a flex item's automatic minimum size to 0 — so when ~8px had to be shaved off to fit everything into 66px, `.card-name` was the one element allowed to shrink to almost nothing, and did. Ranged cards (no coverage tag, one fewer flex child) happened to fit under 66px without shrinking and were never visibly affected — which is why this had gone unnoticed even though it predates this session's layout-parity work entirely. Confirmed fixed via a direct Playwright measurement of `.card-name`'s rendered height going from ~1.9px to its natural ~10.1px; jsdom cannot verify this class of bug at all since it has no real layout engine (`getBoundingClientRect()` always returns all-zero there).

**2026-08-14** – The enemy team is no longer a fixed, static squad — it's randomized fresh every time deployment is (re)entered (`randomizeEnemyTeam()`, called at the top of `initDeployment()`), keeping the same shape as before (`TEAM_CAP`=5 total, 2 Ranged + 3 Melee, same fixed lane-coverage pattern for Melee). Two constraints: at most 2 of the 5 may be Heroes, and at least 4 of the 5 must show distinct colors.
Reason: Direct instruction — "Always randomize enemy team: always at least four colors, max two heroes." Implemented the hero cap by construction rather than by rejection-sampling random 5-character draws: with 32 of 45 characters (~71%) being Heroes, a naive "draw 5 random characters, reject the draw if more than 2 are Heroes" approach would frequently need many retries to succeed, and in principle could stall. Instead, the draw first randomly picks how many Heroes (0–2) the team will have and how that count splits across the Ranged/Melee roles, then samples specific characters from four pre-partitioned buckets (ranged-hero / ranged-non-hero / melee-hero / melee-non-hero) — this guarantees the hero cap on every single draw, no retries needed. The color constraint is comparatively easy to satisfy at random, so it's handled with simple bounded retry (up to 50 attempts, verified in practice to essentially always succeed on the first few tries) rather than a similarly explicit construction. Melee coverage keeps the exact lane0/boundary/lane1 pattern the original static team used — a property of Ranged always having exactly 2 lanes, unrelated to which characters are chosen. Verified via 500 simulated randomizations in jsdom (zero violations of team size, duplicate ids, hero cap, color diversity, or Ranged/Melee type-consistency) plus a live Playwright run confirming no rendering errors with a randomized team.

**2026-08-14** – The dashed "placement lining" (`.dropzone-hint`'s dashed outline) around the player's Melee column no longer appears once the battle starts — `renderBattlefield()` uses a new, narrower `.melee-positioned` class (just the `position: relative` needed as a containing block for the absolutely-positioned Melee cards) instead of `.dropzone-hint`. `renderDeployBattlefield()` is unchanged and keeps showing the dashed hint. Also added a permanent dotted divider (`.side-divider`) between the player's and enemy's columns, present in both deployment and battle.
Reason: Direct instruction — "After starting battle, placement lining should disappear" plus "for now, keep a dotted line between YOU and ENEMY." The dashed outline is a drag-target affordance (it exists to say "you can drop a card anywhere in this box"); once the battle starts nothing is draggable, so leaving it up just reads as stale deployment UI. The user was explicit, though, that removing it shouldn't remove ALL visual separation between the two sides — the dashed box's right edge had been incidentally serving as a YOU/ENEMY boundary marker, so a dedicated, deliberately different-looking (dotted, not dashed) divider replaces that incidental function on its own, independent of whichever hint styling the Melee column happens to have. "For now" suggests this dotted line is a placeholder choice, not a settled final design — worth revisiting if/when the battlefield gets more visual polish. Verified via Playwright: deployment keeps the dashed lining, battle does not (computed `outline-style: none`), and the dotted divider (`border-left: 2px dotted`) renders correctly in both screens with no console errors.

**2026-08-15** – Unfilled Ranged slots (and the player Melee column's "nothing deployed" placeholder) now render with `visibility: hidden` (new `.card.battle-empty-slot` class) instead of the dashed empty-slot box, during battle only.
Reason: Direct follow-up report — "unfilled ranged spot still visible during battle." The prior fix removed the dashed *outline* around the whole Melee column, but missed that each individual unfilled Ranged slot is its OWN dashed box (`.card.empty-slot`), which kept showing during battle for the exact same underlying reason (it's a drag-target cue with nothing left to target). `visibility: hidden` was chosen over `display: none` specifically because the fixed Ranged grid's whole purpose (see the "layout must stay the same" decision above) is to keep every filled slot's row position stable and give Melee band-positioning accurate rects to align against — `display: none` would collapse the slot out of the flex layout, undoing exactly the row-stability guarantee that was the point of building a fixed grid in the first place. `visibility: hidden` hides the box visually while its layout space still holds its place. Verified via Playwright that slot rects (height, top offset) are identical whether a slot is filled-and-visible or empty-and-hidden, and that a Melee card banded next to a hidden slot still aligns correctly.

**2026-08-18** – The enemy team's Ranged/Melee split is now also randomized (previously fixed at 2 Ranged/3 Melee even after `randomizeEnemyTeam()` was added), and the enemy's setup is hidden entirely on the deployment screen (a single "ENEMY ❓" placeholder replaces both real enemy columns) until the player starts the battle.
Reason: Direct instruction — vary the enemy's formation ("3 melee side-by-side defending both ranged, or two melee side-by-side defending three ranged and so on") and keep it a surprise until battle. Added `ENEMY_FORMATIONS`, an array of two shapes picked with equal probability each draw: the original 3 Melee/2 Ranged lane pattern (aligned/boundary/aligned), and a new 2 Melee/3 Ranged pattern (two boundary guards side by side, on lanes 0/1 and 1/2, together covering all 3 lanes). `drawEnemyRoster()`/`buildRandomEnemyRoster()` were generalized to take `rangedCount`/`meleeCount` params instead of hardcoded constants; the hero-cap and color-diversity rules are unaffected and apply identically regardless of which formation is drawn (verified via 40 simulated `randomizeEnemyTeam()` calls in the browser console: zero violations of total-size/hero-cap/color-diversity, roughly even 19/21 split between the two formations). Chose "fully hidden" over a "counts visible, identities hidden" middle ground specifically because revealing even the Ranged/Melee count breakdown would leak which formation was drawn — so `renderDeployBattlefield()`'s two real enemy columns were replaced with one generic placeholder column; `ENEMY_TEAM` itself is still decided once at deployment-entry time (not re-randomized at Start Battle), only the on-screen reveal is deferred to `renderBattlefield()`, which is unchanged and shows the real roster as it always has once the battle starts.

**2026-08-18** – Melee heroes can no longer stack on the same row or half-step gap (supersedes the earlier decision that explicitly allowed and fanned out stacked Melee guards). `onMeleeDragEnd()` now rejects (logs a message, leaves placement unchanged) any drop — new placement or reposition — whose resolved `band` matches another already-placed Melee's `band`; the check excludes the card being repositioned against itself. Added `bandsEqual()`/`meleeBandOccupied()` (~line 1375) as small pure helpers reusing the existing `bandKey()`.
Reason: Direct report — the user dragged two Melee onto the exact same row and they landed fully on top of each other. The existing fan-out offset (`FAN_STEP` in `layoutMeleeCards()`) was meant to keep stacked cards visually distinct but apparently wasn't enough/wasn't the point — the user asked for stacking to be disallowed outright rather than better-fanned. `layoutMeleeCards()`'s multi-member-group fan-out branch is now unreachable (every band can only ever have 0 or 1 Melee) but was left in place rather than refactored out, since it's harmless dead code and removing it wasn't part of the request. Verified live in-browser: dropping a second Melee on an occupied row is rejected with a log message and no state change; dropping on a different row still places normally, including onto a row with no Ranged hero yet (coverage `none`, as before).

**2026-08-18** – The drag ghost (the element that follows the cursor/finger while dragging a Ranged or Melee card) is now a full `.card` — same color gradient, icon, name, HP text, and special-hero glow as the placed/roster card — instead of a small plain-text pill.
Reason: Direct request — "the card should [look] the same as when it's placed while dragging it around for full visual look." `buildGhost(cd, type)` now builds the ghost via the existing `buildDeployPreviewCard('player', type, cd, null)` (same function used for roster/placed cards) and just adds the `.drag-ghost` class on top for the fixed-position/pointer-events-none/floating-shadow mechanics; `.drag-ghost`'s CSS was stripped down to only those positioning concerns so it no longer fights the `.card` styling for background/border/padding. Callers (`startRangedDrag`/`startMeleeDrag`) changed from passing a literal icon emoji to passing `'ranged'`/`'melee'` (matching `buildDeployPreviewCard`'s own `type` parameter). `moveGhost()`'s cursor offset was recentered for the ghost's new, larger footprint (95×78 card vs. the old small pill). Verified live in-browser: ghost now shows the real color-by-element/icon/name/HP/glow while dragging, placement still succeeds and the ghost element is removed on drop, no console errors.

**2026-08-19** – Roster list cards no longer start a drag instantly on touch — a brief ~250ms press-and-hold (finger roughly stationary) is required first; a quick swipe scrolls the list instead. Mouse/pen are unchanged (still instant drag on pointerdown).
Reason: Direct report — "too touchy for mobile," couldn't scroll the roster list without immediately grabbing a card. Root cause: roster cards had `touch-action: none` (blocks all native touch panning on that element) plus an unconditional `e.preventDefault()` on `pointerdown`, so the browser never got a chance to scroll — any touch on any card grabbed it. Fixed by setting roster cards to `touch-action: pan-y` (lets the browser scroll natively) and routing their `pointerdown` through a new `attachRosterDragStart()` helper (~line 1040) that, for touch only, delays the actual drag-start behind a `TOUCH_DRAG_HOLD_MS` (250ms) timer, cancelled if the finger moves more than `TOUCH_DRAG_CANCEL_PX` (10px) or the browser itself takes over the gesture as a native pan (signaled by `pointercancel`) before the timer fires. This is the same long-press-to-grab pattern used by iOS/Android reorderable lists and similar mobile drag UIs, chosen because a direction-based heuristic doesn't work here — placing a card is itself a mostly-vertical drag, the same axis as the list's own scroll, so the first few pixels of movement can't distinguish "scrolling" from "grabbing" on their own; only timing can. Only applied to the roster list — already-placed battlefield cards (Ranged slots, Melee column) keep instant `touch-action: none` drag-start, since that area doesn't scroll and has no such conflict. Verified via synthetic touch PointerEvents: a quick swipe (large movement well within the hold window) never starts a drag; a hold past the threshold does, and placement completes normally afterward with clean state either way; mouse dragging unaffected. Real physical-touchscreen feel not verifiable in this environment — flagged to the user to confirm on-device.

**2026-08-19** – Deployment gets a tap-first interaction model: roster cards now show a "+" badge (auto-places into the next open row from the top) and placed cards show a "−" badge (removes). Drag (both initial placement and repositioning) is now **mouse-only** — touch/pen pointerdown on a card does nothing at all. This supersedes the same-day decision that made touch dragging work via a press-and-hold delay; that whole mechanism (`TOUCH_DRAG_HOLD_MS`/`TOUCH_DRAG_CANCEL_PX`) was removed since it's no longer reachable.
Reason: Direct instruction, explicitly "for testing purposes" on the mouse-only drag restriction — suggesting the "+/−" tap model is the intended real mobile UX going forward, with drag kept around as a developer/desktop convenience for precise placement (particularly Melee's half-step boundary position, which "+" can't express — it only ever places aligned, to the first open row). `autoPlaceCharacter(charId, type)` (~line 1076) picks the target: Ranged uses the first empty index in `playerPlacements.ranged`; Melee scans `RANGED_SLOTS` indices for the first not already occupied by an aligned Melee (reusing `meleeBandOccupied()` from the earlier no-stacking fix), so it can never violate that rule or produce a boundary placement. The mouse-only gate lives in one place — `startRangedDrag()`/`startMeleeDrag()` themselves check `e.pointerType !== 'mouse'` and bail — rather than at each of the 4 call sites (roster cards × 2 types, placed cards × 2 types), so nothing can bypass it. `.card-color-dot` moved from the top-right corner to top-left to make room for the new top-right "+"/"−" badge (`.card-action-btn`); both badges stop their own `pointerdown` from bubbling so tapping one on a mouse never also triggers the card's drag-start. Verified live in-browser: tapping "+" on Ranged/Melee roster cards places into the correct next-open-row (including skipping an already-guarded row for a second Melee); tapping "−" removes and returns the card to the roster; the team-full message still fires correctly on a 6th placement attempt; Start Battle still works with a tap-placed team; synthetic touch PointerEvents confirm touch pointerdown never starts a drag (large post-down movement still leaves `dragState` null) while mouse pointerdown still starts one instantly; no console errors throughout.

**2026-08-19** – Refined the same-day "+/− badges, drag is mouse-only" decision: the mouse-only restriction now applies ONLY to placing a NEW hero from the roster — repositioning an already-placed card works on any input (touch included). Also restyled the badges: no circle/background, just a larger bare "+"/"−" glyph with a dark outline glow for legibility.
Reason: Direct follow-up after testing — the blanket mouse-only restriction from earlier today also silently broke touch dragging of already-placed cards, which was never the intent (the "for testing purposes" mouse-only framing was about the roster→battlefield drag specifically) and, more importantly, made a Melee's half-step boundary position completely unreachable on touch (since "+" only ever places aligned, and repositioning-drag was the only other path to a boundary). Fixed by making the gate conditional on whether this is a new placement or a reposition: `startRangedDrag()` checks `opts.fromSlot == null`, `startMeleeDrag()` checks `opts.meleeIndex == null` — only in that "new placement" case does `e.pointerType !== 'mouse'` bail out; a non-null fromSlot/meleeIndex (dragging a card that's already on the battlefield) proceeds for any pointer type. This is safe from the original scroll-vs-drag conflict that motivated restricting touch dragging in the first place, since the battlefield itself doesn't scroll — that conflict only ever existed for the roster list. Badge restyle ("remove the circle, enlarge the icons") was a separate, purely visual ask in the same message — `.card-action-btn` dropped its circular background/border for a plain colored glyph (`text-shadow` provides contrast against the card's own color gradient instead of a solid backing), sized up from 0.75rem to 1.15rem. Verified live in-browser: synthetic touch PointerEvents confirm a touch pointerdown on a roster card still does nothing (no placement), while a touch-drag on an already-placed Melee card successfully repositions it from aligned-lane-0 to a boundary spanning lanes 0/1 (coverage tag updates to "🛡 R1+R2"); mouse dragging unaffected in both cases; no console errors.

**2026-08-19** – Fixed two regressions from the same-day "repositioning works on any input again" change: (1) Melee cards could visually overlap again — an aligned card and an adjacent boundary card were never being compared as a conflict; (2) on a real phone, holding and dragging a placed card was hijacked by the browser as a text-selection gesture, leaving the card visually stuck.
Reason: Direct report after testing on-device. (1) The no-stacking check (`meleeBandOccupied`/what was `bandsEqual`) only compared exact `bandKey` strings — `aligned(0)` ("a0") vs `boundary(0,1)` ("b0") are different keys, so the check never caught that an aligned row and either of its two neighboring boundary positions overlap heavily on screen (a boundary sits at the midpoint between two row centers, and card height is a large fraction of the row pitch — verified earlier via measured rects: roughly half the card height overlaps). Renamed to `bandsConflict()` with real adjacency logic: two aligned bands conflict iff same index; two boundary bands conflict iff the same pair; an aligned/boundary pair conflicts iff the aligned index is one of the boundary's two indices. Two boundary bands sharing only one index (e.g. `{0,1}` and `{1,2}`) do NOT conflict — verified this doesn't break the "2 Melee side by side" enemy formation, which relies on exactly that non-overlapping adjacency. (2) Root cause: `touch-action: none` (and by extension the browser's willingness to hand touch-and-hold gestures fully to JS instead of offering native text selection) had been on placed Ranged/Melee cards originally, but was stripped out in an earlier same-day pass when touch dragging of placed cards was mistakenly assumed to be gone for good. Once repositioning-drag was restored for touch, the missing `touch-action: none` meant a hold-and-move on a placed card was ambiguous to the browser, and it started a native text-selection gesture instead — which both stole the gesture from our drag JS (leaving `dragState` orphaned mid-drag, hence the card "freezing" in place) and threw up selection UI. Re-added `touch-action: none` to both placed Ranged slot cards and placed Melee cards, and additionally added `-webkit-touch-callout: none` to the base `.card` class as a defensive measure against iOS's long-press callout/magnifier menu specifically (a separate mechanism from `touch-action`, not fully covered by the `user-select: none` that was already there). Verified live in-browser: placing two Melee that would overlap (aligned row adjacent to an existing boundary card) is now rejected via both "+" and drag; `getComputedStyle` confirms `touch-action: none` on a placed card. The text-selection hijack itself can't be reproduced with synthetic PointerEvents (native gesture recognition only responds to trusted touch input) — this fix targets the well-established, standard cause, but genuine confirmation needs the user's actual phone.

**2026-08-19 (latest)** – Added a manual "End Turn" button (`#btn-end-turn`, inside `#end-turn-wrap`) between the Battlefield and the Match-3 board, visible/hidden alongside the existing turn indicator and battle controls.
Reason: Direct instruction — with a mixed team of Heroes and regular characters, the only existing way to skip a hero's action and bank its mana was the per-hero action popup's "Do Nothing" button, which only Heroes (`isSpecial`) even have (regular characters select straight into Basic with no popup, per the existing "normal heroes have no Do Nothing" rule in `checkAutoEndPlayerTurn`'s comment) — so with a full team the player had to open each Hero's popup individually just to pass. The new button still gates on `swapUsedThisTurn` (same rule as attacking — one turn always includes a swap), then marks every living player hero that hasn't yet acted this turn as `actedThisTurn = true` (banking their mana without spending it, identical semantics to the existing per-hero Do Nothing) and calls the existing `endPlayerTurn()`. Deliberately additive: `checkAutoEndPlayerTurn()`'s automatic end-of-turn detection and the per-hero Do Nothing option are both unchanged, this is just a manual shortcut that produces the same end state. Verified via a scripted Playwright pass driving `CHARACTERS`/`heroes` directly: clicking before the swap logs the guard message and does nothing; clicking after the swap marks all three test heroes `actedThisTurn: true` with mana untouched, and correctly transitions `currentTurn` to `'enemy'`. Not yet confirmed on a real phone.

**2026-08-19 (even later)** – The End Turn button is now blocked while any normal (non-`isSpecial`) player character can still afford its Basic Attack and hasn't acted this turn — clicking it logs a message naming that character instead of ending the turn.
Reason: Direct report — the button as first shipped let normal characters skip their attack entirely, which contradicts the long-standing rule (predates this button) that normal characters have no "Do Nothing" option and must attack if able; only Heroes (`isSpecial`) get an optional skip via their action popup's Do Nothing button. Added `normalCharacterMustActFirst()`, checked in the button's click handler right after the existing swap-gate check, using the same click-time-log-and-return pattern (not a disabled/grayed style) for consistency with how the swap requirement is already enforced. Once no such character remains (all have attacked, or none can afford to), the button behaves as before — banking mana for any still-unacted Heroes. Verified via scripted Playwright: blocked with the turn unchanged while an affordable, unacted normal character exists; succeeds immediately after that character attacks.

**2026-08-19 (latest of all)** – The End Turn button now visually grays out (dims to `opacity: 0.4` and sets `disabled`) whenever it isn't usable, instead of only being gated at click-time.
Reason: Direct instruction — "Gray out end turn button until all normal characters have attacked that round." Added `updateEndTurnButtonState()`, reusing the same disabled-opacity convention already established by the Basic/Special modal buttons (`btnBasic`/`btnSpecial` already dim to 0.4 when unaffordable) rather than inventing a new visual language. Hooked into three existing, broadly-called functions — `updateHeroUI()`, `updateTurnUI()`, and the top of `checkAutoEndPlayerTurn()` — rather than adding a new call at every individual state mutation (swap, attack, mana grant, turn transition); those three already run at effectively every point that could change End Turn's eligibility, since they're the same functions the rest of the turn/combat UI already depends on to stay in sync. Verified via scripted Playwright across a full turn: disabled before the swap, still disabled after the swap while a normal character can still afford its attack, auto-enables the instant that character attacks, and a click at that point still correctly ends the turn.

**2026-08-19 (newest)** – The Runic Crystal now also auto-activates when caught in a fresh same-color match (the same "matched into a new group" trigger the Arcane Bomb already had), in addition to its existing direct-tap trigger. This supersedes the earlier design decision that a crystal "never auto-activates" via a match — that restriction is gone; only being touched by an unrelated, non-color-connecting swap still leaves it inert.
Reason: Direct instruction, reached after a clarifying exchange (the user's initial "Issue" report described a mechanic that, on investigation, was actually mostly already implemented and verified working — the shape/spawn detection for match-4/5, in particular, checked out correctly under direct testing, so I asked which specific piece was missing rather than guess at a redesign). The confirmed answer: "Runic Crystal and Arcane Bomb are supposed to auto trigger if the player swaps a tile of the same color that it's connected to," clarified further that a Crystal should need "two more tiles of the same color connected with it" — i.e. itself plus two more, the same ≥3 threshold every ordinary match already requires. Implemented by deleting the crystal-specific `if (type !== 'bomb') return;` early-out inside `queueExplosionIfSpecial()`: `processExplosionQueue()`/`applyCrystalEffect()` were already written to handle a match-caught special generically (they already had to, for the Bomb), so no other code needed to change. Deliberately left the Bomb's second, broader trigger (any adjacent swap touching its cell, regardless of color or match) untouched — the user's wording confirmed the missing *same-color-match* trigger for the Crystal, not a request to narrow the Bomb's existing swap-trigger, and CLAUDE.md instructs against redesigning working, documented behavior that wasn't actually asked to change. Verified via scripted Playwright: a pre-existing red Crystal, with a swap connecting two more red tiles into it, now auto-opens its choice modal (same as a direct tap would) and resolves the chosen effect correctly; a pre-existing red Bomb under the identical setup still auto-detonates exactly as before, confirming no regression to the unrelated Bomb path.

**2026-08-19 (character placement page)** – The deployment (character placement) screen no longer shows any enemy section — the previous single "ENEMY ❓" placeholder column is removed entirely, not just its contents. The remaining player Ranged/Melee columns are centered on the screen (via a new `deploy-battlefield-narrow` class that halves `#battlefield`'s max-width from 420px to 220px, applied only in `renderDeployBattlefield()` and removed again in `renderBattlefield()`), and their column labels drop the "YOU" text, showing just the bare `🏹`/`⚔️` icons.
Reason: Direct instruction, three items in one request: "Remove the enemy section altogether," "Center 'YOU' section to the middle of the screen," "Remove the words 'YOU'." Scoped explicitly to the "characters placement page" — the live battle screen (`renderBattlefield()`) is untouched and still shows all 4 columns with their original "YOU 🏹"/"YOU ⚔️"/"ENEMY ⚔️"/"ENEMY 🏹" labels, verified via a live-browser check after the change. The width-halving was needed because `.bf-column` uses `flex: 1 1 0` with no cap — simply deleting the enemy column would have let the two remaining columns' cards stretch to fill the full 420px row (roughly doubling their width), which nothing in the request asked for; halving the box width keeps their per-card size close to what it was before (and close to the battle screen's own per-card width, since deployment now has half as many columns in half the box). Centering itself needed no new CSS: the parent `body` already uses `display:flex; flex-direction:column; align-items:center`, so a narrower `#battlefield` block is automatically centered by its parent, the same way the box always was. Verified via a headless-browser screenshot and a `getBoundingClientRect()` check: the narrowed box sits at `left:100, right:320` in a 420px viewport — exactly centered. The user noted more changes are coming to this same screen.

**2026-08-19 (character placement page pt.2)** – Removed the outer dashed outline that wrapped the whole Melee column on the placement screen (kept individual empty-slot cards' own dashed borders), and added a "Formation" button (bottom-right corner of the placement box) that opens an explainer modal: a static 3-line text block plus a swipeable 3-slide carousel of inline-SVG diagrams below it.
Reason: Direct instruction, three items. (1) "Remove outer dashed line from around melee section, but leave the inner dashed line" — `.bf-column.dropzone-hint`'s `outline` was the outer box; stripped it out but kept the class itself (now a pure scoping marker) since a nested rule (`.dropzone-hint .card.empty-slot`) still needs it to make the empty-melee placeholder stretch to fill the column height, unrelated to the outline. (2)+(3) "Add a Formation button to the bottom right of the placement section" / tapping it "opens a popup with one page" of three specific text lines "The three photos sit below the text, and user can swipe left or right to see each one." Built as `#btn-formation`, appended fresh inside `#battlefield` on every `renderDeployBattlefield()` call (that function already rebuilds the whole box's content from scratch each render, so this needed no separate show/hide wiring, unlike End Turn/battle-controls) — `#battlefield` gained `position: relative` so the button can anchor to its corner. No real image assets exist anywhere in this project (checked — it's all emoji/CSS), so the "three photos" are inline SVG mini-diagrams built from the same emoji iconography (🏹/⚔️) and color palette (gold `#ffd700` for Melee, blue `#4facfe` for Ranged, red `#f5576c` for "enemy attack") already used throughout the rest of the UI, rather than introducing new art assets or an external image dependency. The carousel supports pointer-based swipe (down→up dx threshold, works for touch/mouse/pen alike) plus tappable dot indicators and ‹/› buttons, so it's fully usable without a touchscreen too (needed for testing, and for desktop players). Found and fixed a real CSS stacking bug while building this: `#formation-track`'s own `transform` (used to animate the slide offset) implicitly creates a stacking context per spec even at `position: static`, and being later in the DOM than the `position: absolute` ‹/› buttons, painted over them — gave `.formation-nav` an explicit `z-index: 2` to fix. Verified via scripted Playwright: outline confirmed removed (`outline-style: none`) while the empty-melee stretch-fill behavior still works; button anchored correctly to the corner and absent from the live battle screen; ‹/›, dots, and simulated swipe gestures (both directions, with correct clamping at each end) all navigate the carousel correctly; modal opens and closes cleanly.

**2026-08-19 (character placement page pt.3)** – Moved the Formation button from an absolutely-positioned overlay inside `#battlefield`'s bottom-right corner to a static row (`#formation-btn-wrap`) directly underneath the placement box, right-aligned to its edge.
Reason: Direct feedback on a screenshot — "the formation button is inside the placement section where you put melee characters. It should sit just outside to the bottom right. Or maybe it should be just underneath it." The original corner-overlay placement could sit on top of a placed card when the bottom row was filled. Chose "underneath, right-aligned" over a pure corner-outside-the-border approach since it fully eliminates any possibility of overlapping a card regardless of team composition, while still reading as "bottom right" (right-aligned directly below the box satisfies both phrasings the user offered). This also let the button become a static element instead of one `renderDeployBattlefield()` had to rebuild every render — moved its `onclick` wiring to the same one-time setup block as the other Formation modal controls, and toggled its wrapper's visibility the same way `#end-turn-wrap` already is (shown in `initDeployment()`, hidden in `startBattle()`). Removed `#battlefield`'s `position: relative`, added only for the old absolute-positioned button and no longer used by anything. Verified via scripted Playwright with the bottom row of both Ranged and Melee filled (the case most likely to overlap under the old approach): zero vertical overlap, right edges flush, modal still opens/closes, button confirmed absent (`display: none`) on the live battle screen.

**2026-08-19 (character placement page pt.4)** – The deployment filter chips (Melee/Ranged/Heroes/Non-Heroes/Clear) now live behind a collapsible "▾ Filter" toggle instead of always being visible, gained a "🎨 Colors" second-level disclosure exposing 5 color chips as real filters, Clear grays out until a filter is active, the deployed-count text changed format, and the placement screen's controls row was reordered to Formation / count / Filter.
Reason: Direct instruction, four items in one request. (1)+(2) Filter icon + collapsible section + Clear disabled-until-active: implemented as `#btn-filter-toggle` toggling `#deploy-filter-bar`'s visibility (`deployFilterBarOpen` state, reset closed on every `initDeployment()`), and `.filter-chip.clear-chip.disabled` (via `pointer-events: none`, so it's genuinely unclickable, not just dimmed) driven by `deployActiveFilters.size > 0`. (1, Colors) implemented as its OWN nested disclosure (`deployColorsOpen`) rather than a filter itself — tapping "Colors" only reveals/hides a sub-row of the 5 actual color-filter chips, which extend `deployActiveFilters` with `'color-<name>'` entries; `renderDeployRoster()`'s existing "OR within a group, AND across groups" filter logic (documented in its own comment) was extended with this as a third group, exactly matching how type and hero-status already combine — no new combination rule needed, the existing one already generalizes cleanly to N groups. (3) `updateDeployStatus()`'s text changed from "N of 5 characters deployed" to "Deployed: N/5", verbatim as requested. (4) The three controls (Formation button, deployed count, Filter toggle) were pulled into one new centered horizontal row, `#placement-controls`, replacing the previous single-item `#formation-btn-wrap` from the last change (which was right-aligned, not centered) — `#deploy-status` was relocated out of `#deploy-panel` into this row (kept its id, `updateDeployStatus()` untouched), and `#deploy-filter-bar` was relocated to sit directly below this row so the expanding panel appears right under the button that opens it, rather than its old spot above the roster list. Verified via scripted Playwright: status text format; filter bar starts and resets collapsed; Clear starts disabled and enables the instant a filter is applied; the Colors sub-row toggles independently of any actual color filter being active; a combined Melee+Red filter correctly narrows the roster (verified exact count and that every visible card's `data-color` was red); Clear resets both the active-filter set and Clear's own disabled look; the live battle screen confirmed to hide both the new controls row and the filter bar, matching how Formation already worked.

**2026-08-19 (character placement page pt.5)** – Removed the instructional blurb and running message log from both the placement and live battle screens (kept the underlying elements, just `display: none`), made the Character Codex link deployment-only (folded into the placement controls row instead of a permanent top-of-page link), and split the placement screen's controls into two rows: Formation/Deployed-count/Codex, then Filter alone underneath.
Reason: Direct instruction with an annotated screenshot (both the top instructional text and the message log crossed out on both the placement and battle screens; the Codex button also crossed out specifically on the battle screen). `.info`/`#log` were hidden via CSS rather than deleted from the DOM: `log()` (used pervasively — every attack, match, turn transition, and win/loss message routes through it) writes to `#log.innerHTML` with no null-guard, so removing the element outright would throw on the next call. Verified live that `log()` still runs cleanly while hidden. The Character Codex link (`.topbar`/`.nav-btn`) was previously a fixed top-of-page element shown on both screens regardless of phase — moved it into `#placement-controls` (restyled to match Formation/Filter's pill button look) so it inherits that container's existing deployment-only show/hide toggle for free, satisfying "remove codex button" on the battle page without any new visibility logic. Filter's own row (`#filter-toggle-wrap`) was added directly below the Formation/Deployed/Codex row, and `#deploy-filter-bar` moved to sit below THAT row instead, so the expanding panel still opens directly beneath the button that reveals it. While in this code, also fixed an unreported staleness bug: `initDeployment()` reset the `deployFilterBarOpen` state variable but not the Filter button's own DOM text/class, so re-entering deployment after a battle (e.g. via "Change Deployment") could show it still labeled "▴ Filter" in its open styling even though the panel was freshly collapsed. Verified via scripted Playwright + screenshots: `.info`/`#log` both `display: none` on deploy and battle; Codex link present and correctly targeting `Roster.html` on deploy, invisible (parent hidden) on battle; two-row layout renders as specified, filter panel opens in the right place.

**2026-08-19 (match-3 board)** – Board tiles no longer show a color letter, and the swap gesture changed from tap-tile-then-tap-neighbor to tap-and-hold-then-drag/swipe onto a neighbor — a swap fires as soon as the drag crosses a small distance threshold in a clear direction, matching standard match-3 UX (Candy Crush and similar).
Reason: Direct instruction, two items: "Remove the letters off of the tiles" and "Allow tap-n-hold a tile, then drag/swipe it onto another neighboring tile... Like most well known match 3 games do it." The letter removal was a one-line change (`renderBoard()`'s `textContent`). The gesture change was a bigger rework: `onTileClick()` (a single click handler doing double duty as "select tile 1" then "select tile 2, swap if adjacent") was replaced with pointer-event handlers (`onTilePointerDown`/`Move`/`Up`/`Cancel`) that track a press-and-drag in real time, firing the swap via a new `attemptTileSwap(x1,y1,x2,y2)` the instant movement crosses `TILE_SWIPE_THRESHOLD_PX` (16px) in a clear dominant direction — mirroring the pointer-based drag pattern already established for placing Ranged/Melee cards on the deployment screen, for consistency. The old two-tap "selected tile" state (`selectedTile`) was removed entirely (variable + every reset site) rather than left as dead code, per the project's own "delete what's certainly unused" convention — a defensive `endTileDrag()` (which also removes the window-level pointermove/pointerup/pointercancel listeners, not just nulls a variable) was substituted at each of those reset sites instead. The Runic Crystal's tap-only activation rule and the Arcane Bomb's "any adjacent swap touches it" rule were both preserved exactly by extracting them into their own functions (`activateCrystalTap`, and a crystal guard inside `attemptTileSwap`) rather than being redesigned — a crystal still can never be one half of a swap, only a genuine no-drag tap activates it. Verified via scripted Playwright, with each scenario run in its own fresh page load after an earlier attempt at chaining scenarios in one page surfaced test-harness contamination (overlapping match-cascade timers from a previous scenario bleeding into the next, and a "fill the whole board with one filler color" setup that itself formed one giant accidental match) — once isolated, all scenarios passed cleanly: vertical drag-swap creating a match, a no-match swap correctly reverting, an off-the-edge drag being a safe no-op, a drag onto a crystal being a no-op while a plain tap on it still opens its modal, and a bomb still detonating from a touching swap with no match required.

**2026-08-19 (match-3 board pt.2)** – The Arcane Bomb now also activates on a direct tap, same as the Runic Crystal — previously only a touching swap (or being matched into a group) set it off.
Reason: Direct bug report — "Tapping the arcane bomb does not make it explode, I have to swap a tile with it for it to activate." Checked against the design history: the Crystal was always meant to be tap-activatable (it's how it's always worked), but the Bomb never had that path — its documented triggers were only "matched into a group" and "any adjacent swap touches it." Since tapping-to-activate is a natural expectation once one special tile has it, and nothing about the Bomb's existing behavior needed to change to add it, generalized the existing `activateCrystalTap(x, y)` into `activateSpecialTap(x, y, type)` and call it for both `'bomb'` and `'crystal'` from the no-drag tap branch of `onTilePointerUp()`. Deliberately additive, not a replacement: the Bomb keeps its "any adjacent swap touches it" trigger and can still be dragged as part of a normal swap (unlike the Crystal, which `attemptTileSwap()` still refuses to let participate in a swap at all) — tap is just a faster third way to set it off without needing a neighbor. Verified via scripted Playwright: a plain tap (zero-movement pointerdown+pointerup) on a Bomb now detonates it and deals damage to a random enemy Melee; Crystal tap-to-open-modal still works; Bomb still detonates via a touching swap with no color-match involved — both regression-checked to confirm nothing else changed.

**2026-08-19 (battle screen)** – Added an "Auto Attack" toggle to the battle screen: when on, each living player hero that can afford its Basic auto-attacks a random valid target once the player's own swap resolves; a Hero that can't yet afford Special has a random chance to bank mana instead. Positioned far-left of End Turn, which stays genuinely centered regardless of Auto Attack's own (variable) width.
Reason: Direct request, but first scoped as a sizing question ("how big would this be?") before building anything — answered that it was small-to-medium specifically because the enemy AI (`enemyTurn()`) already implements almost the exact logic needed (valid-target selection respecting the melee/ranged lane rules, Special-vs-Basic affordability), so most of the hard part was already built and battle-tested; the only genuinely new piece was the "randomly bank mana toward a bigger attack" decision for Heroes, which the enemy AI doesn't do (it always attacks when it can afford to). Once the user confirmed "random valid target" (not a smarter heuristic) and specified the exact layout (Auto Attack far-left, End Turn centered), implemented `scheduleAutoAttack()`/`runNextAutoAttack()`/`cancelAutoAttack()`, chaining through the *existing* `checkAutoEndPlayerTurn()`/`performAttack()` call relationship rather than building a separate resolution loop — `runNextAutoAttack()` resolves one hero and lets `performAttack()`'s own already-existing call to `checkAutoEndPlayerTurn()` continue the chain (which itself now calls `scheduleAutoAttack()` again if heroes remain), so no new turn-ending logic was needed at all. The toggle is a standing preference, not per-battle state — deliberately NOT reset by `startBattle()`/`restartBattle()`, since a player who wants to semi-auto-play shouldn't have to re-enable it after every restart; `cancelAutoAttack()` was still added at those reset points (alongside the existing `endTileDrag()`) purely to clear any in-flight scheduled hop, so a stray timer from a previous battle can never fire into a fresh one.
Found and fixed a real layout bug while building this: making both buttons `position: absolute` (needed so End Turn's centering doesn't depend on Auto Attack's own width, which changes between its "OFF"/"ON" labels — a flex/grid balancing approach was tried first and rejected for exactly this reason, confirmed via a live measurement showing the two `1fr` grid columns weren't actually equal because the browser still respects each track's min-content size by default) caused `#end-turn-wrap` — a flex child of `body` under `align-items: center` — to collapse to zero width, since neither of its now-absolutely-positioned children contributes to its intrinsic size anymore; the resulting zero-width, auto-centered box silently placed both buttons directly on top of each other at the page's horizontal center. Fixed by giving `#end-turn-wrap` an explicit `width: 100%; max-width: 420px;` so its size no longer depends on its children at all. Also shortened both button labels, since even after the width fix the original longer text ("▶ Auto Attack: OFF", "End Turn (bank remaining mana)") was wide enough for the two buttons to visually collide at a typical mobile width — moved the fuller descriptions to `title` tooltips instead of dropping them entirely. Verified via scripted Playwright: precise non-overlapping bounding-box checks for both buttons, a full auto-attack cascade (including a Hero correctly using Special) ending the turn correctly, disabling mid-cascade immediately halting further attacks, and the setting plus a clean timer state both surviving `restartBattle()`.

**2026-08-19 (battle screen pt.2)** – Replaced the manual attack flow's old "tap your hero, then tap an enemy to confirm" two-step selection with a one-tap default (tap a "glowing" hero → attacks a computed Logical Target immediately) plus an optional, order-reversed pre-aim ("tap enemy, then hero, for an aimed attack").
Reason: Direct, precisely specified instruction covering both the targeting algorithm and the exact input sequence. Targeting: `pickLogicalTarget()` ranks only `getValidTargets()`'s existing legal-target list (explicitly "not raw pixel distance", so nothing geometric was introduced) by highest current mana, then lowest HP, then nearest lane — implemented exactly as the three numbered sub-points specified, with "closest to special" read as "highest current mana" since `SPECIAL_COST` is one flat threshold shared by every character (no per-character special-cost variance to normalize against). "Nearest lane" needed a lane concept that works for both attacker and target regardless of type, since Melee has no `laneIndex` of its own — reused the already-existing `primaryLane(coverage)` helper (previously only used for sorting deployment lists) via a new small `heroLane()` wrapper, rather than inventing a second lane-resolution scheme. Input model: since attacks now resolve in a single hop (immediately for normal characters, or the instant Basic/Special is chosen from a Hero's still-unchanged popup), the old `selectedHero`/`selectedAttackType` state — which existed purely to remember "a hero is waiting for its target tap" — no longer had a reason to exist, so it was removed entirely (variable declarations and every reset call site) rather than left as unused/dead state, consistent with the project's own "delete what's certainly unused" convention. A new `aimedTarget` (enemy-only, optional, single-use — consumed by the very next attack regardless of outcome) replaces it at those same reset sites. The pre-existing green `.card.selected` glow was repurposed to mean "this is the pre-aimed enemy" instead of its old meaning ("this player hero is mid-selection"), since that old meaning no longer exists and the visual language (a highlighted glow marking "the thing about to matter") still fits the new meaning directly. Added a new, distinct gold pulsing glow (`.card.can-act`, driven by `canHeroAct()` mirroring `onHeroClick`'s own gating) so "glowing hero" from the spec has a concrete, always-in-sync visual meaning — deliberately a different color from the aim glow so a player can't confuse "this enemy is aimed" with "this hero can act." An aimed target that turns out illegal for whichever specific hero is eventually tapped (e.g. aimed at a Ranged unit later found unreachable by the Melee hero tapped) silently falls back to the Logical Target rather than blocking the attack or surfacing an error — chosen because turns are already time-pressured (every hero needs to resolve each turn) and a hard block would punish a pre-aim that only became stale because of an earlier action, not a mistake. Auto Attack was deliberately left untouched (still uniformly random) — it wasn't part of this request, which was explicitly scoped to manual play ("when not aiming"), and Auto Attack's own targeting was separately confirmed as "random valid target" in an earlier exchange; flagged in TODO.md as a question worth asking rather than assumed. Verified via scripted Playwright: a controlled mana/HP setup confirms the exact tie-break order (mana desc, then HP asc); pre-aiming a target Logical Targeting would NOT have picked and then attacking with it confirms the aim overrides the default; tapping the same aimed enemy twice clears it; the Hero popup still opens on tap (not an immediate attack) and only resolves once Basic/Special is chosen; the can-act glow is absent pre-swap, present once affordable, gone once acted; an aim that's illegal for the eventual attacker correctly falls back to the Logical Target instead of blocking.

**2026-08-19 (battle screen pt.3)** – Fixed the can-act glow going stale for heroes not directly touched by the triggering action, and added a second, separate gold pulse on a Hero's mana bar once it's completely full.
Reason: Direct report — "User characters should always glow when they can attack" — traced to a real bug rather than treated as a restatement: `grantMana()` (called per matched color during match resolution) only refreshes `updateHeroUI()` for heroes of that specific color, so a hero whose color wasn't part of the swap never had its `.can-act` class re-evaluated even after `swapUsedThisTurn` flipped true — meaning a hero sitting on mana banked from a previous turn could be fully eligible to act and simply never light up. Fixed at the root rather than patching each call site: moved `heroes.forEach(updateHeroUI)` into `checkAutoEndPlayerTurn()`, which is already the single function invoked after every swap resolution, attack, and Do Nothing choice — so the fix covers every trigger path in one place instead of needing a matching patch at each of them. The second request ("mana bar should also glow, only for heroes, when mana is full") was implemented as its own class (`.mana-bar.mana-full`, gold pulse — same color family as `.can-act` for one consistent "ready" visual language) gated on `h.isSpecial && h.mana >= h.maxMana`, toggled in the same `updateHeroUI()` pass so it inherits the same now-fixed refresh guarantee. Verified via scripted Playwright: a two-hero, two-color setup where only one color's swap matched confirmed both heroes correctly show `can-act` afterward (previously only the matched-color one would); a Hero's mana bar shows `mana-full` at max mana while a normal character's identically-full bar does not; confirmed the gold glow is actually being painted (not just the class toggled) via a computed-style check on `box-shadow`.

**2026-08-25 (battle screen pt.4)** – Tile swaps now slide visually (FLIP animation) instead of instantly teleporting via re-render; live battle HP is now a depleting bar with the numeric ratio still overlaid; and every tile cleared by a match (or bomb/crystal activation) spawns small glittering shard particles that fly toward the mana bar of each living player hero sharing that tile's color.
Reason: Three direct, independently-scoped requests bundled in one message ("Looking good!" followed by a numbered list), implemented together since none touched the same code paths. (1) Swap animation: the prior `attemptTileSwap()` mutated `board`/`boardSpecial` and called `renderBoard()` immediately, which is an instant full re-render — visually a teleport, since the DOM nodes for the two swapped cells are recreated fresh at their new grid positions with no transition. Rather than trying to animate a diff-based re-render, used the standard FLIP technique: swap the data and re-render first (so the "Last" state already exists in the DOM), then read the now-repositioned elements' `getBoundingClientRect()`, apply an inverse `transform:translate()` so they appear to still be at their old ("First") position, force a reflow, then animate the transform back to identity on the next frame. This keeps `renderBoard()`'s existing "always redraw the whole board from data" model completely intact — no per-cell diffing was introduced — the animation is a purely visual overlay on top of an already-correct DOM. Chose 160ms as fast enough not to feel like added turn latency but slow enough to actually read as a slide rather than a flicker; a revert-on-no-match plays the identical animation in reverse rather than a different one, so a failed swap doesn't look mechanically different from a successful one until the match outcome is known. This did remove the old fixed ~120ms `setTimeout` pause that used to sit between the swap and `processMatches()` — the slide itself now serves that pacing role, so no unnecessary double-delay was added. (2) HP bar: chosen to mirror the existing mana bar's visual language (`.mana-bar-bg`/`.mana-bar`) for consistency, but kept the numeric ratio as an overlay per explicit instruction ("leave the number visible for now") rather than dropping it — read as a deliberate signal that a number-free bar might come later once the player is used to reading bars at a glance, so the two were kept as separate, easily-removable pieces (`.hp-bar` for the fill, `.hp-num` as a purely decorative absolutely-positioned overlay) rather than baked together. Deliberately scoped to the live battle screen's `buildHeroCardEl()`/`updateHeroUI()` only — the deployment screen's `buildDeployPreviewCard` was left on its old plain `.hp-text`, since deployment always shows full/undamaged HP and a bar would always render as 100% full, adding visual noise with no information. (3) Mana shards: hooked into the one place in `processMatches()` where every cleared cell — normal 3/4/5-matches, bomb-blast neighbors, and crystal full-color clears alike — gets nulled out, specifically so the effect is automatically consistent across all clear paths without needing to be called separately from each one (a bomb explosion or crystal clear could easily have been missed if the hook were placed at a match-specific call site instead). Spawns 3 shards per matching hero (not 1) purely for a fuller "glittering" look at a small board-tile size, staggered ~45ms apart so they read as a small burst rather than a single dot; targets the hero's `.mana-bar-bg` center specifically (not the whole card) since that's the element the shard is thematically "delivering to." Explicitly a cosmetic-only effect — it reads the same cleared-cell color/position data `grantMana()` already uses but computes nothing itself and cannot desync from the real mana math, since both are driven off the same `toClear` set in the same commit loop. Verified via scripted Playwright (HP bar width/number tracking real HP through damage; a non-identity `transform` matrix confirmed present mid-swap-animation; shard elements confirmed created then fully self-removed after a forced match; zero console errors) plus a real-browser visual pass via screenshots (deployed a small team, forced damage to inspect the HP bar's appearance, forced a same-color match to catch shards mid-flight) — the HP bar reads clearly at a glance and the shards render as small glowing diamonds departing the matched tile, matching the "tiny glittering shards" description.

**2026-08-25 (battle screen pt.5)** – Widened the existing pre-enemy-turn pause into a clearer, named beat (`ENEMY_TURN_START_DELAY_MS`, 600ms → 900ms); enemy mana gain now spawns shard particles falling from off-screen above the viewport into the boosted hero's mana bar.
Reason: Two more direct, small-scoped requests. (1) A pause between the player's turn ending and the enemy acting already existed (`setTimeout(enemyTurn, 600)`) but wasn't reading as deliberate — likely because the "Enemy Turn..." indicator and board lock both fire instantly in `endPlayerTurn()`, so with only 600ms before mana/attacks started, the whole handoff could read as one continuous beat rather than two distinct steps. Pulled the magic number into `ENEMY_TURN_START_DELAY_MS` and raised it to 900ms — enough to register as a genuine pause without adding noticeable turn latency, per "slight." (2) Enemy mana shards: extended the existing player-side mana-shard flourish (added last round) to the enemy's per-turn "simulates their match" mana boost, which previously just silently incremented `mana` and called `updateHeroUI()` with zero visual acknowledgment of where it came from. The player's version starts shards at a real board tile; the enemy has no such board to point to, so per explicit instruction ("as if their mana board is hidden above the screen") the shards instead spawn just above the top edge of the viewport (`top: -20px`, comfortably off-screen so nothing is visible until the fall begins) and fall straight down into the boosted hero's own mana bar — giving the gain a visual origin without needing to fabricate or render an actual off-screen board. Refactored the guts of `spawnManaShards()` (the per-shard spawn/stagger/animate/cleanup loop) out into a shared `flyManaShards(startX,startY,endX,endY,hex,count)` helper rather than duplicating that ~20-line block for the new `spawnEnemyManaShards(hero)` — the two functions now differ only in how they compute their start point and color, which is exactly the kind of near-identical duplication worth factoring out. Also pushed the enemy's first-attack delay from 400ms to 700ms, since the shard flight (~590ms worst-case, including its own internal stagger) would otherwise still be airborne when the first attack fired — a hero attacking before the player has even seen its mana arrive would undercut the whole point of adding the visual. Verified via scripted Playwright: calling `endPlayerTurn()` directly and sampling at 700ms confirms zero shards exist yet (proving the 900ms pause genuinely holds, not just cosmetically), sampling past 900ms shows exactly 3 shards per boosted enemy, and live `getBoundingClientRect()` reads during the flight confirm real on-screen movement (not just CSS target values) descending toward the correct mana bar; shards fully self-remove once their flight completes. Followed up with a real-browser screenshot mid-flight (~1150ms after the turn-end call) showing two small glowing diamonds visibly descending from above the battlefield into an enemy hero's mana bar. Zero console errors throughout.

**2026-08-25 (match-3 board, reverted)** – Rolled back the "pure connected-blob matching" change (Empires & Puzzles-style, any orthogonally-touching 3+ same-color group matches regardless of shape) at the user's request immediately after it shipped ("Uh oh, I made a mistake on that. Please roll back changes.").
Reason: The user flagged the request itself as a mistake right after seeing it implemented, not a bug in the implementation — no root cause to chase, just an undo. Reverted via `git revert` (commit `8f10e71`) rather than a hard reset, since the change was already pushed to both `main` and the working branch and a revert commit is the safe way to undo already-shared history. This restored straight-run match detection (`findMatchedCells`/`findMatchGroups` requiring an actual row/column run of ≥3 before flood-fill can merge cells into a shape) and reverted the CLAUDE.md/GAME_MECHANICS.md/TODO.md wording back to describing that rule. Flagging here specifically so a future AI doesn't reintroduce blob-style matching by inference from an old TODO/DECISIONS mention without confirming with the user first — the underlying idea may still come back later, but only if asked for again.

**2026-08-25 (character placement page pt.6)** – Moved the Formation popup's melee-coverage explainer into a speech-bubble tooltip just left of the button; repurposed the Formation popup itself into a swipeable picker of 6 preset unit-placement layouts.
Reason: Two related but distinct requests bundled together — "take that pop-up information and create a bubble just left of the formation button," then separately "give a few options for unit placement... about six pre-set options... swipe left or right." (1) Bubble: kept the same 3 text bullets from the old popup verbatim, but deliberately dropped its 3 illustrative SVG diagrams rather than also moving them into the bubble — a small tooltip isn't a good home for a 3-slide carousel, and the new preset slides (below) now illustrate the same underlying mechanic with real, complete formations rather than one isolated rule each, making the old single-rule diagrams redundant rather than lost. Positioning turned out to be the hard part: a straightforward CSS `right:100%` anchor relative to the button's own positioned ancestor looked correct in isolation but actually ran the bubble off the left edge of the viewport, since the info button sits only ~64px from the page's left edge on this ~420px-max-width mobile layout — nowhere near the ~178px (168px bubble + 10px gap) a purely-CSS anchor would need. Switched to `position:fixed` with a JS-computed `left`/`top` (`positionFormationInfoBubble()`), clamped to `max(8px, ...)` so it always stays on-screen even though that means it sometimes overlaps nearby controls it pops out over — an acceptable, common pattern for a transient, tap-away-to-close tooltip overlay. Along the way, also caught and fixed a real (if minor) pre-existing-pattern bug: `offsetWidth` read on the bubble while it was still `display:none` returned 0, silently breaking the clamp math — fixed by hardcoding the bubble's known CSS width as a constant instead of reading a layout property that isn't reliable before the element is visible. Also had to widen `#placement-controls` from 220px to 300px — adding the new info button pushed the row's natural content width past the old cap, and because the row uses `justify-content:center`, overflow past that cap doesn't visibly clip anything; it just silently shifts the whole row (and every child's measured position) off from where the container's own box appears to be, which is exactly the kind of bug that looks fine on casual visual inspection but breaks any position math anchored to a specific child — caught here via a real bounding-rect comparison, not eyeballing a screenshot. (2) Presets: designed 6 concrete Ranged-row/Melee-band layouts using the player's real `RANGED_SLOTS` grid (0..4) rather than reusing `ENEMY_FORMATIONS`'s simpler "lane" model, because the enemy's formations aren't actually legal under the player's own `bandsConflict()` overlap rule (e.g. the enemy's "aligned + boundary straddling the same lane" pattern is only valid because the enemy renders as a plain non-overlap-checked list, not a pixel-aligned grid) — every preset here was instead hand-designed and then verified pairwise conflict-free against the real `bandsConflict()` logic before being committed to code. Chose a genuinely varied set rather than 6 near-identical "just move the same shape around" options: two boundary-heavy efficiency plays (Twin Wall, Bottleneck), one aligned-heavy defensive spread (Checkerboard), one melee-flooded glass-cannon-support shape (Spearhead), one hybrid guard-plus-extra-attackers shape (Vanguard Brawl), and one deliberately minimal starter pair (Solo Guard, the only preset below `TEAM_CAP`) — covering the space of "efficient," "risky," "melee-heavy," and "lean" rather than six variations on one idea. `applyFormationPreset()` was designed to reorganize rather than discard: it releases everyone currently placed back to a pool, then refills the preset's shape preferring those same released heroes (by type-fit) before reaching into the wider roster, so choosing a preset after already hand-picking a team rearranges that team into an efficient shape instead of throwing the player's choices away — falling back to the roster only for whatever the preset needs that the player didn't already have enough of, and returning any now-excess heroes to the roster if the preset is smaller than what was deployed. Verified via scripted Playwright covering all 6 presets' exact resulting counts and on-screen card counts, a downsize case confirming excess heroes correctly return to the roster, bubble open/close/positioning/content, carousel navigation through the real preset data, and that pre-existing manual placement (tap +/-, Filter toggle) still works after using a preset — zero console errors. Followed up with real-browser screenshots of the bubble and two different preset diagrams (plus the resulting live battlefield) to confirm visual quality, not just the underlying data.

**2026-08-25 (battle screen pt.6)** – Wired real pixel-art assets (pushed by the user directly to the repo under `/Artwork`) into live battle: random day/night battlefield backgrounds, and Idle/Attack/Impacted character pose art mapped onto whichever roster characters actually get deployed.
Reason: Direct request, arriving in two parts across the conversation. First, the user described the feature before the art existed ("I put some maps and artwork in the folder structure... character folders have poses") — but this session runs in an isolated cloud container cloned fresh from GitHub, with no access to the user's local filesystem (`D:\Documents\...`), so the actual work couldn't start until the user found a way to get the files into the repo themselves. Walked them through GitHub's web "Add file → Upload files" flow (no git CLI needed) rather than assuming they could run git commands locally, since they'd explicitly said "idk how to add them directly." Once pushed to `main` as a single "Artwork" commit, fast-forward-merged it into this working branch — trivial since it only added new binary files, no code changes to conflict with. Design decisions once the actual asset inventory was known (25 files: 4 battlefield backgrounds, 7 character folders each with 3 poses): (1) Maps — the 4 backgrounds split evenly into 2 "with a diamond-tiled arena floor at the bottom" (plain name, e.g. "Day Forest Map.png") and 2 "without" ("...Map2.png") — inspected several to confirm both day and both night variants share the same composition, then treated all 4 as one flat random pool rather than trying to assign the two variants different in-game roles, since nothing in the request or the art itself suggested they serve different purposes — "random day and night maps, changes every battle" is satisfied by a flat 4-way random pick, and it maximizes use of the provided variety. Applied as `#battlefield`'s background with a flat dark tint layered over it (`linear-gradient(rgba(10,10,20,0.55)...), url(...)`) so the existing YOU/ENEMY labels and translucent card content stay legible against a busy painted scene — reused the identical "gradient tint over art" pattern for hero card art below, for one consistent visual language. Picked fresh on both `startBattle()` and `restartBattle()` (not just first start) since a same-team restart still reads to the player as "starting the fight over," matching "changes every battle" in spirit; reset to `none` in `initDeployment()` so the roster-picking screen doesn't show a stale map from a previous fight. (2) Character skins — inspected representative samples from most of the 7 character folders (not all, but enough to build confidence) to confirm the naming convention (`Idle ___`/`Attacking ___`/`Impacted ___`, with two folders using a bare un-prefixed filename for the Attack pose specifically — verified by actually opening those two and seeing a mid-swing/firing pose in both). Sorted the 7 into 3 Ranged (all bow-wielding: Female Archer, Male Armored Bowman, Male Heavy Bowman) and 4 Melee (Male Armored Spearman, Male Spearman, Male Swordman, Male Warhammer) by weapon type shown in the art — the original request said "three ranged and three melee" (written before the art existed), but the actual delivered art has 4 melee skins, not 3; used all 4 rather than arbitrarily dropping one to force the original number, since more variety is strictly better and nothing about the mechanic requires exactly 3. `assignHeroArt()` groups the battle's `heroes` array by side+type and assigns from the matching pool in deployment order, wrapping via `% pool.length` once a side has more of that type than the pool has skins — directly implements "if they select more than three of either type, use a copy of that same type," generalized to whatever the real pool size turned out to be per type. Chose per-side independent cycling (not one shared pool across both sides) so the player's own team and the enemy's team each start their own type's pool fresh at index 0, rather than a player's 2nd Ranged hero visually matching whichever skin the enemy's Ranged happened to consume first — keeps each side's own roster order the only thing that determines its own skin assignment. Deployment preview cards were deliberately left untouched (still the old emoji-icon placeholder) — skin assignment depends on the FINAL battle-time deployment order across the whole team, which only exists once `buildArmies()` runs at `startBattle()`, so there's no correct skin to preview before that point without inventing a second, likely-inconsistent assignment scheme just for the preview.
Found and fixed a real bug while building the pose-art rendering itself: the first implementation added an `<img class="card-art">` child, positioned with `z-index:-1` specifically so it would sit behind the card's existing text/bars while still being a real DOM element (easy to swap `.src` on for pose changes). It rendered as a completely blank card — the image never appeared at all, confirmed via a cropped screenshot of just the card. Root-caused by testing the theory directly rather than guessing: setting the same image's z-index to a large POSITIVE number made it immediately visible (proving the image itself, its `src`, and its box dimensions were all fine) — meaning the problem was specifically about negative-z-index stacking. The actual cause: `.card` has `position: relative` but no explicit `z-index`, and `position:relative` alone does **not** establish a new CSS stacking context (only `position` combined with a non-`auto` `z-index`, or a handful of other properties like `transform`/`opacity`/`filter`, does) — so the child's `z-index:-1` wasn't scoped to "behind this card's own background," it escaped to the next real stacking-context ancestor up the tree and painted behind unrelated content there instead, nowhere near visible. First attempted fix was adding an explicit `z-index: 0` to `.card` itself (which does force a stacking context) — this technically worked but felt like patching around a fragile mechanism, so instead switched approaches entirely: dropped the `<img>` child and applied the art as a **CSS `background-image`** on the card element directly (`heroArtBackground(url)`, a shared helper building the same tinted-overlay-plus-art string used by both `buildHeroCardEl()` at build time and `triggerHeroPose()` at pose-change time via `hero.el.style.backgroundImage`). A background-image is guaranteed by the CSS painting-order spec to render below all of an element's own box content, full stop — no stacking context, z-index, or DOM-order subtleties involved at all, which is a strictly more robust guarantee than "make sure this specific ancestor happens to form a stacking context." `triggerHeroPose()` clears any pending revert-to-Idle timer before starting a new one (so a hero hit again mid-flash doesn't have a stale timer snap it back under a fresher pose) and is hooked into both `performAttack()` (attacker → Attack, target → Impacted) and `dealBoardDamage()` (target → Impacted, covering Bomb/Runic Wave damage too) — deliberately fire-and-forget, reading the same already-applied damage/mana state rather than gating any of it, same pattern as the existing mana shard particles. Verified via scripted Playwright: correct skin assignment and cycling per side+type; zero failed art requests; background correctly reflects a real map during battle and `none` during deployment; attack/impacted poses correctly apply and revert on schedule; skins and HP correctly persist/reset across `restartBattle()`; the map genuinely varies across repeated restarts; and a full re-run of every previously-existing test (Formation presets, deployment placement, enemy turn pause/shards) still passes with zero regressions. Followed up with real-browser screenshots of the fixed card art, the full battlefield with a map background, and a hero mid-attack-pose to confirm visual quality, not just the underlying data/DOM state.

**2026-08-25 (battle screen pt.7)** – Stripped the "card" box look from live battle hero cards entirely — no background, no border, no fixed height — and moved HP/mana bars to sit below the now fully-visible sprite instead of overlaid on top of it.
Reason: Direct, precisely-worded request scoped explicitly to battle ("Changes during battles: 1. Move health and mana bar just below the character sprite. 2. Remove the card looks, leaving the sprite fully visible with health and mana bar just below it."). Implemented as a new `.card.sprite-style` modifier (added only by `buildHeroCardEl()`, i.e. only for live battle — the deployment preview card's own boxed `.card` look is untouched, since nothing in the request mentioned deployment and its cards don't have "a sprite" to show mid-attack anyway) that zeroes out the background/border/padding/fixed-height chrome from the base `.card` rule. Restructured the markup from "name, then art-as-card-background with HP/mana overlaid on top" to a plain top-to-bottom flex stack: header row (color dot + name) → `.hero-sprite` (a new dedicated square div, `background-size:contain` so the full source art — all roughly 1:1 square canvases, verified by reading their actual PNG dimensions rather than assuming — is always shown uncropped, which is the literal meaning of "fully visible") → HP bar → mana bar → coverage tag. Two real bugs surfaced and were fixed while building this, neither found by reasoning alone — both confirmed by testing the actual rendered page:
1. **The color dot started covering the hero's name.** `.card-color-dot` is a `position:absolute` corner overlay pinned to the card's own top-left corner — harmless in the old layout, where an icon glyph (not text) sat in that corner, but now that the name is the very first line of content, the corner-pinned dot landed directly on top of its first character or two, visible in a screenshot as "Korr the Unbroken" rendering as "...r the Unbrok...". Fixed by introducing a battle-only inline variant (`.card-color-dot-inline`, placed in a shared `.card-header-row` flexbox next to the name) rather than trying to nudge the existing corner-overlay class's coordinates — an approximate pixel offset would only ever be "usually right," while an inline flex layout is correct by construction regardless of name length or card width. Left `.card-color-dot` itself completely alone since the deployment preview card still needs its original corner-overlay behavior.
2. **The sprite was invisible again — a different failure than the z-index/stacking-context bug fixed last round.** The first pass set `style="background-image:${heroSpriteUrl(h.skin.idle)}"` directly inside the card's HTML template string. `heroSpriteUrl()` wraps its URL in `url("...")` — double quotes — which land inside the template's own double-quoted `style="..."` attribute value. The HTML parser has no way to know those inner quotes are meant to be part of the attribute value; it reads the attribute as terminating at the very first `"` it encounters, silently truncating everything after — no console error, no thrown exception, just a card with a completely blank sprite area. Confirmed via a `getBoundingClientRect()` + computed-style check showing `background-image: url("")` — an empty string, not a 404 or a malformed URL, which is what pointed at a quoting bug rather than a path/asset problem. Fixed by setting `.hero-sprite`'s background via the `.style.backgroundImage` **DOM property** after the template is inserted, mirroring the pattern `triggerHeroPose()` already used correctly for pose changes — a JS property assignment has no HTML-attribute quoting to escape at all, regardless of what characters the URL contains. Also updated `buildEmptySpriteSlot()` (the unfilled-Ranged-slot placeholder) to mirror the exact same header-row/sprite/bars structure — needed because Melee band alignment reads every Ranged row's live rendered rect, filled or not, and a placeholder built with old, now-mismatched dimensions would have thrown off where a boundary Melee card centers itself between two rows. Verified via scripted Playwright: `.hero-sprite`'s background (not the card's own, confirmed empty) carries the right Idle art for every hero; attack/impacted pose swap-and-revert still functions correctly against the relocated target element; zero failed art requests; and a full re-run of every previously-existing test suite (Formation presets, deployment placement, enemy turn pause/shards, restart persistence) passed with zero regressions. Followed up with real-browser screenshots of the fixed name/dot layout, the sprite rendering correctly across a full battlefield, and a mid-attack pose to confirm the swap still reads correctly in the new structure. Flagged in TODO.md that the new taller per-row footprint (full square sprite + name/bars, ×5 stacked Ranged rows) makes the battlefield section noticeably taller and more scroll-heavy on a phone-sized viewport — not addressed now since it wasn't raised as a problem, but worth a real-device check given the project's mobile-first goal.

**2026-08-25 (battle screen pt.8)** – Fixed a CSS specificity bug that made the previous round's "remove the card box look" change silently not work: cards still showed the old colored gradient background after shipping.
Reason: Direct bug report immediately after pt.7 shipped — "They still looks like they are in a cards." Didn't match the intent (fully sprite-visible, chrome-free), so verified against the actual computed style rather than re-reading the diff: `getComputedStyle(card).background` still showed `linear-gradient(160deg, rgb(26,42,74), rgb(42,42,74))`, the exact per-color tint gradient from `.card[data-color="blue"] { background: linear-gradient(...); }` (and its four sibling color rules). Root cause: CSS specificity is scored by selector *kind counts*, not selector length or "how specific it sounds" — `.card.sprite-style` (two class selectors) and `.card[data-color="blue"]` (one class + one attribute selector) score identically under the spec (attribute selectors count the same as classes), so when both rules set the same property (`background`) on the same element, the one declared LATER in the stylesheet wins the tie. The `[data-color="..."]` rules happen to sit later in this file than `.card.sprite-style` was inserted, so they silently re-applied the gradient over top of the "remove it" rule — no error, no warning, just a visually unchanged card despite the CSS technically containing the intended override. Fixed by adding a second, deliberately-more-specific rule: `.card.sprite-style[data-color] { background: none; }` — matching `[data-color]` with no value requirement still matches every real card (all of which always carry a `data-color` attribute), and adds a third selector to the specificity count, putting it strictly above the two-selector color rules regardless of which one happens to be declared later. This is a more durable fix than reordering the stylesheet (which would work today but silently re-break the moment anyone reorders CSS again without realizing the ordering was load-bearing) or reaching for `!important` (which stops the *next* override attempt from working at all, rather than fixing the actual specificity mismatch). Verified via a fresh computed-style check showing `background: none` for real, plus real-browser screenshots of a full battlefield confirming every card now shows only its sprite with no visible box — and a full re-run of the existing test suite (pose swap/revert, art/skin assignment, restart persistence, Formation presets, deployment regression) came back with zero regressions, confirming this was purely a CSS specificity fix with no behavioral side effects.

---

(Add new entries above this line)
